# RigPilot AI Context

Read this file before changing the repository. It records the current implementation, safety rules, evidence, and known gaps. The repository and passing tests are the source of truth. Planning transcripts and generated status reports are background only.

## Product boundary

RigPilot is a GPL-3.0 Windows desktop control suite inspired by G-Helper's low-friction workflow. Desktop hardware has no universal control interface, so every read or write is mediated by a capability-gated hardware adapter.

## Brand and compatibility

RigPilot is the public display brand as of 13 July 2026. `PCHelper` remains the deliberate internal identity for namespaces, service name, named-pipe names, CLI executable, local operator group, and `%ProgramData%\PCHelper` state. Do not rename those identifiers without a separately tested data and service migration.

Current workspace target: `0.5.5-alpha`, a protocol-v2 desktop-suite development line with transactional GPU control, continuous cooling, exact-route RGB, commissioning, gaming workflow, recovery, and contained adapter hosts. It is not a qualified release and it does not claim certified CPU, GPU, motherboard-fan, pump, AIO, firmware, or RGB families. Controlled reference-system passes remain Experimental and do not satisfy the certification gate. The unsigned `0.5.5-preview.1` release shape is compiled read-only: every service mutation returns `PUBLIC_PREVIEW_READ_ONLY` while monitoring remains available.

Supported operating-system target:

- Primary: Windows 11 24H2 or later, x64.
- Compatibility target: Windows 10 22H2 x64, with an end-of-support warning.
- Framework: .NET 10, WPF, framework-dependent x64 deployment.
- Initial qualification system: ASUS ROG Strix X570-E, Ryzen 7 5800X, RTX 3090.

Version 1.0 remains blocked until the physical qualification matrix in `COMPATIBILITY.md` passes. Never generalise support from one device to a product family.

## Non-negotiable safety rules

1. Never ship WinRing0, recommend disabling Windows security, or bypass the vulnerable-driver blocklist.
2. Privileged hardware access must use a signed PawnIO path or a documented vendor API.
3. A writable capability must publish discoverable bounds, risk, evidence, ownership, execution context, control domain, read-back support, and reset behaviour.
4. Built-in profiles remain within stock limits. Above-stock values are Experimental and require global plus per-device acknowledgement.
5. Never increase voltage automatically or apply positive voltage at startup. Manual positive voltage requires exact vendor bounds plus a fresh per-session device acknowledgement. RAM timing and voltage writes remain out of scope.
6. Hardware profiles contain typed actions only. User scripts are separate `ScriptActionV1` records, run only by `PCHelper.AutomationHost` as the signed-in user, and lose trust when their SHA-256 changes.
7. Detect competing writers and block only overlapping controls by default. Takeover may stop only an exact path/product/publisher/signer/hash identity covered by stored consent; binary changes invalidate consent. New takeover is hard-blocked unless the service image is Authenticode-verified. Never terminate by product name alone.
8. Apply profiles transactionally. Persist rollback state before the first write, verify each domain, and roll back all attempted domains on failure or cancellation.
9. A failed rollback blocks further profile writes and requires explicit recovery. Never report a transaction as safe when reset or verification failed.
10. Stale or critical cooling input must fail toward maximum cooling and firmware/default control.
11. The Windows service performs no network access. Update checks and approved report uploads belong to the user process.
12. Reports are previewed and redacted locally. Upload requires explicit user approval. Do not collect usernames, hostnames, serial numbers, exact paths, network inventory, or unrelated installed applications.
13. Capability labels are evidence claims: `Verified`, `Experimental`, `Read-only`, `Blocked`, or `Unsupported`. Do not widen them for UI convenience.

### Owner policy amendment — 2026-07-16 (user-directed)

At the owner's explicit instruction, per-family per-session arming ceremony is replaced by a
**single persisted "Hardware control" acknowledgement** in the dashboard header: once the owner
checks it, the app automatically arms every implemented GPU write family (fan duty, power
limit, clock offsets) with the required Experimental + exact-device confirmations each time it
connects to the service, and re-arms after service restarts. Unchecking disarms and restores
vendor defaults. The dashboard also displays disarmed armable controls as "Off" and armed ones
as "On" (the underlying `CapabilityAccessState` and rule 13 evidence semantics are unchanged in
IPC, CLI, and stored records). Rules 1–12 are NOT relaxed: the voltage prohibition, pump/CPU-fan
protection, stale-sensor fail-safe, transactional rollback, and the no-fabricated-qualification
rule remain in force regardless of this switch.

## Solution map

| Project | Responsibility |
| --- | --- |
| `src/PCHelper.Contracts` | V1/V2 DTOs, adapter-pack/profile/cooling/ownership/effect/game/update records, capture/trace records, adapter contracts, IPC envelopes. |
| `src/PCHelper.Core` | Validation, transactional profile/update/takeover/capture coordinators, advanced cooling/effect graphs, OSD rendering, macro playback, clean-room importers, adapter-pack trust, privacy-minimised adapter tracing, ownership leases, local game scanning, SQLite state/history, report redaction. |
| `src/PCHelper.Adapters` | System inventory, Windows power plans, LibreHardwareMonitor telemetry, conflict detection, adapter manifests; NVML/NVAPI GPU fan/power/clock adapters (arm-gated), Kraken X3 telemetry parser, PawnIO session + Ryzen SMU feasibility reader, the transport-less CPU PBO tuning scaffolding (adapter, seam, boot sentinel), and the ADLX/IGCL feasibility detectors. |
| `src/PCHelper.Ipc` | Versioned, length-prefixed JSON over secured named pipes. |
| `src/PCHelper.Service` | Boot-capable Worker Service, adapter ownership, polling, history, profile coordination, rollback recovery, local logs. |
| `src/PCHelper.AdapterHost` | Restartable, job-isolated process boundary for built-in native adapters. All commands require the launch token. |
| `src/PCHelper.AutomationHost` | Signed-in-user script process with hash recheck, bounded output, timeout, and child-tree termination. Refuses LocalSystem. |
| `src/PCHelper.EffectHost` | Signed-in-user WebView2 effect process with hash recheck, denied host/network/navigation/file requests, 512 MB process-job limit, LED/frame caps, and watchdog. Refuses LocalSystem. |
| `src/PCHelper.GameBarWidget` | Optional UWP/MSIX Game Bar companion. Its only IPC path is a package-SID-authorised, read-only request to the signed-in user agent. |
| `src/PCHelper.Cli` | Headless `probe --json`, bounded adapter `trace --json`, service status, inventory, profile validation/application, reset, and report export. |
| `src/PCHelper.App` | WPF dashboard, tray, user-agent v2 pipe, per-user suite state, game scanning, OpenRGB and Windows Dynamic Lighting bridges, explicit still-image snapshots, a non-injecting desktop OSD, same-session monitor-brightness discovery/control, WGC video recording, the RTSS shared-memory bridge + frame-rate benchmark, portable (`--portable`) read-only mode, and the localization pipeline (`--culture`). |
| `tests/PCHelper.Core.Tests` | Deterministic core, safety, persistence, and privacy tests. |
| `tests/PCHelper.Integration.Tests` | IPC, server authentication/framing, and bounded logging tests. |
| `report-api` | TypeScript Cloudflare Worker for opt-in encrypted-report storage and deletion. |
| `installer` | WiX MSI and Burn bundle definitions. |
| `tools/PCHelper.UiSnapshot` | Deterministic WPF renderer for all dashboard pages at normal and minimum window sizes. |

The hardware trust boundary is App/CLI -> secured service pipe -> safety coordinator -> restartable Adapter Host -> native adapter. Each host is assigned to a kill-on-close Windows job object and uses a unique pipe plus a random 256-bit token for reads and writes. User-owned work follows App/tray -> user-agent pipe -> per-user state or isolated Automation Host. The service never executes scripts, captures screens/audio, scans game stores, or opens update/report network connections.

## Core contracts and protocol

`IHardwareAdapter` exposes `Probe`, `ReadSensors`, `Prepare`, `Apply`, `Verify`, `Rollback`, `ResetToDefault`, and `GetHealth`. Optional v2 contracts are `IOwnershipAwareAdapter`, `IUpdateAdapter`, and `ITraceableAdapter`.

Important records:

- `CapabilityDescriptorV2`: v1 capability plus hazard class, exact bounds source, read-back/reset guarantees, ownership, driver/firmware gates, and boot policy.
- `SensorSample`: sensor ID, timestamp, nullable value, unit, quality, and freshness.
- `ProfileV2`: typed hardware actions plus cooling-graph, lighting-scene, and OSD references; manual-only action IDs remain separate from automation.
- `CoolingGraphV1`, `FanCalibrationV2`, `FanCommissioningSessionV1`, `EffectGraphV1`, `AutomationWorkflowV1`, `MacroV1`, `MacroRecordingSessionV1`, `ScriptActionV1`, `GameEntryV1`, `CaptureSnapshotRequestV1`, `CaptureSnapshotResultV1`, `AdapterTraceEvent`, and the update/ownership records are stored or transported as type-checked records.
- `AdapterPackManifestV1`: signed `.pcha` identity, protocol range, exact hardware IDs, permissions, entry point, licence, and SHA-256 payload map.
- `ProfileTransaction`: revision, rollback snapshot, pending actions, verification results, and final state.
- `TunePlan`: objective, bounds, screening workload, rejection conditions, and provisional metadata.
- `AdapterManifest`: version, licence, required driver, identifiers, execution context, and control families.

Named-pipe protocol 2 retains read-only protocol-1 compatibility for one upgrade cycle. V1 clients cannot mutate. Messages are limited to 2 MB; mutations carry a request ID, expected revision, and idempotency key. Hardware, user-agent, and private Adapter Host pipes are distinct.

Service commands include v2 capabilities/profiles, cooling graphs, fan commissioning and bounded identification pulses, import previews, signed adapter packs, ownership consent, inventory, redacted adapter trace retrieval, transactional apply/reset, calibration/tuning, and reports. User-agent commands own workflows, effects, games, macros and visible macro recording, scripts, OSD layouts, capture presets, same-user visible-confirmed PNG snapshots, read-only external-overlay discovery, and monitor brightness because physical displays belong to the interactive Windows session rather than Session 0.

## Profile transaction invariants

The transaction engine must preserve this sequence:

1. Validate the profile, capabilities, conflicts, revision, and acknowledgements.
2. Persist a pending-transition journal and rollback snapshot before writes.
3. Apply domains in safety order: `CoolingSafety`, `Power`, `Cpu`, `Gpu`, `Cooling`, `Lighting`, then `Other`.
4. Preserve user action order only within a domain.
5. Record an action as attempted before calling its adapter, because a native call can partially mutate and then throw.
6. Read back and verify applied values.
7. Commit only after all required verification succeeds.
8. On error or cancellation, roll back every attempted domain in reverse order.
9. Retain a recoverable pending journal if rollback itself fails. Block new writes until recovery succeeds.

Required unsupported actions reject a profile. Optional actions may be skipped only with a visible explanation.

Manual profile selection overrides automation. Other rules use numeric priority followed by most-recent activation, with 5-second entry debounce, 15-second exit debounce, and a 30-second switch cooldown.

## Implemented hardware surface

| Surface | Current state | Notes |
| --- | --- | --- |
| Windows inventory | Implemented, read-only | CIM-based system, board, CPU, display, memory, storage, and OS inventory. |
| Monitor brightness | Implemented, user-session, Experimental | The Devices page enumerates every logical display returned by the signed-in Windows session. External monitors use the Windows DDC/CI path; Windows-managed internal panels use WMI. Displays that expose neither remain visible as `Unsupported` with a reason. A write requires direct confirmation, an idempotency key, a usable reported range, and immediate read-back; mismatches attempt restoration of the previous value. It is not part of profiles or automation. |
| Compatibility catalogue | Implemented, read-only | Versioned CPU/GPU/SMBIOS/USB-HID family recognition annotates inventory and exposes `ReadOnly` eligibility cards for Ryzen/Threadripper 1000-9000, Intel Core 6th-14th/Core Ultra, GTX 700-16 and RTX 20-50/professional, Radeon RX 400-9000/professional, Arc A/B/professional, major board vendors, and a data-driven GPU-board registry for ASUS/ROG/TUF, Gigabyte/AORUS, MSI/Mystic Light, ZOTAC/SPECTRA, EVGA/K\|NGP\|N, ASRock/Polychrome, Sapphire, PowerColor, XFX, PNY, Palit, Gainward, INNO3D, GALAX/KFA2, Colorful, Maxsun, Yeston, Manli, Leadtek, Sparkle, and common OEM cards. HID LampArray and expanded popular RGB/peripheral families remain inventory-only. It is identity evidence only and never enables a hardware write. See `docs/compatibility-catalog.md` and `docs/gpu-board-rgb-registry.md`. |
| Windows power plan | Implemented, capability-gated | Native apply, read-back, rollback, and reset passed twice on the reference system. Broad compatibility still requires the physical matrix. |
| LibreHardwareMonitor 0.9.6 | Telemetry plus Experimental bounded fan API | Runs in Adapter Host. `SetSoftware`, software-value read-back, rollback, and `SetDefault` are implemented. Two reference-system passes observed physical RTX 3090 and NCT6798D fan response and firmware return. Overlapping owners convert exact controls to Blocked. This remains uncertified single-system evidence. Non-finite readings become unavailable samples. |
| Fan commissioning | Implemented, capability-gated | A persisted session pairs an RPM sensor with the exact control, issues an explicit 2-5 second bounded identification pulse only after acknowledgements, verifies software read-back, then returns the controller to firmware/default. CPU/pump fan stop remains prohibited. Do not run a pulse while a competing writer owns the channel. |
| NVML telemetry and bounds | Implemented, read-only | Dynamically loads NVML for NVIDIA temperature, fan, power, and clock telemetry plus public fan-target, power-limit, and driver-exposed voltage-frequency bounds. The local RTX 3090 probe found two fan targets and a 100-385 W power range through NVML 610.62. It exposes no write, reset, tuning, or driver-control endpoint. |
| Vendor eligibility cards | Implemented, blocked/read-only | Exact AMD Zen, NVIDIA, ADLX, and IGCL cards describe missing bounds, driver gates, or reset evidence. They do not claim a tuning API merely because a vendor runtime is installed. |
| Aura and detected USB controllers | Inventory only | Direct USB protocol stays in Adapter Host and is `ReadOnly` until containment, timeout, reset, ownership, and exact-device physical tests pass. |
| Conflict detection | Implemented | Detects common fan, tuning, RGB, and OEM control applications without terminating them by default. |
| Windows takeover executor | Implemented, hard-gated | Exact identity discovery rechecks path, product, publisher, signer, and SHA-256 before any process or startup mutation. Catalog-signed Windows binaries use a bounded system PowerShell signer fallback. The executor requires a valid Authenticode service image, stored consent, verified default reset, and a lease; current unsigned builds reject before mutation. |
| Protocol-v2 profiles | Implemented, software-verified | V1 migration, generated Windows power profiles, manual-voltage/boot/automation rules, exact-device confirmation, and typed suite persistence. The dashboard, automation, and game selectors resolve V2 profiles and apply them through `ApplyProfileV2`; Manual Only actions require a non-persisted per-session acknowledgement and exact-device confirmation. Physical writes remain adapter-gated. |
| Adapter packs | Import/verification implemented + public SDK | Bounded `.pcha` parsing, Ed25519 manifest signatures through NSec, payload hashes, protocol/permission checks, zip-slip prevention, exact debug hash allowlist, and safe install staging. Public SDK: `docs/adapter-pack-sdk.md`, `docs/sdk-template/`, `scripts/New-AdapterPack.ps1` (template pack verified against the live verifier). No production publisher key is enrolled yet, so only the DEBUG dev-hash route can install. |
| Qualification evidence flow | Implemented, fabrication-proof | `pchelper-cli qualification-draft` builds UNSIGNED DRAFT `HardwareQualificationRecordV1` entries from the real local identity; requires all four witnessed attestations + `--confirm-witnessed`, refuses unclassifiable hardware, and hard-codes `signedProductionBuild=false` so a draft can never advance the 18-system gate. `qualification --ledger` evaluates the gate. |
| Portable mode | Implemented, read-only | `PCHelper.App --portable` runs without the service or any pipe server: read-only in-process probe, writes locked, distinct "Portable (read-only)" labelling. |
| Localization | Scaffolding live | `Localization/Strings.resx` + satellites (German reference), `L10n`/`{loc:Loc}`, `--culture` startup switch, per-key English fallback; portable-mode strings extracted first. Workflow and top-8 language list in `docs/localization.md`; safety wording never ships machine-translated. |
| PawnIO | Read-only telemetry live via LibreHardwareMonitor | The signed PawnIO driver is installed and running on the reference machine (2026-07-15); LibreHardwareMonitorLib 0.9.6 loads its embedded RyzenSMU/AMDFamily17 modules through it, delivering per-core SMU power, Tctl/Tdie, CCD Tdie, SVI2 voltages, and effective clocks. Telemetry only: do not claim any privileged *write* or controller support merely because PawnIO is installed — CPU/SMU tuning stays Blocked. |
| NVIDIA GPU control (fan/power/clock) | Implemented, arm-gated, Experimental | NVAPI is the primary transport for fan duty (cooler API) and power limit (client power policies); NVML is retained only as a fallback / read-only anchor, because this class of GeForce card refuses NVML's fan and power *setters* with `NVML_ERROR_NO_PERMISSION` while the equivalent NVAPI calls succeed — see the 2026-07-20 snapshot. NVAPI pstates20 core/memory clock-offset adapters unchanged (documented public SDK only; no voltage entry is ever constructed). All disarmed by default: each family registers ReadOnly and needs its `SetGpu*Armed` Experimental + exact-device acknowledgement; disarm and service stop restore defaults. Fan verification polls a bounded settle window (up to 8×250 ms) before comparing to tolerance, because the NVAPI cooler reports live level, not setpoint, mid-ramp. The deployed fan policy requests a 20% uncalibrated minimum but always preserves a higher controller-reported minimum; the reference RTX 3090 currently reports 30–100%. VF editor / OC Scanner deferred by documented-API policy. |
| AMD/Intel CPU tuning | Gated scaffolding only — Blocked everywhere | `ISmuTuningTransport` seam (PPT/TDC/EDC only, fakes only — **no SMU write transport exists in the repo**), `AmdSmuTuningAdapter` double-gated on a qualification witness AND the acknowledged arm, `CpuTuneBootSentinel` boot-recovery journal wired at service start, and `SetCpuTuningArmed` IPC/CLI that refuses with `CPU_TUNING_QUALIFICATION_REQUIRED`. Live writes stay blocked until the full gate in `docs/qualification/cpu-tuning-and-intel-arc.md` passes. |
| AMD/Intel GPU vendor bridges | Feasibility detectors only | `amd.adlx` and `intel.igcl` presence-only detectors (never initialise the vendor library, expose no write, silent on absent hardware). Real ADLX/IGCL telemetry/tuning waits for physical Radeon/Arc systems. |
| Fan calibration/control loop | Implemented, Experimental | 100%-downward 5-point calibration, stable median windows, optional non-critical-fan stop, consecutive state confirmation, multi-cycle restart candidate promotion, hysteresis-aware stop duty, per-sensor thermal ceilings, rollback, cancellation, status, and boot recovery exist. Owner-selected uncalibrated automatic curves now use a 20% configured floor, while any higher controller minimum still wins. The pump stays at its separate 60% floor, CPU/pump role protections remain, and zero-RPM stays disabled. RTX 3090 restart verification remains rejected after inconsistent hysteresis and an 85.2 °C hotspot abort. |
| USB/AIO controllers | Contained read-only discovery + Kraken telemetry | HidSharp-based discovery and HID inventory run in dedicated crash-contained Adapter Host children (`--discover-controllers`/`--read-hid`), so a native fault cannot reach the service. Kraken X3 liquid temp/pump RPM/duty parse read-only from the firmware's unsolicited status stream (liquidctl-derived, zero HID writes); NZXT CAM holding the device yields a designed AccessDenied refusal. No AIO write path exists. |
| RGB routing matrix | Implemented, evidence-gated | Per endpoint, the Lighting page routes through Windows Dynamic Lighting, a user-enabled local OpenRGB SDK bridge, a `Verified` exact adapter, or read-only direct qualification. A running overlapping writer blocks standard output. Manufacturer recognition stays inventory evidence; no raw USB/HID command is inferred. See `docs/rgb-compatibility.md`. |
| OpenRGB | Implemented user-session bridge | Loopback-only protocol negotiation and controller enumeration plus static colour, brightness scaling, and off state. Never bundle OpenRGB; other active lighting writers block use. |
| Windows Dynamic Lighting | Implemented user-session bridge | LampArray discovery, device enable/disable, brightness, physical zones, and per-index static colour compile against the Windows SDK. Physical RGB devices are not qualified. |
| Lighting effects | Implemented, user-session | Declarative effects render deterministically. Trusted JavaScript effects run in the isolated Effect Host with hash, network/navigation/file-request, permission, memory, LED, and watchdog gates. Physical RGB output is not qualified. |
| Games and user tools | Implemented alpha foundation | Bounded local game scanning, per-user storage, visible macro recording/editor/test controls, macro playback, OSD frame validation, a local non-injecting desktop OSD, visible-confirmed GDI/PrintWindow PNG snapshots, capture coordination, per-game bundles, workflows, and the dark dashboard workspace. The Game Bar package has a package-SID-authorised read-only user-agent bridge; its production build script requires certificate and manifest-identity validation before producing a signed MSIX. WGC H.264/WASAPI recording (ScreenRecorderLib) and the validated RTSS shared-memory bridge (OSD publish on explicit confirmed request, frame stats, window-based frame-rate benchmark with honest 1%/0.1% labelling) are live in the user agent. |
| Trusted scripts | Implemented host boundary | Per-script SHA-256 trust, absolute interpreter/script paths, one-hour maximum timeout, bounded output, child-tree cancellation, fresh UAC via `runas`, and LocalSystem refusal. |
| Automation triggers | Implemented | SQLite rules cover process, foreground app, schedule, session lock, idle, and registered hotkeys with 5-second entry, 15-second exit, 30-second cooldown, priority, and manual override. |
| Auto-tuning | Generic engine implemented, hardware endpoints gated | One bounded capability per run, voltage exclusion, thermal/power/WHEA/display-reset checks, cancellation, rollback, 10-minute final screening label, provisional profile output, and boot sentinel are implemented. No CPU/GPU tuning endpoint is currently eligible. |

Protocol-v2 Quiet, Efficiency, Balanced, and Performance profiles are generated from Windows power schemes actually discovered on the machine. V1 compatibility presets remain stock-safe. No generated profile invents CPU/GPU/fan writes.

## Cooling requirements for future adapters

- Calibrate from 100% duty downward in 5% steps and record maximum RPM, stall duty, and restart duty.
- Default minimum duty is verified restart duty plus 5 percentage points.
- CPU fans and pumps cannot use zero RPM. Case-fan stop requires explicit opt-in and a successful restart test.
- Support interpolation, averaging, hysteresis, slew limits, and the maximum of multiple temperature sources.
- A source stale for three polling intervals commands 100%, attempts firmware/default mode, and raises a safety event.
- Critical threshold defaults are adapter limits: CPU `TjMax - 10 degrees C`, GPU thermal limit minus `5 degrees C`, or `90 degrees C` without a reliable limit.
- Service stop, upgrade, and uninstall must restore firmware/default control.

## State, retention, and privacy

State lives under `%ProgramData%\PCHelper` in production.

- SQLite normalises sensor definitions, five-second samples, and one-minute aggregates.
- Five-second history is retained for 24 hours; one-minute aggregates for 30 days.
- Database plus write-ahead log is capped at 250 MB.
- One-second live samples stay in memory.
- Structured NDJSON logs retain seven days with a 50 MB total cap and 10 MB segments.
- Only finite temperature, fan speed, utilisation, power, and clock values enter persistent history.

The report Worker accepts at most 2 MB, rejects compressed bodies, validates bounded schemas, stores deletion-token hashes, supports token-authorised deletion, and deletes expired D1 rows on schedule. R2 30-day lifecycle deletion is a deployment configuration requirement. The Worker is implemented but not deployed from this repository.

## Build and verification commands

Use the SDK pinned by `global.json`. On this workstation .NET SDK `10.0.301` is installed under `C:\Program Files\dotnet`.

```powershell
$dotnet = (Get-Command dotnet).Source
& $dotnet restore .\PCHelper.sln --locked-mode
& $dotnet build .\PCHelper.sln -c Release --no-restore
& $dotnet test .\PCHelper.sln -c Release --no-build

Push-Location .\report-api
npm ci
npm run check
npm test
npm audit --audit-level=high
Pop-Location

.\scripts\publish.ps1 -Version 0.4.0-alpha
.\scripts\build-installer.ps1 -Version 0.4.0 -SkipPublish `
  -RuntimeInstaller .\artifacts\dependencies\windowsdesktop-runtime-10.0.9-win-x64.exe

.\scripts\render-ui.ps1
.\scripts\render-ui.ps1 -OutputDirectory .\artifacts\ui-snapshots-compact -Width 960 -Height 640

$msbuild = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\2022\BuildTools\MSBuild\Current\Bin\MSBuild.exe"
& $msbuild .\src\PCHelper.GameBarWidget\PCHelper.GameBarWidget.csproj /restore /p:Configuration=Debug /p:Platform=x64
```

For a dependency review, use locked restores and both NuGet vulnerability output and `npm audit`. Do not silently update safety-sensitive dependencies.

The WiX toolchain is pinned to 5.0.2. Do not move to a tool version that requires accepting new legal terms without the user's explicit decision. Local output is unsigned and is not a production release.

## Verification snapshot: 2026-07-12

Last verified in this workspace:

- The `0.4.0-alpha` source build adds a bounded header-identification pulse, persisted commissioning sessions, typed macro recording/editor/test, physical lighting layouts, per-game profile/scene/macro/OSD/capture bundles, NVML telemetry, vendor eligibility cards, and read-only direct-USB inventory cards. These features were not used to write local hardware during this verification pass.
- A final `pchelper-cli probe --local --json` read-only pass found 59 devices, 328 sensors, and 8 capabilities: 1 `Verified` Windows-power control, 3 `ReadOnly` telemetry/inventory controls, and 4 conflict-blocked controls. NVML reported exact NVIDIA driver `610.62` telemetry without a tuning endpoint. Fan Control, MSI Afterburner, and NZXT CAM remained running; no ownership takeover, fan pulse, RGB, GPU, or CPU write was issued.
- The Microsoft Visual Studio Build Tools UWP/MSIX workload and .NET SDK 10.0.301 are installed. `PCHelper.GameBarWidget` restores, compiles, validates its manifest, and emits an unsigned debug MSIX. The widget can request only `GetOverlayStatus` through the user agent; the service pipe and all hardware/profile/macro/script/capture mutations are denied.
- Current source UI automation visits all nine pages and validates commissioning, lighting-layout, macro-editor, and per-game controls with unique accessible automation IDs. This is deterministic WPF rendering, not a real Game Bar or external computer-control session.
- .NET SDK 10.0.301 was used.
- Solution build completed with zero warnings and zero errors.
- 141 .NET tests passed: 103 core and 38 integration. New coverage includes catalog-signed exact identity discovery, unsigned takeover gating, graceful-only takeover consent, Manual Only profile-card acknowledgement, RPM transients, commissioning transitions/pulse bounds, Game Bar read-only package authorisation, macro-recording privacy/cancellation, disconnected IPC clients, and repository-owned UI automation.
- 7 report-API tests passed twice; TypeScript checking and the npm high-severity audit passed with zero vulnerabilities.
- Baseline live CLI probing found 56 devices, 323 sensor records, and 3 capabilities. A privileged takeover probe found 65 devices, 441 sensor records, and 10 capabilities. After ownership restoration, 9 overlapping fan controls returned to `Blocked` and Windows power remained `Verified`.
- Calibration/tuning fake-adapter tests cover acknowledgements, voltage rejection, bounded candidate generation, stall/restart detection, final-screen labelling, generated profiles, and prior-state restoration.
- Adapter Host process smoke passed and a wrong-token direct reset request was rejected. The host now requires its token for reads and writes and runs in a kill-on-close job object. The first smoke exposed a HidSharp native-load process crash; USB-controller discovery remains disabled.
- `.pcha` tests cover valid Ed25519 signatures, payload tampering, exact debug-hash trust, firmware warnings, and contained extraction. Importer tests cover Afterburner numeric/VF/manual-voltage mapping and the observed Fan Control graph/calibration schema.
- Advanced cooling tests cover graph cycles, mixed sensors, RPM conversion, avoid bands, stale emergency output, feedback response, and separate rise/fall slew. Effect tests cover physical gradients, temperature colour, audio bins, and blending.
- Takeover, update, capture, OSD, and macro tests cover identity changes, startup rollback, firmware-default verification, update reboot/rollback/recovery states, encoder/target failures, strict formatting, ordered playback, and held-input release.
- User-agent tests verify same-user access, macro/OSD validation, and per-user persistence. Script tests verify trust invalidation after file edits. Local game tests cover Steam/Epic discovery and path-escape rejection.
- The real WebView2 Effect Host rendered a hash-bound JavaScript frame, denied a network request, and terminated a runaway effect through its watchdog. The published Effect Host binaries passed the same three integration tests.
- OpenRGB protocol integration tests negotiate protocol 5, parse an exact controller/LED count, verify brightness-scaled RGB byte order, and reject non-loopback hosts.
- Automation tests cover overnight schedules, process matching, idle state, debounce, cooldown, immediate manual override, hotkeys, and SQLite save/update/delete.
- Report privacy smoke found no username, hostname, user path, MAC address, or network-device record in the redacted payload.
- Installed-app process smoke passed: the tray process stayed alive, a second instance exited cleanly, activation produced a non-zero main-window handle, and initial app working set was 194.4 MB.
- The external Windows computer-control native pipe remains unavailable and cannot be repaired from this repository. A repository-owned WPF automation smoke now visits all nine pages, validates Advanced Lab switching, checks 82 critical automation IDs, and confirms all 104 interactive controls have accessible names with no duplicate application IDs. Real pointer and screen-reader testing remains external.
- Two controlled hardware passes validated exact signed Fan Control 271 and MSI Afterburner 4.6.6 identities before stopping them. Both passes completed RTX 3090 fan high-duty/read-back, two-channel calibration, NCT6798D high-duty/reset, Windows power apply/read-back/rollback, and owner restoration. No WHEA, display-reset, unexpected-power, or RigPilot application-error event occurred during the second pass.
- The first installed cold start exposed a 2-second Adapter Host probe timeout; read timeouts are now operation-specific. Fault injection then exposed missing SCM recovery and an unhandled pipe-write cancellation. MSI recovery actions and best-effort disconnected-client response handling were added and retested against installed binaries.
- A privileged 10-minute closed-dashboard soak measured 0.458% combined CPU, 394.1 MB initial and 388.1 MB final working set, -1.52% growth, and zero unexpected TCP connections. CPU, growth, and network checks passed; the under-200 MB working-set target failed. The 24-hour soak remains open.
- Unsigned framework-dependent `0.3.0-alpha.5` app, service, Adapter Host, Automation Host, Effect Host, and CLI outputs were published locally. The unsigned WiX MSI is 9,646,080 bytes with SHA-256 `FDD54C11E2E5E6416F94FD847DAF685227771977CB1357687E8D9CF021632782` and numeric version `0.3.4`. Authenticode correctly reports `NotSigned`.
- Locked restores passed for the solution and both WiX projects. The MSI and bundle use separate committed lock files because their dependency graphs differ.
- GitHub workflow syntax passed `actionlint` 1.7.12.

The last installed alpha pre-dates the RigPilot rebrand: it is under `C:\Program Files\PC Helper` as MSI display version `0.3.4`. Installed app, service, Adapter Host, and CLI hashes match the published `0.3.0-alpha.5` payload. The service is Running/Automatic with three five-second recovery actions. The dashboard is stopped because it was not running at baseline. Fan Control, MSI Afterburner, and NZXT CAM are restored; their overlapping RigPilot controls stay blocked.

The uninstalled rebrand artifact is `artifacts\installer\RigPilot.Installer.msi`, numeric version `0.3.5`, 9,646,080 bytes, SHA-256 `32D99C37ED41B91257FDBF48A5A381604C06E72055E76817762A6704BB9A02CF`. Its MSI tables were inspected: ProductName, install folder, Start Menu shortcut, and service display are all `RigPilot`; the actual service name remains `PCHelper`. The release build and both full test passes succeeded, and deterministic WPF rendering visited all nine pages.

Re-run the relevant checks after every material change. Replace this snapshot only with observed evidence.

## Verification snapshot: 2026-07-14 (bounded Fan #1 calibration)

- The temporary unsigned `0.4.0-alpha` service at `%ProgramData%\RigPilot\LocalAlpha\0.4.0-alpha-uac-20260713-212004` remains `Running` / `Automatic`. It is a reversible UAC-staged development deployment; it is not a signed installer upgrade.
- Fan #5 remains persisted as `Pump` / `AIO_PUMP` and was not touched. Fan #1 (`lhm.control:/lpc/nct6798d/0/control/0`, paired RPM `lhm.sensor:/lpc/nct6798d/0/fan/0`) is persisted as `CaseFan` with software alias `CASE_FAN_1`; the alias is not physical-header proof.
- A user-authorised bounded Fan #1 calibration ran from 100% through 0% in 5-point steps. It completed in operation `f29e1c16d65e4ea6918b709fdb4db21e`, measured a 1959.36 RPM maximum, recorded stable samples, stayed below the 70 C controller-temperature ceiling (45.5 C maximum), and completed rollback successfully.
- At 0%, the paired tach still measured about 546 RPM. Therefore no stop duty, restart duty, or repeated restart verification exists. The operation is a completed non-stall characterisation, not a commissioned curve. The service left the session `ReadyForCalibration`; no curve, profile action, or zero-RPM policy was enabled.
- Post-operation probe reported healthy adapters, no active competing writer, good Fan #1 RPM telemetry, and no warning. The System log window contained no WHEA logger, display-driver, or NVIDIA driver-reset event. The RTX 3090's conservative 50% unverified fan floor remains unchanged.
- The calibration CLI/UI/report path now distinguishes `CompletedNoStallObservedAtMinimumCommand` from a failed restart. The UAC report wrapper accepts the CLI's nonzero incomplete-commissioning exit code, preserves the completed measurement payload, and records the non-qualifying result instead of losing it.
- Software-only stale-source tests passed: stale input commands 100% cooling and requests firmware/default return. This does not count as a live stale-source timing test. No live host crash, synthetic stale sensor, suspend/resume, BIOS configuration change, or second physical calibration was issued.

## Follow-up hardening: 2026-07-14

- The source now treats `HeaderConfirmed` as an operator declaration distinct from `PhysicalHeaderObserved`. The CLI generic case-fan declaration sets only the former. A session cannot become `Completed` without visual physical-header observation, and the UI labels a declared-only session as observation pending.
- A completed calibration is now bound to `CommissioningSessionId`. Qualification reports no longer attach one control's calibration to every historical session for that control. The physical-header report step is `Ready`, not `Passed`, for a user-declared generic alias.
- Service-side completion rejects a calibration with `RestartVerified=false`; it returns `RESTART_NOT_VERIFIED` and refuses a commissioned curve/profile. Cooling-graph/profile drafts may be saved, but profile activation checks for an exact completed, physically observed session and a linked positive restart duty.
- `NamedPipeRequestServer` now treats a closing-pipe `IOException` during accept as a connection-scoped retry or normal shutdown instead of an Adapter Host process crash. The previously failing Adapter Host integration test now passes.
- Validation after these changes: Debug solution build 0 warnings/errors; 194 core and 67 integration tests passed; the Release dashboard rendered all nine pages. Visual inspection of the Cooling page confirmed the declared-only header label and cleared physical-observation checkbox.
- The source hardening is not deployed to the temporary unsigned alpha service. Two normal-UAC restore launches did not return through this desktop session; the live service ImagePath still points to `%ProgramData%\RigPilot\LocalAlpha\0.4.0-alpha-uac-20260713-212004\payload\service\PCHelper.Service.exe`. No additional hardware command ran. Restore manually with `scripts\Restore-LocalAlphaRuntime.ps1` through normal UAC before any future live test.

## Verification snapshot: 2026-07-14 (qualification-gate and diagnostics hardening)

- The source qualification planner now selects only an exact CPU/GPU tuning endpoint. Read-only compatibility cards, NVML public telemetry, fan ranges, and power ranges cannot make Ryzen or NVIDIA tuning look eligible. The plan reports exact identity, vendor transport/runtime, bounds, apply/read-back, default-reset, and screening gates separately.
- On this reference system, a read-only local source probe detected the Ryzen 7 5800X on BIOS 5031 and surfaced `AMD Zen transport feasibility` as `ReadOnly`; the actual `AMD Zen tuning` control remains `Blocked`. It also detected the installed NVIDIA NVAPI runtime for the ZOTAC RTX 3090 on driver `32.0.16.1062`, but retained the actual NVIDIA clock/power/VF control as `Blocked`. NVML 610.62 telemetry and its 100-385 W public range remained `ReadOnly`.
- The diagnostics baseline now includes notify-only stale-telemetry rules for selected CPU temperature, GPU temperature, and pump RPM sensors in addition to thermal, pump-RPM, WHEA, and display-reset alerts. No baseline rule can issue a hardware command.
- The Performance and Lighting qualification cards now show evidence-gate progress/readiness rather than presenting generic compatibility or telemetry state as control support. Direct RGB controller plans still require containment, static-scene read-back, reset, ownership, unplug, and recovery evidence. RGB route cards now state whether their boundary is a Windows/OpenRGB bridge or direct-device evidence only; no raw USB/HID protocol is implied.
- Monitor-brightness cards now label the exact DDC/CI, WMI, or unsupported transport and the reported range. This is presentation-only; the existing explicit confirmation and immediate read-back/rollback requirement is unchanged.
- Release build/test validation after this source slice: `dotnet build PCHelper.sln -c Release --no-restore` completed with 0 warnings/0 errors; 256 core and 74 integration tests passed. The release WPF renderer passed all nine pages, Simple/Advanced switching, 161 named interactive controls, no duplicate automation IDs, and 5,710 visual elements. The rendered source UI is in `artifacts/verification/qualification-gates-release`.
- Read-only runtime preflight against the existing local alpha service remained ready at protocol 2 with no missing features and no emergency state. The service currently says only Verified controls can be written. Fan #1 remains `CASE_FAN_1` / `CaseFan`; Fan #5 remains `AIO_PUMP` / `Pump`. No fan, RGB, CPU, GPU, monitor, profile, or takeover command was issued during this verification.
- The changes above are source-level and have not replaced the temporary LocalAlpha service runtime. Do not treat its existing qualification-plan response as proof of the newly built planner until a separately approved, reversible runtime update is performed.

## Release and qualification gaps

The following remain open and must not be hidden by version labels:

- Run interactive keyboard, pointer, screen-reader, and tray QA after Windows automation is available; deterministic full-page render QA is implemented.
- Upgrade and service recovery passed on this Windows 11 reference system. Fresh install, installer rollback, uninstall, Windows 10, and the HVCI enabled/disabled matrix remain open.
- Verify that uninstall does not remove a PawnIO installation shared by another application.
- The user accepted the current 388-394 MB working set for the alpha. It remains an optimisation item rather than a current blocker; the 24-hour memory-growth soak is still open. The 10-minute CPU and network targets passed.
- Complete physical fan header identification, longer-settling calibration, restart testing, emergency timing, suspend/resume, failed native calls, GPU-driver restart, unexpected reboot, and uninstall recovery. Two live passes issued bounded fan writes and restored firmware control, but that is not full qualification.
- USB/HID discovery now runs in crash-contained Adapter Host children (done); qualifying specific AIO/peripheral *write* protocols (Kraken pump control first) remains open.
- Add audited vendor bridges and exact-device evidence before exposing CPU writes or AMD/Intel GPU writes; the CPU PBO scaffolding is deliberately transport-less and Blocked until the qualification gate passes.
- Complete ASUS X570-E header mapping and low-duty calibration. RTX 3090 fan/power/clock setters now exist arm-gated (NVML/NVAPI public SDK; VF editor/OC Scanner policy-deferred), but their user-witnessed live write passes and repeated-cycle qualification are still open — Experimental evidence is not full qualification.
- Run the signed Game Bar packaging path with a production certificate, then verify installation, exact-package-SID bridge, and Game Bar activation. Visible macro recording/editing/test, local non-injecting desktop OSD, visible-confirmed PNG snapshots, WGC H.264/WASAPI recording, the RTSS OSD/frame-stats/benchmark bridge, and an unsigned development MSIX build are implemented; the RTSS live pass still needs RivaTuner installed on the reference machine.
- Obtain Authenticode credentials and run physical signed-package takeover tests. The Windows executor now handles exact-identity process/startup control, default-reset verification, leases, recovery, and give-control-back, but the current unsigned build hard-blocks before mutation and there is no production signed execution result.
- Wire exact-device vendor update providers, official discovery/download, BitLocker handling, and vendor firmware launch. The service-owned driver executor now provides a staged-INF/catalog/hash/device/OEM-export/rollback path, but it requires a signed service and has not been run against a supplied vendor package. Transaction/reboot/rollback orchestration is implemented; no firmware writer exists.
- Test the OpenRGB bridge against physical controllers and conflicting RGB applications; the current proof uses a bounded fake SDK server.
- Run an actual 10-minute tuning screen only after a physical controller is explicitly selected and competing writers are closed; current tests use fake adapters and monitors.
- Configure and deploy Cloudflare R2/D1/rate-limit bindings, R2 lifecycle rules, encryption policy, and domain outside local builds.
- Obtain Authenticode credentials, sign all executable payloads and installers, produce release SBOMs/attestations, and verify signatures.
- Complete the 18-system, three-generation qualification matrix and publish two independent successful reports for each claimed write-capable controller family.

Version 1.0 is blocked by any unresolved BSOD, stuck fan, failed reset, unauthorised write, or rollback defect.

## Verification snapshot: 2026-07-13 (release-gate tooling)

- `scripts\Test-CodeSigningPrerequisites.ps1` found `signtool.exe` at `C:\Program Files (x86)\Windows Kits\10\bin\10.0.26100.0\x64\signtool.exe`, but found zero current certificates with a private key and the Code Signing EKU. `publish.ps1 -RequireSigning` and `build-installer.ps1 -RequireSigning` both stopped before artifact creation or installation work.
- `publish.ps1` now signs and immediately verifies RigPilot executables and DLLs; `build-installer.ps1` signs and verifies the MSI and optional bundle. These paths require an explicit valid certificate (or exactly one unambiguous valid certificate) and an HTTPS timestamp service. No self-signed or development certificate is accepted for production signing.
- `Invoke-ValidatedDriverInstall.ps1` parses successfully and is no-mutation by default. An actual install requires an exact PnP instance, catalog signature/publisher, canonical package SHA-256, stable external power, exported OEM rollback package, explicit `-Apply -ConfirmExactDevice`, and a trusted signed service image. `Test-VendorFirmwarePackage.ps1` performs exact package/hash/signer/device/recovery preflight only; it intentionally cannot flash firmware.
- `HardwareQualificationRecordV1` plus `QualificationMatrix` and `pchelper-cli qualification --ledger` now enforce the 18-system, signed-record, CPU/GPU/platform, and two-independent-controller-report gates. `docs\qualification\reference-system.json` has one unsigned ASUS X570-E/Ryzen 5800X/RTX 3090 reference record and correctly returns blocked (one physical system, zero signed systems).
- Release build completed with zero warnings/errors. All tests passed: 106 core and 38 integration (144 total). PowerShell parse checks passed for all five changed release/update scripts. No hardware write, process termination, driver installation, firmware launch, RTSS write, WGC capture, or USB transaction was issued in this pass.

## Verification snapshot: 2026-07-13 (production-path hardening)

- A fresh `pchelper-cli probe --local --json` read-only run found 59 devices, 332 sensors, and 11 capabilities: 1 `Verified`, 6 `ReadOnly`, and 4 `Blocked`. The NVIDIA adapter loaded NVML 610.62, reported two fan-target capabilities with a 50% conservative minimum, and discovered a 100-385 W RTX 3090 power range. No NVIDIA setter, fan pulse, profile apply, or other hardware mutation ran.
- `WindowsDriverUpdateExecutor` was added to the service IPC path. Its fake-package integration tests cover contained staged INF input, exact PnP/catalog/publisher/hash validation, OEM rollback export, explicit confirmation, unsigned-service rejection, reboot journalling, and firmware-plan rejection. It has not installed a real package.
- `build-gamebar.ps1` creates a certificate-validated, manifest-verified, signed MSIX candidate without installation. Its missing-certificate preflight was exercised and stopped before build or package output.
- Release build completed with zero warnings and zero errors. All .NET tests passed: 106 core and 42 integration (148 total). PowerShell parser checks passed for seven release/update scripts. `Test-ReleaseReadiness.ps1` correctly blocked signed alpha and 1.0 publication because no usable Code Signing certificate exists and the ledger has zero signed physical systems out of 18.
- No hardware write, process termination, driver installation, firmware launch, RTSS output, WGC capture, or USB transaction was issued in this pass.

## Verification snapshot: 2026-07-13 (broad compatibility catalogue)

- `HardwareCompatibilityCatalog` now classifies mainstream desktop identity strings as read-only inventory evidence: Ryzen/Threadripper 3000-9000, Intel Core 10th-14th and Core Ultra 200, NVIDIA RTX/GTX, Radeon RX, Intel Arc, major board vendors/OEMs, HID LampArray, and common USB/HID peripheral vendors. It never creates a write capability from a family match.
- The local source probe found 60 devices, 332 sensors, and 15 capabilities: 1 `Verified`, 10 `ReadOnly`, and 4 `Blocked`. It recognised this reference system as ASUS X570-E, AMD Ryzen/Threadripper 5000 (Zen 3), NVIDIA RTX 30, ASUS Aura, and HyperX inventory. The observed Lian Li case controller remains a read-only `VID_1532&PID_0F13` entry. No hardware mutation was issued.
- A USB/HID transport gate was added after a live probe caught an ASUS-labelled ACPI interface false-positive. ACPI identities now remain outside peripheral inventory; the regression test covers that case.
- Release build completed with zero warnings and zero errors. All .NET tests passed: 140 core and 43 integration (183 total). Deterministic WPF automation visited all nine pages, passed the new `Devices.CompatibilitySummary` automation ID, found 104 named interactive controls, and reported no duplicate automation IDs. The report API passed TypeScript checking, seven tests, and `npm audit --audit-level=high` with zero vulnerabilities.
- `Test-ReleaseReadiness.ps1` still blocks a signed alpha and 1.0 because the workstation has no usable Code Signing certificate and the qualification ledger has zero signed physical systems out of 18.

## Verification snapshot: 2026-07-13 (OSD, snapshots, tracing, and expanded catalogue)

- The per-user runtime now owns two bounded local features: a transparent non-activating WPF desktop OSD and explicit same-user PNG snapshots. Snapshot requests require an idempotency key and visible-session confirmation, accept only a discovered display/window target, cap output at 8K/33,177,600 pixels, and write only below the user Pictures `RigPilot\Snapshots` directory. They use GDI/PrintWindow and never run through the service. No real desktop image was captured during this verification pass.
- `TraceableHardwareAdapter` wraps service adapters and keeps a bounded privacy-minimised operation trace. It records adapter, operation, optional capability ID, outcome, and exception type only; it excludes sensor values, paths, and raw exception text. `GetAdapterTrace` and `pchelper-cli trace --json` exist in source. The currently installed service was not replaced or restarted for this source-only verification.
- `HardwareCompatibilityCatalog` now adds identity-only detection for Ryzen/Threadripper 1000/2000, Intel Core 6th-9th, GTX 700/900, RTX Ada professional, Radeon RX 400/500 and Pro/Instinct, Arc Pro, Supermicro/Colorful/Maxsun boards, and additional cooling-controller vendors. Every new family remains `ReadOnly`.
- Locked restore and release build completed with zero warnings and zero errors. All .NET tests passed: 162 core and 47 integration (209 total). Added coverage rejects invalid snapshot schemas and forged target kinds before a backend creates output, and verifies trace redaction/bounded retention.
- Deterministic WPF rendering visited all nine pages. The smoke harness found 2,638 visual elements, 109 named interactive controls, no unnamed interactive controls, no duplicate automation IDs, and a working Advanced Lab toggle. It includes the desktop OSD and snapshot controls.
- The report API passed TypeScript checking, seven tests, and `npm audit --audit-level=high` with zero vulnerabilities.
- A current `pchelper-cli probe --local --json` read-only run found 60 devices, 332 sensors, and 15 capabilities: 1 `Verified`, 10 `ReadOnly`, and 4 `Blocked`. All six adapters reported healthy. Fan Control, MSI Afterburner, and NZXT CAM were detected as running overlapping writers. No hardware write, screenshot, OSD display, process termination, driver installation, firmware action, RTSS output, WGC video/audio capture, or USB transaction was issued.
- `Test-ReleaseReadiness.ps1` correctly returned its blocked status: no usable Code Signing certificate and zero signed physical systems out of the required 18. This is expected and preserves the 1.0 qualification gate.

## Verification snapshot: 2026-07-13 (reliability, evidence, and monitoring workspace)

- `ReliabilityContracts.cs`, `ReliabilityLab.cs`, `WindowsSystemHealthSignalProbe.cs`, the service runtime, and the user-agent pipe now provide typed health rules, bounded health-alert history, read-only WHEA/display-reset classification, local tray notifications, persisted recovery safe mode, cooling qualification reports, device qualification plans, monitoring aliases/pins/trends/timeline, CSV export, and bounded local hardware-evidence export. A requested emergency profile is deliberately queued for operator confirmation; it does not silently write hardware. Safe mode suppresses client automation.
- The cooling qualification report explicitly tracks X570-E header mapping, repeated restart testing, emergency-response timing, suspend/resume recovery, and reset-to-default state. Missing, incomplete, or failed steps remain visible and cannot promote a fan channel to qualified. RTX 3090 restart evidence remains failed/incomplete and its 50% conservative floor remains unchanged.
- Performance and Lighting now show exact-device qualification plans for the Ryzen 5800X, RTX 3090, ASUS Aura, and detected Lian Li/direct-USB devices. They explain required apply/read-back/reset/driver/containment evidence but create no vendor tuning or direct-USB write path.
- The local desktop OSD now persists global hotkey, monitor, anchor, opacity, and scale settings. Windows Graphics Capture exposes an explicit preflight only: picker consent is required and no reviewed Media Foundation/WASAPI encoder path exists, so video capture stays disabled. RTSS remains optional/read-only discovery.
- Release build completed with zero warnings and zero errors. Full tests passed: 169 core and 53 integration (222 total). New tests cover health-rule transitions, system event classification, evidence boundaries, cooling-report honesty, Aura/Lian Li plan inclusion, valid/invalid OSD preference persistence, and user-agent state-store entity mapping.
- `scripts\\render-ui.ps1 -Configuration Release -OutputDirectory .\\artifacts\\ui-snapshots-reliability` rendered all nine WPF pages. Visual review confirmed the dark Overview, Cooling, Performance, Games & tools, and Diagnostics layouts. The renderer did not start the production tray/user agent; its offline user-agent banner is expected in that isolated render.
- A current `pchelper-cli probe --local --json` read-only run found 60 devices, 332 sensors, and 15 capabilities: 1 `Verified` Windows-power control, 10 `ReadOnly`, and 4 `Blocked`; all six adapters were healthy. It confirmed ASUS X570-E, Ryzen 7 5800X, RTX 3090, BIOS 5031, and NVIDIA NVML 610.62 telemetry. MSI Afterburner, NZXT CAM, and Fan Control were running, so their overlapping domains remain blocked. The command performed no mutation.
- After the user explicitly authorised closing blockers, the exact active NZXT CAM processes and elevated Fan Control/MSI Afterburner processes were terminated. A subsequent read-only local probe found no active competing writer, 1 `Verified`, 10 `ReadOnly`, 2 `Blocked`, and 2 `Experimental` capabilities. The two Experimental controls are GPU Fan 1 and GPU Fan 2 only; they still require explicit in-product acknowledgement and physical qualification. No hardware write, profile apply, or calibration was issued.
- No live fan pulse, fan calibration, profile apply, CPU/GPU tuning write, direct RGB/USB operation, driver/firmware action, RTSS output, WGC video/audio capture, or service installation/restart was issued in this source and test pass. The separately authorised conflict-clearance action terminated only the named competing applications.

## Verification snapshot: 2026-07-13 (comparison workspace and conservative health baseline)

- `MonitoringComparisonLayoutV1` adds a separate per-user, bounded persistence record for zero to four exact sensor IDs. `GetMonitoringComparisonLayout` and `SaveMonitoringComparisonLayout` are owned solely by the user-agent pipe, validate trimmed/distinct IDs, and cannot alter sampling, alerts, profiles, or hardware control. The dashboard displays a normalized four-series overlay with native values/ranges in its legend so unrelated units are not represented as directly comparable magnitudes.
- `HealthRuleRecommendations` now derives a deterministic, opt-in baseline from the live CPU/GPU temperature and pump RPM trends plus WHEA/display-driver event rules. The dashboard's `Add recommended rules` action persists only missing rules and every recommendation is `NotifyOnly`; it cannot apply a profile, write hardware, request takeover, or enter safe mode.
- The user-agent state-store type map includes the new comparison layout. Integration coverage persists a valid two-series selection and rejects a five-series layout; core coverage verifies recommendation identity uniqueness, typed validation, and notify-only actions.
- Release build completed with zero warnings and zero errors. Full tests passed: 170 core and 54 integration (224 total). `render-ui.ps1 -Configuration Release -OutputDirectory .\\artifacts\\ui-snapshots-comparison` rendered all nine WPF pages; the new Overview and Diagnostics markup compiled and rendered in the deterministic snapshot host.
- Fan Control, MSI Afterburner, and NZXT CAM remain stopped after the separately authorised conflict-clearance action. No hardware write, calibration, profile apply, native capture, driver/firmware action, or direct USB transaction was issued in this feature pass.

## Verification snapshot: 2026-07-13 (runtime parity and operator UX)

- The service handshake now advertises protocol-2 features and the dashboard/CLI independently evaluate protocol, required feature names, and major/minor release line before exposing a service-owned write. `pchelper-cli runtime-preflight --json` against the installed service reported source client `0.4.0-alpha`, installed service `0.3.0.0`, missing protocol-2 features, and `UpgradeRequired` with exit code 3. This was an expected read-only diagnostic result; the installed service was not stopped, replaced, or restarted.
- The dashboard now makes this state explicit: service monitoring can remain available, but Profiles, reset, calibration, tuning, and commissioning writes are locked. The dark Overview renders a concise runtime-update explanation instead of conflating the mismatch with a generic connection failure.
- Cooling commissioning now requires a visible-observer acknowledgement for the bounded 60% two-second header pulse. It resets that acknowledgement when the target or saved session changes and keeps CPU/pump stop protections unchanged. No fan pulse, calibration, or restart test ran in this pass; RTX 3090 restart qualification remains incomplete and its 50% conservative floor is unchanged.
- Devices now provides a capability decision matrix with state, risk, evidence, ownership, and a next-safe-action explanation. Monitoring adds presentation-only All/Pinned/Temperature/Fan/Power trend filters and All/Health/Profile/Conflict/Adapter timeline filters. These filters do not alter collection, alerts, history, or hardware state.
- `publish.ps1` writes a six-component `runtime-contract.json` with file version and SHA-256 data. `Test-RuntimePayload.ps1` validates containment, hashes, component coverage, protocol feature names, and installer release line; it passed for a new unsigned `artifacts\runtime-contract-smoke` publish and rejected an intentionally mismatched `0.5.0` release line. `build-installer.ps1` invokes the same verifier before WiX. A non-installed MSI build against that isolated payload succeeded at `artifacts\installer-runtime-contract-smoke\RigPilot.Installer.msi` (9,859,072 bytes, SHA-256 `06926ab6eb4946c96b6acf5450fda2e4cf3772e9ced0009cbcd52ec07a083bb5`).
- Final verification: release build succeeded with zero warnings/errors; 174 core tests and 56 integration tests passed (230 total); PowerShell parser checks passed; the renderer rebuilt and rendered all nine pages to `artifacts\ui-snapshots-runtime-ux-final`. No hardware write, fan pulse, profile apply, process termination, service replacement, driver/firmware action, RTSS output, WGC capture, or USB transaction occurred.

## Verification snapshot: 2026-07-13 (motherboard fan execution-context gate)

- The temporary `0.4.0-alpha` service ran three no-write `Prepare` preflights for Nuvoton Fan #1 (`lhm.control:/lpc/nct6798d/0/control/0`, paired with `lhm.sensor:/lpc/nct6798d/0/fan/0`) while Fan #5 was explicitly protected as `AIO_PUMP`. Every LocalSystem Adapter Host run failed before `Apply`; one captured `IOException` `0x80070542` / Win32 1346. The reports record `applyIssued=false`, `rollbackIssued=false`, and `resetIssued=false`; no physical pulse, calibration, reset, or profile write ran.
- A separately elevated interactive `LibreHardwareMonitorAdapter.PrepareAsync` succeeded at a 60% request with `applyIssued=false`, `verifyIssued=false`, `rollbackIssued=false`, and `resetIssued=false`. This isolates the defect to the service/Adapter Host execution context. It is not physical control evidence and does not unblock the service.
- The source now changes a failed no-write preflight into terminal `FanCommissioningState.Failed`, redacts the repeated pulse path through the same structured diagnostics, and refuses a physical pulse from that evidence record. An operator must create a new session only after the execution-context defect is fixed. A future bridge must be authenticated, user-session scoped, and explicitly UAC-approved; no shortcut or impersonation bypass was added.
- The originally installed `PCHelper` service was restored and remains `Running` / `Auto` from `C:\Program Files\PC Helper\service\PCHelper.Service.exe`. The preflight baseline reported Fan #1 at 67.45% / 1481.9 RPM and Fan #5 at 100% / 2743.9 RPM. The pawnio driver remained running.
- Release build succeeded with zero warnings/errors. Full tests passed: 185 core and 63 integration (248 total). A fresh unsigned `artifacts\publish` payload passed `Test-RuntimePayload.ps1` for all six components at protocol 2. No live write ran after the source change.

## Verification snapshot: 2026-07-13 (explicit elevated no-write diagnostic)

- `RunInteractiveFanPreflight` is a current-protocol user-agent command. It is available in Cooling only for a failed, non-pump, declared chassis-header session whose capability is a bounded LPC LibreHardwareMonitor control. The UI labels it `Run elevated diagnostic` and states that it requests UAC, runs `Prepare` only, and cannot enable a service write.
- The user agent creates a one-shot private named pipe plus a 256-bit random token, then explicitly launches the same dashboard executable with `runas`. Its special host mode parses only fixed `--pipe`, `--token`, and bounded `--capability` arguments. The elevated child may call `Probe` and `Prepare`; it has no code path to `Apply`, `Verify`, `Rollback`, or `Reset`. Result flags are structurally false for every one of those operations. The service neither calls nor trusts this result as write authority.
- Source tests cover current-protocol command classification, invalid-capability rejection before any launcher call, injected user-agent dispatch with all write flags false, and strict private-host argument parsing. A malformed special-host invocation from both the build output and the freshly published payload exited with code 2 before adapter access. No UAC prompt or live adapter `Prepare` was issued in this verification pass.
- Release build succeeded with zero warnings/errors. Full tests passed: 186 core and 66 integration (252 total). `render-ui.ps1` rebuilt and rendered all nine pages to `artifacts\ui-snapshots-interactive-preflight`; the Cooling page includes the disabled-by-default diagnostic action. A fresh unsigned `artifacts\publish` payload passed `Test-RuntimePayload.ps1` for all six components at protocol 2.
- This is a diagnostic aid only. A typed user-session write bridge still requires independent design, service authentication, transaction/recovery semantics, live apply/read-back/reset evidence, and physical qualification. The Nuvoton Fan #1 LocalSystem control remains blocked; Fan #5 remains protected as `AIO_PUMP`.

## Verification snapshot: 2026-07-13 (durable physical cooling-output roles)

- `CoolingOutputAssignmentV1` is a typed, persisted service entity keyed to the discovered cooling capability, adapter, and device. It corrects the unsafe assumption that a generic Nuvoton name such as `Fan #5` can be treated as a chassis fan. The dashboard now lets an operator select the output, record `Pump` with header `AIO_PUMP`, and save that policy without sending any fan command.
- The service, not the dashboard, enforces the record. A CPU-fan role blocks identification pulses and zero-RPM calibration. A pump role is stricter: it blocks commissioning pulses, service-side preflight, calibration, cooling-graph outputs, profile actions, and automatic tuning until a controller-specific pump qualification establishes a safe nonzero policy. The dashboard also disables its user-session no-write diagnostic for the persisted role. Removing either protected role requires an explicit acknowledgement and a typed service mutation.
- This source change did not update the installed service or persist Fan #5 into its state database. The user-declared mapping is ready to save only after a matching 0.4 runtime is installed. No fan pulse, `Prepare`, calibration, profile, reset, takeover, driver/firmware, or USB operation ran in this pass.
- Verification: release build completed with zero warnings/errors; full tests passed with 191 core and 66 integration tests (257 total). The repository-owned UI smoke added and validated the role editor automation IDs. `render-ui.ps1` rendered all nine pages to `artifacts\ui-snapshots-output-roles`, and the tall Cooling render at `artifacts\ui-snapshot-output-roles-tall\03-cooling.png` was visually inspected. A fresh unsigned six-component `0.4.0-alpha` payload at `artifacts\publish-cooling-output-roles-contract-20260713` passed `Test-RuntimePayload.ps1` at protocol 2 and explicitly contains the required `cooling-output-roles` feature; it was not installed.

## Verification snapshot: 2026-07-13 (service identity and LocalSystem no-write diagnostic)

- The normal named-pipe client now requests `TokenImpersonationLevel.Impersonation` rather than `Identification`. The service evaluates normal clients with `RunAsClient`; the former level can trigger Win32 1346 and silently reduce a legitimate operator to read-only. Token-authenticated private Adapter Host pipes continue to skip client impersonation and fail closed as non-operator. New IPC integration coverage verifies normal client identity evaluation; the focused suite passed six tests.
- `pchelper-cli direct-prepare --capability ... --confirm-no-write [--output FILE]` was added as a local-process diagnostic. It performs only LibreHardwareMonitor `Probe` and `Prepare`, structurally reports `Apply`, `Verify`, `Rollback`, and `Reset` as false, and has no service IPC path. `scripts\Invoke-LocalSystemDirectPrepare.ps1` runs that command through one temporary LocalSystem scheduled task, validates the report contains no forbidden operation, then removes the task.
- A standard-user execution on Fan #1 correctly reported `CapabilityUnavailable` at `ProbeCapabilityLookup`, with every hardware-operation flag false. This is expected because motherboard control is privileged. The automated UAC launch from the desktop execution channel did not reach task registration or emit a LocalSystem report, so the LocalSystem/PawnIO path remains unverified. No UAC bypass was attempted.
- `pchelper-cli runtime-preflight --json` confirmed the updated client can reach the existing `0.3.0.0` service, which correctly reports `UpgradeRequired` and locks writes because it lacks protocol-2 features. The installed service remains `Running` / `Automatic`; it was not replaced or restarted.
- Full verification after the source change: 191 core and 67 integration tests passed (258 total). `render-ui.ps1` rebuilt and rendered all nine dashboard pages to `artifacts\ui-snapshots-service-identity-local-system`; visual review of Cooling confirmed the dark layout and locked runtime state. A fresh unsigned six-component payload at `artifacts\publish-service-identity-local-system-diagnostic-20260713` passed `Test-RuntimePayload.ps1` at protocol 2 and includes `cooling-output-roles`; it was not installed. No fan pulse, calibration, profile application, reset, service replacement, driver/firmware operation, or USB transaction ran in this pass.

## Verification snapshot: 2026-07-13 (UAC local-alpha deployment and pump protection)

- Normal Windows UAC was used; no elevation bypass was attempted. The LocalSystem disposable `direct-prepare` diagnostic on Nuvoton Fan #1 completed with `Prepared=true` and all four control flags (`Apply`, `Verify`, `Rollback`, `Reset`) false. The temporary `0.4.0-alpha` service then completed its authenticated Adapter Host `commission-preflight` for the same Fan #1 / RPM pair with `PREPARE_SUCCEEDED_NO_WRITE`; the temporary service restored the installed service afterwards.
- `scripts\Install-LocalAlphaRuntime.ps1` and `scripts\Restore-LocalAlphaRuntime.ps1` provide a reversible development deployment. The install script validates the six-component protocol-2 payload, stages it under `%ProgramData%\RigPilot\LocalAlpha`, grants `LocalSystem` access, exports the complete `PCHelper` service registry key, copies the stopped service data directory for recovery, switches only the service `ImagePath`, and requires a ready handshake plus read-only probe. On failure it restores the original service key and original running state. It does not overwrite `C:\Program Files\PC Helper`, run an MSI, install a driver, or issue a hardware control command.
- The current active service is the unsigned staged `0.4.0-alpha` runtime at `C:\ProgramData\RigPilot\LocalAlpha\0.4.0-alpha-uac-20260713-212004\payload\service\PCHelper.Service.exe`. Its matching staged CLI reports `Ready`, protocol 2, no missing feature, and `canUseServiceWrites=true`. The original `0.3.0.0` image path and registry backup remain in the deployment manifest; the paired restore script can return to it through UAC. This is not a signed production installation or release qualification.
- The typed CLI command `set-output-role` now persists a service-owned physical-output role with an expected state revision. It validates that the target is a numeric cooling control and that an optional RPM sensor belongs to the exact same controller before the service canonicalizes and saves it. UAC saved `lhm.control:/lpc/nct6798d/0/control/4` as `Pump`, header `AIO_PUMP`, paired with `lhm.sensor:/lpc/nct6798d/0/fan/4` (about 2749 RPM during the read-only probe). A post-save trace query found no operation for that capability. The policy blocks pump commissioning, preflight, calibration, cooling-graph, profile, and tuning routes; no pump command was sent.
- Release build/payload validation and the complete test suite passed after these source changes: 191 core and 67 integration tests (258 total). PowerShell parser validation passed for both deployment scripts and the existing commissioning scripts. The fresh unsigned payload is `artifacts\publish-local-alpha-uac-20260713-211919`.
- UAC then ran a fresh no-write Fan #1 commissioning preflight successfully and one bounded 60% two-second `commission-pulse` for `lhm.control:/lpc/nct6798d/0/control/0` with paired sensor `lhm.sensor:/lpc/nct6798d/0/fan/0` and provisional alias `CHA_FAN1`. The UAC CLI returned exit code 0. The subsequent live trace recorded successful `Prepare` at `2026-07-14T01:23:19Z`, `Apply`, `Verify`, and `ResetToDefault` at `2026-07-14T01:23:23Z`; the post-reset read-only probe reported 1478.6 RPM. There were no running conflicting writers, no service warning, and no WHEA/display-driver event in the post-operation 15-minute System-log query. This is one controller-path/default-recovery data point, not physical header certification, calibration, restart qualification, or a curve approval. RTX 3090 conservative 50% floor remains unchanged.

## Verification snapshot: 2026-07-14 (Fan #1 evidence normalization)

- Added `scripts\Export-CaseFanCalibrationEvidence.ps1`. It is a read-only query/export tool: it only calls `status`, `operation`, and `commission-sessions`, validates an exact expected operation/session/control/RPM identity, and writes a normalized local JSON evidence record. It cannot start calibration, change a profile, reset a controller, or mutate commissioning state.
- Built the Release CLI with zero warnings/errors, then used it against the currently running temporary alpha service. The exact calibration operation `f29e1c16d65e4ea6918b709fdb4db21e` returned `Completed`, with prior-policy restoration, all 21 measurements stable, 1959.36 RPM maximum, no stall at the API 0% command, no restart duty, and zero completed restart cycles.
- The read-only evidence artifact under `artifacts\commissioning` reports `CompletedNoStallObservedAtMinimumCommand` and `curveEligible=false`. Its independent blockers are false restart verification, no observed stall/restart duty, zero restart cycles, and no recorded physical header observation for the declared `CASE_FAN_1` alias. It does not alter the active temporary service, fan state, or RTX 3090 floor.
- Added source-level `GetOperationById` IPC, durable SQLite lookup, and `pchelper-cli operation --id OPERATION_ID --json`. It is read-only and lets a matching future service retrieve historical calibration evidence by immutable ID rather than whichever operation ran most recently. New store and IPC-policy coverage passed. The currently running temporary alpha predates this command and rejects it at protocol parsing, so the evidence script safely records `LatestIdentityCheckedFallback` after comparing the returned latest ID to the expected calibration ID. The current alpha service was not replaced or restarted.

## Verification snapshot: 2026-07-14 (adaptive per-output calibration and graph runtime)

- Source now distinguishes three calibration outcomes: a flat or mismatched tachometer stays blocked; a stable controller that remains running at the API minimum may use only a measured nonzero floor; and an exact zero-RPM graph point requires repeated stop/restart verification. `FanCalibrationV2` schema 3 persists the effective floor duty/RPM, first responsive duty, non-stopping-floor evidence, and explicit verified-stop state.
- Full calibration now requires `PhysicalHeaderObserved`, not only a declared header alias. The dashboard reports that distinction accurately and disables calibration for the existing `CASE_FAN_1` declared-only session. The current Fan #1 result remains historical evidence only: it predates schema 3 and lacks a physical header observation, so it is not promoted.
- `CoolingGraphRuntime` preserves a last-good sensor value for two missed polls. On the third missed poll, evaluation enters maximum-duty emergency recovery; the service sends the maximum command, requests firmware/default control, logs the recovery, and deactivates the graph. Positive graph commands are clamped again after avoid-band and slew processing so they cannot traverse below an output's measured safe floor.
- The source service now activates a qualified graph after a successful V2 profile transaction, verifies each adapter update, restores old graph outputs on profile changes/service stop, and includes graph-output devices in Experimental confirmation. This source path has not been deployed or physically exercised on the reference system.
- A completed, physically observed calibration can now generate an inactive V2 profile and conservative CPU/GPU mixed graph for the exact output. It selects only fresh CPU/GPU temperature sources, clamps all graph requests to the measured nonzero floor, saves graph then profile with revision/idempotency protection, includes graph devices in the later Experimental confirmation, and never applies a fan command during generation. `GetFanCalibrations` exposes only persisted exact-session calibration evidence, so the same flow works after an app/service restart and fails closed if the matching service feature is unavailable. The UI labels the result and fixes the commissioning-card title/badge crop.
- Verification for this change: `dotnet build PCHelper.sln --no-restore --nologo` completed with zero warnings/errors; `dotnet test PCHelper.sln --no-restore --nologo` passed 210 core and 68 integration tests (278 total). Focused tests cover non-stopping adaptive floors, verified stop behavior, legacy schema-2 restart evidence, post-slew floor enforcement, three-poll stale recovery, physical-header calibration gating, persisted adaptive graph generation, exact controller bounds, and read-only calibration lookup. `scripts\render-ui.ps1` rendered all nine dashboard pages to `artifacts\ui-snapshots-adaptive-calibration`; the corrected Cooling page was visually inspected. The repository UI smoke passed with the new `Cooling.CreateAdaptiveCurve` accessibility ID. No installed service, fan command, profile, reset, pulse, or calibration was executed in this pass.

## Verification snapshot: 2026-07-14 (competitor parity ledger and manual curve studio)

- Added `docs\competitor-parity.md`, a clean-room source/evidence ledger comparing the desktop-relevant behaviour of Fan Control, G-Helper, MSI Afterburner, EVGA Precision X1, Armoury Crate, SignalRGB, iCUE, CAM, OpenRGB, and Windows Dynamic Lighting. It explicitly distinguishes implemented source, partial foundations, and blocked write paths; it does not claim replacement parity.
- Advanced Lab now has a calibration-bound manual curve studio on the Cooling page. It accepts two to eight `temperature:duty` points, renders a normalized curve preview, uses the existing maximum CPU/GPU temperature source selection, preserves the exact output's measured nonzero floor, requires the final point to reach the controller maximum, and stores independent rise/fall hysteresis and response settings. It saves a typed graph and inactive V2 profile; it never applies a fan command during editing or saving.
- `CustomCoolingCurveFactory` is core-owned and requires the normal completed, physically observed commissioning session and matching calibration. It rejects an output below the measured floor, non-increasing/invalid temperature points, invalid hysteresis/response bounds, and a missing full-speed thermal ceiling. CPU/pump protections, profile acknowledgement, service compatibility, and later transactional application are unchanged.
- Verification: `dotnet build PCHelper.sln --no-restore --nologo` completed with zero warnings/errors. `dotnet test PCHelper.sln --no-build --no-restore --nologo` passed 213 core and 68 integration tests (281 total), including three new manual-curve factory tests. `scripts\render-ui.ps1 -Configuration Debug -OutputDirectory artifacts\ui-snapshots-competitor-parity-final -Width 1240 -Height 1000` rendered all nine pages; a scrolled Advanced Lab Cooling render visually verified the plotted curve and its placement above the Fan Control import. That inspection caught an initial placement mistake on Profiles, which was corrected before the final render. The UI smoke passed with all manual-curve accessibility IDs, 5,055 visual elements, and 153 named interactive controls; it now also asserts that page-prefixed controls live on their expected dashboard page. No service was installed/restarted and no fan command, profile, reset, pulse, or calibration was run.

## Verification snapshot: 2026-07-14 (local-alpha runtime update)

- `publish.ps1` and `Test-RuntimePayload.ps1` now require `fan-calibrations`, matching `ServiceRuntimeFeatures.RequiredByDashboard`. This prevents a staged payload from passing its release contract while leaving a current dashboard in `UpgradeRequired` state.
- `scripts\Update-LocalAlphaRuntime.ps1` is an elevation-required, reversible update wrapper. It restores an active LocalAlpha service from its exact registry backup before calling the validated local-alpha installer for the new payload. It does not overwrite `Program Files`, install a driver, or issue a hardware command.
- Normal Windows UAC deployed the unsigned local `0.4.0-alpha` payload from `artifacts\publish-runtime-update-20260714-143227` to `%ProgramData%\RigPilot\LocalAlpha\0.4.0-alpha-20260714-143512`. Its deployment manifest records a `Ready` protocol-2 handshake, no missing features, and a successful read-only inventory probe (69 devices, 450 sensors, 23 capabilities, zero warnings). Both staged and source CLI `runtime-preflight --json` returned `Ready`; the freshly launched source dashboard no longer showed the runtime-update warning. No fan, profile, lighting, tuning, driver, firmware, or USB operation ran.

## Verification snapshot: 2026-07-14 (Experimental Control Center)

- Advanced Lab now exposes an `Experimental Control Center` on Overview. It separates the exact `Experimental` capability count from blocked/unsupported controls, lists each capability's bounds/evidence/next-safe-step, and classifies the route as header commissioning, separate GPU validation, safety protected, or missing evidence. It does not relabel any `ReadOnly`, `Blocked`, or `Unsupported` capability.
- Only a bounded, resettable, non-conflicted, non-GPU, non-protected cooling output can be routed into the existing Cooling commissioning wizard. The route merely selects the exact target and navigates to the wizard; it sends no hardware command, does not record a per-device acknowledgement, and does not alter a stored output role. GPU fans retain the separate conservative-floor/restart-validation path. CPU-fan and pump roles remain blocked; the persisted `Fan #5` `AIO_PUMP` role is counted as protected.
- The global Experimental acknowledgement is now explained at the point of use as session-only. It remains distinct from exact-device confirmation and cannot bypass physical-header/RPM pairing, witnessed pulses, commissioning, or pump/CPU safety policy.
- Fixed a dashboard status-order defect: a valid service status was previously read before `IsServiceOnline` was set, so a ready protocol-2 service could retain an obsolete sidebar message saying writes were update-locked. The app now marks the connection online immediately after the valid status reply, before composing that message. The badge wording is also `Service write path ready`, which does not imply every detected hardware control is Verified.
- Verification: Release build completed with zero warnings/errors. Full tests passed: 254 core and 74 integration (328 total). The deterministic UI smoke passed all nine pages, Simple/Advanced separation, the three new Overview automation IDs, both Overview route actions (Cooling and Devices), zero unnamed interactive controls, and zero duplicate IDs (5,687 visual elements, 161 named interactive controls). The rendered Advanced Overview was visually reviewed at `artifacts\verification\experimental-control-center-render\01-overview.png`.
- Live read-only verification against the installed local alpha: `runtime-preflight --json` reported protocol 2 `Ready` with no missing features and `canUseServiceWrites=true`; the service reported healthy, writes enabled, no emergency mode, and its existing policy message that only Verified controls can be written. Persisted output roles remain `CASE_FAN_1` (CaseFan) and `AIO_PUMP` (Pump). No fan, profile, lighting, tuning, reset, driver, firmware, USB, or takeover operation ran in this pass.

## Verification snapshot: 2026-07-14 (contained USB/AIO discovery + GPU-fan write path scoping)

- **Adapter-pack removal** completed a half-built feature: `RemoveAdapterPack` service handler, `AdapterPackManager.Remove` (path-escape-safe, cleans the DB inspection record), and CLI `pack-list`/`pack-install`/`pack-remove`. Local file/DB only; no hardware, no network.
- **Contained USB/AIO controller discovery** replaces the previously-disabled inline path. Discovery runs in a disposable Adapter Host child (`--discover-controllers`) inside a kill-on-close job; `ContainedControllerDiscovery` maps native crash, timeout, nonzero exit, malformed output, and start failure to a *contained* result that never leaks a controller list. A new read-only `DiscoverControllers` IPC command and `pchelper-cli discover-controllers` surface it. The direct probe enumerated the real NZXT Kraken X3 (5x, clean exit); it is read-only inventory with no writable capability. This was deployed to a fresh reversible `0.4.0-alpha` LocalAlpha runtime (`0.4.0-alpha-20260714-190859`) via UAC; live `discover-controllers` returned `Succeeded`/contained (LocalSystem/Session 0 sees 0 controllers, which is the expected session-visibility gap, not a fault). The original `0.3.0.0` image and registry backup remain for restore.
- **RTX 3090 GPU-fan write path, Steps 1-4 of 5 (no live write).** See `docs/qualification/rtx3090-fan-write-path.md`.
  - Step 1: `NvmlTelemetryAdapter` detects `nvmlDeviceSetFanSpeed_v2` + `nvmlDeviceSetFanControlPolicy` by symbol presence only (never marshalled) and surfaces a read-only `nvml.fan-transport:<device>` feasibility card. Driver `610.62` exports both, so NVML is a viable transport (NVAPI not needed). The telemetry adapter's write methods remain `NotSupportedException`, test-guarded.
  - Step 2: `NvidiaGpuFanAdapter` implements the full prepare/apply/verify/rollback/reset contract over an injectable `IGpuFanCoolerTransport`; read-only unless `enableWrites`. 12 fake-adapter tests cover floor/ceiling rejection, read-back verify, rollback-to-prior/auto, reset-to-auto, conflict→Blocked, and unchecked-value refusal.
  - Step 3: the real adapter runs through the real `ProfileTransactionEngine` (domain `Cooling`) — commits on success, rolls back to automatic on verification failure (2 integration tests).
  - Step 4: `NvmlGpuFanCoolerTransport` is the only class that marshals the NVML setters, triple-gated by `enableWrites`, the `PCHELPER_GPUFAN_REAL_TRANSPORT=1` operator opt-in (checked at call time), and driver setter presence. A real bench preflight read bounds `[50,100]%` and current duty `32%` and confirmed the write refuses without the opt-in. **No fan was commanded.**
  - Step 5 (bounded live duty pass): DONE, user-authorised. A first-ever verified GPU-fan write on the reference RTX 3090. An elevated standalone runner commanded 60% then 80%: the physical fan rose 0%->56%->73%, read-back targets matched exactly, both fans were restored to the driver automatic curve, and `nvidia-smi` then read 0% at 44 C. Zero WHEA, display-reset, or nvlddmkm events. NVML fan control returns `NVML_ERROR_NO_PERMISSION` when not elevated (a non-elevated attempt failed safe with no physical change); the LocalSystem service satisfies elevation. This is one bounded pass on one system via a standalone runner = `Experimental` / SingleSystem evidence, NOT `Verified` and NOT full qualification. The deployed `0.4.0-alpha-190859` service predates this code and still exposes the GPU fan as `ReadOnly`; the write was not run through the service. Remaining before a `Verified` claim: repeated cycles, soak, a second system, a live rollback exercise, and the in-product acknowledgement gate.
- Validation: changed projects Release-build with 0 warnings/0 errors. Full suite passed: 269 core and 94 integration (363 total), up from 328, with no regressions. No CPU/GPU tuning, voltage, pump, RGB, driver, firmware, or takeover write was enabled; the takeover signature gate and pump protections are unchanged. The RTX 3090 fan write is the only new physical-write capability and remains Experimental/elevation-gated.
- CPU tuning + Intel Arc research/build (docs/qualification/cpu-tuning-and-intel-arc.md): deep research confirmed the safe path and the hard limits. AMD Ryzen — a rule-compliant SMU path exists (signed PawnIO + a RyzenSMU module; WinRing0-based ZenStates-Core is excluded), but Curve Optimizer/PBO writes are voltage/VF manipulation that stay `Blocked` until per-family mailbox bounds, applied-curve read-back, guaranteed stock reset, and a boot-recovery revert are qualified. NO live CPU write was built; the `amd.zen.tuning` Blocked card reason was sharpened with this finding. Intel Arc — built `IntelGraphicsControlAdapter`, a read-only presence detector for `ControlLib.dll` + `ctlInit` (no IGCL handle created, no write), wired into service/CLI/App and surfacing a `ReadOnly` `igcl.feasibility` card on Intel systems; it stays silent and fails safe on this NVIDIA reference machine (verified: 0 igcl cards). The full IGCL telemetry adapter was deliberately NOT shipped because complex struct P/Invoke is untestable without an Intel Arc GPU. Tests: 269 core + 97 integration. No new NuGet dependency (Intel is a system-DLL P/Invoke).
- Library integration (ScottPlot): added `ScottPlot.WPF` 5.1.59 (MIT + SkiaSharp MIT; attributed) and replaced the hand-rolled Overview sensor-comparison `Canvas`/`Polyline` with a dark-themed `WpfPlot` (`Overview.ComparisonChart`). `SensorComparisonSeriesDisplay` now carries raw `Values`; the code-behind normalises each series to [0,1] and refreshes on collection change. Verified: App Release-clean under warnings-as-errors; the deterministic offline render pipeline survives the SkiaSharp control (17 UI smoke/presentation tests pass, all nine pages render, dark theme intact); full suite 269 core + 96 integration. The running dashboard was not needed to be killed (it was not running); the service and the user's live VRChat/SteamVR session were preserved. Note: WPF-UI/CommunityToolkit.Mvvm/H.NotifyIcon were NOT force-added — the app already has a polished custom dark theme + hand-rolled MVVM/tray, so those would be low-value/high-risk reskins or invasive refactors, and dead deps are disallowed by the dependency discipline.
- Library integration (NvAPIWrapper): added `NvAPIWrapper.Net` 0.8.1.101 (LGPL-3.0, GPL-3.0-compatible; attributed in THIRD_PARTY_NOTICES) and implemented `NvApiGpuFanCoolerTransport : IGpuFanCoolerTransport` — an NVAPI cooler-control fan transport alongside the hand-rolled NVML one, gated identically by the acknowledged-arm flag. NuGet restore/lock-file regen worked; no vulnerabilities flagged; net10-compatible (net45 assembly via compat). Real-hardware read-only test: NVAPI loaded, read bounds `[50,100]%`, and the write correctly refused without an arm. Known limitation: NVAPI `CurrentPolicy`/`CurrentLevel` read-back is imprecise (reported `Manual/68` while the fan was plainly on the automatic curve tracking a live VRChat/SteamVR GPU load) — ReadState policy mapping needs live refinement before this transport is used for verification. It is NOT wired as the service's default transport (NVML remains wired); it is an available, tested alternative. 269 core + 96 integration tests pass. Note on scope: a broader "integrate everything" was declined — UI-tier libraries (WPF-UI/ScottPlot/etc.) are high-risk to rush into this app given TreatWarningsAsErrors + the custom theme + the deterministic render pipeline + SkiaSharp native deps; and safe CPU tuning / Intel IGCL C# are build-from-scratch efforts (custom signed PawnIO module; hand-written P/Invoke), not dependencies.
- Real-use arm keystone (source only, NOT yet deployed): the GPU-fan write is now gated by an in-product acknowledged arm instead of a dev env var. `NvmlGpuFanCoolerTransport` marshals setters when write-capable but blocks calls behind an in-process `Armed` flag (or the env override); `NvidiaGpuFanAdapter` takes a live `Func<bool> isArmed` and reports `Experimental` only when armed, else `ReadOnly`. The service registers the adapter disarmed and exposes a mutation IPC `SetGpuFanControlArmed` requiring `ConfirmExperimental` + exact-device confirmation (`nvidia:gpu-0`); disarming restores the automatic curve first. CLI: `gpu-fan-arm --confirm-experimental --confirm-device <id>` / `gpu-fan-disarm`. Tests: arm toggle flips ReadOnly<->Experimental and gates writes; acknowledgement deny-paths covered. Full suite 269 core + 95 integration (364) green. This is built and tested but the live LocalAlpha service still runs the earlier `194628` payload (GPU fan `ReadOnly`); deploying the arm keystone is pending a UAC approval that was repeatedly declined. Open-source note: the user asked to pull in more online libraries; declined for the safety-critical service (locked restores + license/vuln review required) — the vetted stack (LibreHardwareMonitor, already exposing Experimental GPU fan controls via NVAPI; NVML; NSec) already covers this.
- Earlier deployment: the GPU-fan code is now wired into the service. `PCHelperRuntime` registers `NvidiaGpuFanAdapter` for `nvidia:gpu-0` channel `0` only when NVML reports valid bounds, with `enableWrites` tied to the `PCHELPER_GPUFAN_REAL_TRANSPORT=1` operator opt-in — so it deploys **ReadOnly by default** even though the service is LocalSystem (elevated); the transport re-checks the opt-in at call time. Service stop best-effort restores the fan to the automatic curve. The current live LocalAlpha runtime is `0.4.0-alpha-20260714-194628` (reversible; original `0.3.0.0` Program Files image + registry backup retained). Verified live: `runtime-preflight` Ready/protocol-2/no-missing-features; `capabilities` shows `gpufan.duty:0` = `ReadOnly` and `nvml.fan-transport:...` = `ReadOnly`; service healthy, no emergency mode; `nvidia-smi` fan 0% at 47 C; 0 WHEA/display-reset events. The deploy did NOT arm a live write. Note: `Update-LocalAlphaRuntime.ps1` aborts its restore-first branch when replacing a LocalAlpha image because a benign `reg.exe` success message on stderr trips `ErrorActionPreference=Stop`; re-running from the restored Program Files image (install-direct path) succeeds. Worth hardening that script later.

## Verification snapshot: 2026-07-15 (dashboard presentation fixes)

- Live-screenshot UI review found and fixed three presentation defects. Diagnostics `SERVICE` stat card truncated long service versions; it now wraps at FontSize 16 with a full-version tooltip. The Devices monitor-brightness section rendered two near-duplicate summary lines; the redundant `MonitorBrightnessSummary` property and its bindings were removed, the surviving live `MonitorBrightnessStatus` message keeps its automation ID, and its count grammar now pluralises correctly ("1 display; 1 exposes"). The sidebar service/runtime messages clipped mid-line under `MaxHeight`; the caps were removed and both messages carry full-text tooltips.
- Presentation-only change: no IPC, capability, safety, or persistence path was touched. Debug solution build completed with zero warnings/errors; 278 core and 124 integration tests passed (402 total). `render-ui.ps1 -Configuration Debug` rendered all nine pages to `artifacts\ui-snapshots-ui-review-fixes`; visual review of Diagnostics and Devices confirmed the wrapped service version, the single correct brightness summary, and the unclipped sidebar runtime message. The Release build was intentionally not produced because the user's running Release dashboard (from `src\PCHelper.App\bin\Release`) locks its output; rebuild Release after that process exits.
- A second presentation pass fixed profile-card title truncation ("GPU Fan Cycle...") — card names now wrap to two lines with full-name/objective tooltips — and corrected the Simple-mode Cooling/Performance status labels, which rendered every state in warning orange: new presentation-only `CalibrationAvailabilityTone`/`TuneAvailabilityTone` properties drive Safe (green) styling when the engine is ready and keep Warning otherwise. Rebuilt with zero warnings/errors, all 402 tests passed, and `artifacts\ui-snapshots-ui-review-fixes-2` renders confirmed the wrapped titles and tone-correct labels on Profiles, Cooling, and Performance.

## Verification snapshot: 2026-07-15 (beta slice 1: GPU power limit, WGC video recording, update-script hardening)

- The beta roadmap lives at `docs/beta-roadmap.md` (competitive plan, release train 0.5→1.0).
- `Update-LocalAlphaRuntime.ps1` now runs its restore-first child under a scoped `Continue` preference and verifies the deterministic post-condition (ImagePath no longer under LocalAlpha) instead of dying on a stray native-stderr ErrorRecord; real child `throw`s still propagate. Parse check passed. Not yet exercised live under UAC.
- **NVML GPU power-limit control (source, disarmed by default).** New `IGpuPowerLimitTransport` seam, `NvidiaGpuPowerLimitAdapter` (capability `gpupower.limit:0`, watts, ControlDomain.Gpu, prepare/apply/verify/rollback/reset, driver-constraint clamping, rollback to captured prior limit, reset to vendor default), and `NvmlGpuPowerLimitTransport` — the only marshaller of `nvmlDeviceSetPowerManagementLimit`, triple-gated (enableWrites + armed-flag/`PCHELPER_GPUPOWER_REAL_TRANSPORT=1` checked at call time + setter presence). Service registers it ReadOnly/disarmed; `SetGpuPowerLimitArmed` IPC mirrors the fan arm flow (Experimental confirmation + exact-device id, disarm restores the vendor default first); service stop restores the default limit. CLI: `gpu-power-arm` / `gpu-power-disarm`. 15 new tests (13 adapter incl. rogue-value/conflict/invalid-constraint refusals, 2 real-transaction-engine commit/rollback). A read-only `nvidia-smi -q -d POWER` check on the reference RTX 3090 confirmed the exact modelled constraints (min 100 W, max 385 W, default/current 350 W). No live power-limit write has been issued; the deployed LocalAlpha service predates this code.
- **Bounded WGC video recording (user-session).** ScreenRecorderLib 6.6.0 (MIT, attributed in THIRD_PARTY_NOTICES) added to PCHelper.App only, referenced directly as its x64 assembly with `ExcludeAssets="build"` (the package's Platform-keyed targets reject AnyCPU; the app is now `PlatformTarget` x64, matching the x64-only deployment). New `IDesktopVideoRecorder`/`WindowsDesktopVideoRecorder`: explicit visible-session confirmation, idempotency key, freshly re-discovered display/window targets only, 5–600 s duration clamp enforced by a local timer, H.264 via WGC + Media Foundation, optional system-loopback audio (microphone is never enabled), output only under Videos\RigPilot\Recordings. New IPC: `StartVideoRecording` / `StopVideoRecording` (mutations) and `GetVideoRecordingStatus` (read-only), user-agent-owned; the service rejects them as WRONG_EXECUTION_CONTEXT. Dashboard Games & tools card gained Start/Stop recording controls (`Games.StartVideoRecording` / `Games.StopVideoRecording` / `Games.VideoRecordingStatus`); `OverlayBridgeProbe.ProbeRecordingPreflight` now reports the configured encoder. 4 new tests cover confirmation/idempotency denial before the recorder is invoked, start→status→stop through the user agent, and the Windows recorder's duration and forged-target rejections (validation precedes any encoder construction). No real video was recorded in this pass.
- Validation: locked restore regenerated `src/PCHelper.App/packages.lock.json`; `dotnet list package --vulnerable` clean; Debug build 0 warnings/0 errors; full suite 278 core + 143 integration (421 total) passed; all nine pages rendered to `artifacts\ui-snapshots-video-capture` with ScreenRecorderLib.dll present in the App output. Live recording, live power-limit writes, and the LocalAlpha redeploy remain user-gated.

## Verification snapshot: 2026-07-15 (beta slice 2: RTSS OSD bridge)

- **RTSS shared-memory bridge (user-session, explicit-request writes only).** New `IRtssOsdBridge`/`RtssSharedMemoryBridge` in PCHelper.App implements the documented `RTSS_SHARED_MEMORY` v2 contract (provenance recorded in THIRD_PARTY_NOTICES: field layout cross-checked against two public mirrors of the SDK's `RTSSSharedMemory.h`; no RTSS code copied). Every operation re-opens the mapping and validates signature `'RTSS'`, major version 2, minimum entry sizes, and header bounds against the actual mapping capacity before any access; wrong signature/version/bounds → hands-off with an explanatory status. Read path: per-app frame statistics (PID, name, FPS from dwTime0/dwTime1/dwFrames, frame time from dwFrameTime µs). Write path: claims at most one OSD slot by the SDK's ownership convention (never slot 0, never a foreign-owned slot; refuses when all slots are taken), bumps `dwOSDFrame`, and releases (zeroes) its slot on stop and on user-agent dispose.
- New IPC: `GetRtssOsdBridgeStatus` / `GetRtssFrameStats` (read-only) and `PublishRtssOsdText` / `ReleaseRtssOsd` (mutations); all user-agent-owned, rejected by the service as WRONG_EXECUTION_CONTEXT. Publishing requires `ConfirmedThirdPartyOsdWrite`; text is sanitised to ≤255 printable-ASCII/newline chars. User-agent handshake gained the `rtss-osd` feature flag.
- Dashboard: the Games & tools "Local desktop OSD" card gained an "RTSS in-game OSD" section (`Games.PublishRtssOsd` / `Games.ReleaseRtssOsd` / `Games.RtssOsdPublishStatus`). The published line is built from the same layout/sensors as the desktop OSD and refreshes on each snapshot tick while publishing (single-flight guard; a vanished RTSS stops publishing safely with a card message instead of an error dialog). `OverlayBridgeProbe` copy updated: the old "writes disabled until ABI audited" wording is superseded by the runtime ABI validation described above.
- 10 new tests (`RtssSharedMemoryBridgeTests`) run against a faithful in-memory v2 layout in a named memory-mapped file: claim/in-place-update/release with frame-counter bumps, foreign-slot and slot-0 non-interference, all-slots-foreign refusal, wrong-signature/wrong-major-version/out-of-bounds-header refusals (memory untouched), FPS/frame-time math, absent-mapping fail-safe, sanitisation bounds, and the user-agent confirmation gate end-to-end. RTSS is not installed on this machine, so no live RTSS pass has run yet; first live validation is queued with the other user-witnessed passes.
- Validation: Debug build 0 warnings/0 errors; full suite 278 core + 153 integration (431 total) passed; all nine pages rendered to `artifacts\ui-snapshots-rtss-osd`.

## Verification snapshot: 2026-07-15 (beta slice 3: Kraken X3 read-only telemetry)

- **Kraken X3 liquid-cooler telemetry (read-only, contained).** New `KrakenX3TelemetryReader` in PCHelper.Adapters derives its protocol from liquidctl's GPL-3.0 `kraken3.py` (attributed with review date in THIRD_PARTY_NOTICES; licence-compatible derivation, no code copied): VID 0x1E71, PIDs 0x2007/0x2014; the firmware streams unsolicited 64-byte status reports marked `0x75 0x02` carrying liquid temperature (bytes 15/16), pump rpm (little-endian 17/18), and pump duty (19). **This path performs no HID writes of any kind** — it opens the stream, reads ≤12 reports with a 2 s timeout, and parses the first plausible report. The parser anchors on the `0x75 0x02` marker at index 0 or 1 (HidSharp may or may not prepend a report-ID byte relative to hidapi) and rejects readings outside plausible bounds (temp 1–90 °C, rpm ≤6000, duty ≤100, 0xFF sentinel).
- Containment mirrors the HID inventory: the read runs in the Adapter Host child (`--read-kraken`), `ContainedKrakenTelemetry` maps start failure/native crash/timeout/malformed output/mismatched schema to non-Succeeded outcomes, and any non-Succeeded outcome scrubs the readings so partial data cannot pose as live telemetry. New read-only IPC `ReadKrakenTelemetry` (service-side); Devices page "Connected peripherals" card gained a "Liquid cooler telemetry" row (`Devices.ReadKrakenTelemetry` / `Devices.KrakenTelemetryStatus`).
- 8 new tests: 5 parser (both anchor alignments, marker rejection, implausible-reading rejection incl. the 0xFF broken-sensor sentinel, truncated report) and 3 containment (success, reading-scrub on non-success, crash/timeout/start-failure/garbage/exit-code containment).
- **Live evidence (this machine, 2026-07-15):** a real `--read-kraken` child run detected the Kraken (got past DeviceNotFound) and returned `AccessDenied` with the designed message because NZXT CAM (running, 5 processes + 2 helpers) holds the HID stream; RigPilot did not force access and CAM was not touched. A live numeric pass (liquid °C / pump rpm) is queued for a user-witnessed run with CAM closed.
- Validation: Debug build 0 warnings/0 errors; full suite 281 core + 160 integration (441 total) passed; vulnerability scan clean; all nine pages rendered to `artifacts\ui-snapshots-kraken-rtss`. No package changes (HidSharp was already a direct dependency).

## Verification snapshot: 2026-07-15 (0.6 slice 1: NVAPI GPU clock offsets)

- **NVAPI GPU clock-offset control (core + memory, disarmed by default).** New `IGpuClockOffsetTransport` seam, `GpuClockOffsetBounds`/`GpuClockOffsetState` (kilohertz; vendor default is always 0 kHz = stock clocks), and `NvapiGpuClockOffsetTransport` over NvAPIWrapper performance states 2.0 — the only caller of `SetPerformanceStates20`, submitting exactly one P0 frequency-delta clock entry per write with **no voltage entries and no overvolting settings ever constructed or submitted** (the V1 payload cannot carry an overvolting section). TryCreate refuses when the driver marks pstates non-editable; reads are side-effect free; writes require the armed flag or `PCHELPER_GPUCLOCK_REAL_TRANSPORT=1` checked at call time.
- `NvidiaGpuClockOffsetAdapter` (one instance per domain: `gpuclock.core:0` / `gpuclock.memory:0`, unit MHz, ControlDomain.Gpu) clones the power-limit shape: driver delta range is the authoritative clamp discovered read-only before any write, Apply re-validates (defence in depth), Verify reads back within 1 MHz, rollback restores the exact prior offset (fallback stock clocks), reset returns to 0. Service registers both ReadOnly/disarmed behind one arm keystone: `SetGpuClockOffsetArmed` mirrors the fan/power flows (Experimental confirmation + exact-device id; disarm returns both domains to stock clocks first), service stop restores 0 offsets before dispose. CLI: `gpu-clock-arm` / `gpu-clock-disarm`.
- 15 new tests: 13 adapter (ReadOnly/Experimental/Blocked states, arm gating, driver-range rejection both sides, rogue out-of-range Apply refusal, verify mismatch, rollback/fallback/reset, cross-domain capability rejection, unavailable-range refusal) and 2 real-transaction-engine (commit at +120 MHz; rollback to prior offset when the driver silently keeps stock clocks).
- **Live read-only evidence (this machine, 2026-07-15):** a scratchpad NVAPI probe (no setter call in the binary) confirmed the RTX 3090 reports editable pstates with P0 Graphics delta range [-1000000, 1000000] kHz, P0 Memory [-1000000, 3000000] kHz, both currently 0; P0 Video is non-editable and the transport only ever addresses Graphics/Memory. No clock write has been issued; the deployed LocalAlpha service predates this code.
- Validation: Debug build 0 warnings/0 errors; full suite 281 core + 175 integration (456 total) passed. No package changes (NvAPIWrapper.Net was already a direct dependency).

## Verification snapshot: 2026-07-15 (0.6 slice 2: frame-stats surface + OC Scanner policy audit)

- **OC Scanner / VF curve editor deferred by policy (audited).** Reflection over the referenced NvAPIWrapper 0.8.1.101 assembly confirmed: no OC Scanner invocation is wrapped at all, and the per-point VF curve read exists only as `PrivateVFPCurveV1`/`NvAPI_GPU_GetVFPCurve` — NDA/undocumented NVAPI surfaces absent from the public SDK. The ground rule "documented vendor APIs only" therefore excludes both; `docs/beta-roadmap.md` records the audit and the substitute path (screening pipeline over user-chosen public-SDK offsets). The shipped pstates20 clock offsets are in the public NVAPI SDK and are unaffected.
- **RTSS frame statistics on the dashboard.** The read half of the RTSS bridge (previously IPC-only) is now surfaced: the Games & tools RTSS section gained "Read frame stats" (`Games.ReadRtssFrameStats` / `Games.RtssFrameStatsStatus`) listing each monitored application's FPS and frame time via `GetRtssFrameStats`. Fails safe to the bridge's explanatory message when RTSS is absent.
- **Per-GPU fan curve binding proven (no new engine code needed).** Audit confirmed the cooling-graph loop already drives any Cooling-domain capability with numeric bounds + a reset path through prepare/apply/verify, and the GPU fan capability qualifies once armed — including the physically-observed commissioning/calibration gate, which correctly applies to the GPU fan like any output. New `GpuFanCoolingGraphBindingTests` (3 tests) prove the binding end to end against the real `NvidiaGpuFanAdapter`: a GPU-temp curve settles at the interpolated duty (60 °C → 65 %) and applies through the exact service-loop shape; sustained sensor loss commands maximum duty and reset restores the driver-automatic curve; and the armed capability satisfies every invariant `ApplyCoolingGraphOutputsAsync` re-checks per tick. First live GPU-fan graph activation still requires the arm + a commissioning session (user-witnessed).
- Validation: Debug build 0 warnings/0 errors; full suite 459 tests passed (281 core + 178 integration); all nine pages rendered to `artifacts\ui-snapshots-frametimes`.

## Verification snapshot: 2026-07-15 (beta redeploy payload staged + PawnIO runtime discovered)

- **LocalAlpha redeploy payload built and validated (not deployed).** `scripts\publish.ps1 -Version 0.4.0-alpha-20260715-beta2` published all six components to `artifacts\publish` (the win-x64 RID output paths avoid the running dashboard's Release file locks); `Test-RuntimePayload.ps1` passed (ProductVersion 0.4.0-alpha-20260715-beta2, protocol 2, 6 components). This payload contains every beta slice: GPU fan/power/clock arm keystones, video recording, RTSS bridge, Kraken telemetry, and frame stats. Deployment remains user-gated behind UAC (`Update-LocalAlphaRuntime.ps1 -PayloadRoot artifacts\publish`).
- **PawnIO runtime discovered live on the reference machine (2026-07-15):** `C:\Program Files\PawnIO` holds PawnIOLib.dll, PawnIOLib.h, and PawnIOUtil.exe, and the signed PawnIO kernel driver service reports Running. **No PawnIO modules (.amx) are installed** — the RyzenSMU module must be sourced (PawnIO modules repo / as LibreHardwareMonitor embeds them), licence-reviewed, and loaded by RigPilot itself via `pawnio_load`. This is the 0.7 CPU-stack entry point; the existing read-only `PawnIoRuntimeProbe` already reports presence without loading anything.
- Validation: no repo code changed in this pass beyond earlier snapshots; full suite remains 459 passing.

## Verification snapshot: 2026-07-15 (deploy failure root-caused: duplicate clock-adapter manifest id)

- **The first beta redeploy attempt failed and rolled back cleanly.** The staged `0.4.0-alpha-20260715-beta2` service crashed on startup (`ArgumentException: An item with the same key has already been added. Key: nvidia.gpuclock` — Application event log, PCHelper.Service id 3000): both clock-offset adapter instances (core + memory) shared one manifest id, and `ProfileTransactionEngine` keys adapters by manifest id. The reversible deployment contract behaved exactly as designed — the installer's readiness gate caught the crash, restored the original Program Files service registration, and the service is Running on the pre-alpha image.
- **Fix:** per-domain manifest ids (`nvidia.gpuclock.core` / `nvidia.gpuclock.memory`) built in the adapter constructor; capability descriptors and health reports now carry `Manifest.Id`. **Test-gap closed:** the original tests never registered both domains together; a new regression test constructs one `ProfileTransactionEngine` with both adapters, asserts distinct manifest ids, and commits a core-offset profile through it.
- Payload rebuilt as `0.4.0-alpha-20260715-beta3` and re-validated (6 components, protocol 2, Passed). Full suite 460 tests, 0 warnings. Redeploy remains user-gated.

## Verification snapshot: 2026-07-15 (beta3 LocalAlpha deployed and verified live)

- **The user deployed `0.4.0-alpha-20260715-beta3` via UAC (`Update-LocalAlphaRuntime.ps1`) and it is live.** Verified read-only after the deploy: service Running from `C:\ProgramData\RigPilot\LocalAlpha\0.4.0-alpha-20260715-195541\payload\service\PCHelper.Service.exe`; `runtime-preflight` reports state Ready, client/service both beta3, protocol 2, `canUseServiceWrites: true`.
- **All four GPU capability cards registered on the live service, correctly disarmed:** `gpuclock.core:0` ReadOnly [-1000..1000] MHz, `gpuclock.memory:0` ReadOnly [-1000..3000] MHz, `gpufan.duty:0` ReadOnly [50..100] %, `gpupower.limit:0` ReadOnly [100..385] W — ranges match the earlier read-only driver evidence exactly, and the duplicate-manifest-id fix is confirmed in production (both clock domains coexist). The earlier beta2 attempt's crash-and-rollback is documented in the previous snapshot.
- Remaining user gates: relaunch the dashboard from the new payload (`artifacts\publish\app\PCHelper.App.exe`), witnessed live passes (Kraken read with CAM closed, arm-gated GPU fan/power/clock writes, test recording, RTSS publish if installed), signing signup, 24 h soak.

## Verification snapshot: 2026-07-15 (0.7 milestone: Ryzen SMU read-only telemetry live via PawnIO)

- **The 0.7 "RyzenSMU read-only telemetry" roadmap item is delivered with zero new code.** LibreHardwareMonitorLib 0.9.6 (shipped since beta3) embeds compiled PawnIO modules as resources (`RyzenSMU.bin`, `AMDFamily17.bin`, `IntelMSR.bin`, LPC/SMBus families — confirmed by assembly-resource inspection of the exact NuGet binary) and loads them automatically through the signed PawnIO driver the user installed. No `.amx` sourcing, licence-review, or new P/Invoke was needed; the module blobs arrive inside the already-attributed MPL-2.0 library.
- **Live evidence (deployed beta3 service, read-only `probe`, 2026-07-15):** 450 sensors total; the 5800X reports per-core SMU power (`Core #1–8 (SMU)`), package power 54.6 W, `Core (Tctl/Tdie)` 62.3 °C, `CCD1 (Tdie)` 54.5 °C, `Core/SoC (SVI2 TFN)` voltages, per-core VIDs, and per-core real + effective clocks — all `quality=Good`. Per-core effective clocks confirm the MSR module path; SMU-labelled sensors confirm the RyzenSMU mailbox path.
- Docs updated: the AI_CONTEXT PawnIO status row and the roadmap Workstream C beta item now record telemetry-live/writes-Blocked. The next CPU step (0.6–0.8) remains the gated PBO-limit write family per `docs/qualification/cpu-tuning-and-intel-arc.md`; nothing in this snapshot un-gates any write.

## Verification snapshot: 2026-07-16 (0.7 slice 2: Ryzen SMU read-only PBO feasibility pass)

- **PBO qualification gate step 1 implemented (read-only, contained).** New `PawnIoModuleSession` — a minimal P/Invoke wrapper over the installed PawnIOLib's documented open/load/execute/close surface (API from the locally installed `PawnIOLib.h`); `TryOpen` degrades to a reasoned failure without admin rights. New `RyzenSmuFeasibilityReader`: extracts the `RyzenSMU.bin` PawnIO module at runtime from the already-shipped LibreHardwareMonitorLib 0.9.6 resources (no new module distribution), then calls **only read-class module functions** — `ioctl_get_code_name`, `ioctl_get_smu_version`, `ioctl_resolve_pm_table`, `ioctl_update_pm_table` (the same PM-table refresh LHM issues every poll), `ioctl_read_pm_table`. No tuning, limit-set, or register-write module function is referenced; a test pins the exact allowed function-name list as an invariant.
- PM-table decode is allowlist-gated to the audited Zen 2/3 layouts (Matisse 0x240803/0x240903, Vermeer 0x380804/0x380805/0x380904/0x380905 — offsets cross-checked against ryzen_monitor's pm_tables mapping: floats 0/1 PPT W, 2/3 TDC A, 4/5 THM °C, 8/9 EDC A, limit/value pairs). Unrecognised versions report raw evidence (SMU firmware, table version) with **no decoded numbers**; short tables fail rather than invent values.
- Plumbing mirrors the Kraken pass: Adapter Host `--read-ryzen-smu` child, `ContainedRyzenSmuFeasibility` (scrubs limits on any non-Succeeded outcome), read-only IPC `ReadRyzenSmuFeasibility`, CLI `ryzen-smu-feasibility`. This evidence never un-gates a CPU write — CPU/SMU tuning stays Blocked.
- 9 new tests: Vermeer decode from documented offsets, unaudited-version refusal, truncated-table refusal, read-class-only function invariant, injected-PawnIoException containment, live no-admin fail-safe, and 3 contained-process tests (success passthrough, limit scrub on non-success, crash/timeout/start/garbage/exit containment).
- **Live evidence (2026-07-16, non-elevated):** the `--read-ryzen-smu` child extracted the module from LHM successfully and returned `PawnIoUnavailable` with `pawnio_open HRESULT 0x80070005` — the designed fail-safe. A LocalSystem pass (real PPT/TDC/EDC numbers from the 5800X) requires the beta4 redeploy, which is staged: `0.4.0-alpha-20260716-beta4` published and payload-validated (6 components, protocol 2).
- Validation: Debug build 0 warnings/0 errors; full suite 469 tests (284 core + 185 integration) passed.

## Verification snapshot: 2026-07-16 (0.6 slice 3: frame-rate benchmark sessions)

- **RTSS-sampled frame-rate benchmark (user-session, passive).** New `FrametimeBenchmarkRecorder` + pure `FrametimeBenchmarkStatistics` in PCHelper.App: a session samples the RTSS bridge every 250 ms, keeps only distinct RTSS ~one-second measurement windows (consecutive identical raw readings dropped), and on stop/deadline computes session mean/min/max FPS, the means of the worst 1% and 0.1% of windows, and mean frame time. **Honest labelling is part of the contract:** the results record and dashboard text state these are window-based lows, not per-frame 1% lows (which would need per-frame data RigPilot does not collect). Duration clamped 10–3600 s (dashboard uses 300 s); a run fails safely after ~5 s without a presenting application or if RTSS becomes unreadable; no injection or OSD write is involved.
- New IPC: `StartFrametimeBenchmark` / `StopFrametimeBenchmark` (mutations) and `GetFrametimeBenchmarkStatus` (read-only), user-agent-owned and service-rejected; handshake feature `frametime-benchmark`. Games & tools RTSS section gained Start/Stop benchmark controls (`Games.StartFrametimeBenchmark` / `Games.StopFrametimeBenchmark` / `Games.FrametimeBenchmarkStatus`). Interface member named `StopBenchmark` (CA1716).
- 6 new tests: statistics math on a 200-window distribution (mean/extremes/worst-1%-pair/worst-0.1%-floor), window dedupe + completion message, sustained-absence failure, invalid-request and concurrent-session rejection, explicit process-id targeting, and user-agent routing incl. BENCHMARK_IN_PROGRESS on double start.
- Validation: Debug build 0 warnings/0 errors; full suite 475 tests (284 core + 191 integration) passed; all nine pages rendered to `artifacts\ui-snapshots-benchmark`. A live benchmark needs RTSS installed and a running game — queued with the RTSS witnessed passes.

## Verification snapshot: 2026-07-16 (software line complete; 1.0 gap is evidence, not code)

- `Test-ReleaseReadiness.ps1` run: `CanPublishSignedAlpha=false` (no code-signing certificate) and `CanPublishVersionOne=false` (0/18 signed physical-system records; every CPU/GPU/motherboard family requirement unpopulated; `blockingDefects` empty). The qualification ledger is `docs/qualification/reference-system.json`.
- Final beta payload `0.4.0-alpha-20260716-beta5` published and validated (6 components, protocol 2); includes every slice through the frame-rate benchmark. Vulnerability scan clean across all 8 projects. The deployed service is still `beta3`; deploying beta4/beta5 remains user-gated.
- `docs/beta-roadmap.md` gained a "Status ledger — 2026-07-16" section recording what shipped and the exact non-code path to 1.0: signing enrolment, the 18-system community/lab qualification matrix, and the user-witnessed live passes. No qualification record may be fabricated to satisfy the gate.

## Verification snapshot: 2026-07-16 (beta5 deployed; first LocalSystem SMU evidence)

- **User-approved UAC deploy of `0.4.0-alpha-20260716-beta5` succeeded** via `Update-LocalAlphaRuntime.ps1` (restore-then-install contract; `RestoredExistingLocalAlpha=True`). Live image: `C:\ProgramData\RigPilot\LocalAlpha\0.4.0-alpha-20260716-083019`. `runtime-preflight` Ready (dashboard+service beta5, protocol 2); `status` healthy, writes enabled, only Verified controls writable.
- All four GPU control capabilities present and correctly **ReadOnly while disarmed**: `gpuclock.core:0` [-1000..1000] MHz, `gpuclock.memory:0` [-1000..3000] MHz, `gpufan.duty:0` [50..100] %, `gpupower.limit:0` [100..385] W — each hazard-tagged with `resetGuarantee=ReadBackVerified`.
- **First elevated (LocalSystem) Ryzen SMU feasibility evidence on the reference 5800X:** `pchelper-cli ryzen-smu-feasibility` → Succeeded. SMU firmware 56.76.0, PM table 0x380905 (audited Vermeer layout), codename id 11. PPT 70.22 / 160 W, TDC 29.67 / 110 A, EDC 148.93 / 160 A, THM 53.63 / 90 °C. Read-only qualification evidence; CPU/SMU writes remain Blocked.
- Reliability note: the first invocation failed transiently — `ioctl_update_pm_table` HRESULT 0x8007054F (ERROR_INTERNAL_ERROR), immediate retry succeeded. Likely SMU mailbox contention with the service's own LHM PawnIO polling. Candidate hardening: one bounded retry inside the feasibility handler; not yet implemented.
- Remaining Phase 1 witnessed passes (user at machine): Kraken read with CAM closed, arm-gated GPU fan/power/clock writes with disarm-to-defaults verification, test recording, RTSS suite (RTSS not installed as of 2026-07-15), 24 h soak. Next-steps prompt: `docs/next-steps-prompt.md`.

## Verification snapshot: 2026-07-16 (Phase-4/0.9 no-hardware line: gated PBO scaffolding, ADLX, pack SDK, qualification drafts, portable mode, localization)

- **CPU PBO tuning scaffolding (gate steps 2–4, fully inert).** `SmuTuningControl` seam (`ISmuTuningTransport`: PPT/TDC/EDC limits only — Curve Optimizer deliberately unmodelled; no voltage anywhere), `AmdSmuTuningAdapter` with a **double gate** (qualification witness AND acknowledged arm; unqualified → Blocked with the gate-doc rationale; writes throw), `CpuTuneBootSentinel` (journal durable-on-disk BEFORE any SMU command; settled only after read-back verify; failed write settles to avoid wedging; failed verify keeps it pending; service-start recovery restores stock via a transport or retains evidence without one — wired into `PCHelperRuntime.InitializeAsync` with a null transport). `SetCpuTuningArmed` IPC + `cpu-tuning-arm`/`cpu-tuning-disarm` CLI refuse with `CPU_TUNING_QUALIFICATION_REQUIRED`. **No SMU write transport exists in the repository**; the adapter is exercised only against in-memory fakes. 14 tests incl. journal-before-write ordering and restart-recovery.
- **AMD ADLX feasibility detector** (`amd.adlx`, presence-only check of amdadlx64.dll/ADLXInitialize, never initialises ADLX, silent + healthy fail-safe on this NVIDIA rig), registered beside the IGCL detector in service/CLI/App local probes. 1 test.
- **Adapter-pack SDK published**: docs/adapter-pack-sdk.md documents the shipped verifier exactly (64 MiB/128 MiB/256-entry limits, manifest schema v1, payload-hash allowlist, Ed25519-over-manifest, dev-trust via `PCHELPER_DEV_PACK_HASHES` DEBUG-only); docs/sdk-template/ + scripts/New-AdapterPack.ps1 build an unsigned .pcha. **Live-verified** against the deployed beta5 service: the template pack passes every structural check and fails only signature/trust, exactly as documented.
- **Community qualification drafts**: `QualificationRecordDraftBuilder` (PCHelper.Core) + `pchelper-cli qualification-draft`. Honesty invariants pinned by 23 tests: SignedProductionBuild hard-coded false (a draft can never advance the 18-system gate — asserted via QualificationMatrix), refuses without all four witnessed attestations (live-verified refusal, exit 3), refuses unclassifiable CPU/GPU families instead of guessing, SystemId is a truncated SHA-256 of hardware identity only (no machine/user names).
- **Portable mode**: `--portable` app switch → no service pipe attempt, no user-agent server, read-only local probe, "Portable (read-only)" data source + portable-specific safety copy. Snapshot host gained a `portable` arg; overview page rendered to `artifacts\ui-snapshots-portable` showing writes locked and 332 live sensors.
- **Localization scaffolding**: `Localization/Strings.resx` (+ German satellite), `L10n.Get`/`ApplyCulture`/`{loc:Loc}`, `--culture` startup arg, per-key English fallback, missing keys render `[Key]`; portable-mode strings extracted as the first set; workflow + top-8 language list in docs/localization.md. 4 tests (neutral, satellite, fallback, invalid culture).
- Validation: full suite **517 tests (307 core + 210 integration), 0 warnings**; payload `0.4.0-alpha-20260716-beta6` published and validated (6 components, protocol 2, Passed). Deployed service remains beta5; deploying beta6 is user-gated as always.

## Verification snapshot: 2026-07-16 (SMU update retry hardening; beta7 deployed)

- **SMU mailbox-contention retry implemented.** `RyzenSmuFeasibilityReader.ReadWithSession` now retries `ioctl_update_pm_table` exactly once after a 150 ms pause on `PawnIoException` (the live 0x8007054F transient from the 2026-07-16 beta5 snapshot); a second failure is reported, never retried again. The read-class-only function invariant is unchanged. Two new tests: a transient failure recovers on the bounded retry and decodes real limits; a persistent failure stops after exactly two update attempts and fails safe. Full suite **519 tests (307 core + 212 integration), 0 warnings**.
- **`0.4.0-alpha-20260716-beta7` published, validated (6 components, protocol 2), and user-deployed via UAC** (`Update-LocalAlphaRuntime.ps1`; restore-then-install contract, `RestoredExistingLocalAlpha=True` from the beta5 image). It is beta6's content (the Phase-4/0.9 no-hardware line: inert CPU PBO scaffolding, ADLX detector, adapter-pack SDK, qualification drafts, portable mode, localization) plus the retry fix; beta6 itself was never deployed.
- Verified live read-only after the deploy: `runtime-preflight` Ready (client+service beta7, protocol 2, `canUseServiceWrites=true`); service healthy, only Verified controls writable. `ryzen-smu-feasibility` Succeeded on the first invocation through the new build: SMU firmware 56.76.0, PM table 0x380905, PPT 63.48/160 W, TDC 25.75/110 A, EDC 139.57/160 A, THM 51.43/90 °C — read-only qualification evidence only. All four GPU controls remain **ReadOnly while disarmed** (`gpuclock.core:0`, `gpuclock.memory:0`, `gpufan.duty:0`, `gpupower.limit:0`); `amd.zen.tuning` remains **Blocked**. No hardware write, arm, pulse, or takeover was issued.
- Remaining user gates are unchanged: witnessed live passes (Kraken read with CAM closed, arm-gated GPU fan/power/clock writes with disarm-to-defaults verification, test recording, RTSS suite — RTSS still not installed), 24 h soak, and the signing decision.

## Verification snapshot: 2026-07-16 (repo public; PresentMon, file sensors, Battle.net, nav localization)

- **The repository is public: https://github.com/NiccTM/rigpilot** (user-directed, SignPath-track prerequisite). A pre-publication scan of all tracked files found no personal names, e-mail addresses, hostnames, secrets, or serials; the qualification ledger carries hardware-family identity only, by design. The full beta line (beta2→beta7) was committed and merged as PR #11.
- **Intel PresentMon per-frame benchmark (user-session, optional external tool).** `PresentMonBenchmarkRecorder` implements the existing `IFrametimeBenchmarkRecorder` interface over the separately-installed MIT-licensed PresentMon console (never bundled or downloaded; discovery via `%ProgramFiles%\Intel\PresentMon`, PATH, or `PCHELPER_PRESENTMON_PATH`). One bounded passive ETW capture per session (PresentMon `--timed` plus a local watchdog), header-driven CSV parsing (1.x `msBetweenPresents` and 2.x `FrameTime`), and **true per-frame statistics** — the low figures are the mean rates of the slowest 1% / 0.1% of individual frames, complementing the honestly-labelled RTSS window-based benchmark. New user-agent IPC `StartPresentMonBenchmark` / `StopPresentMonBenchmark` / `GetPresentMonBenchmarkStatus` (service rejects them as wrong-execution-context), feature flag `presentmon-benchmark`, Games & tools card controls (`Games.StartPresentMonBenchmark` / `Games.StopPresentMonBenchmark` / `Games.PresentMonBenchmarkStatus`), THIRD_PARTY_NOTICES entry. 10 tests: per-frame math, both CSV header generations, malformed-row/pre-header refusals, dominant-process and explicit-pid selection, no-frames failure, missing-binary fail-safe without entering Running, invalid/concurrent rejection, and user-agent routing. No live PresentMon run yet (the tool is not installed); queued with the witnessed passes.
- **File-backed sensor inputs (service, read-only, Fan Control parity).** `FileSensorAdapter` reads operator-declared numeric sensor files defined in `%ProgramData%\PCHelper\file-sensors.json` (re-read on timestamp change, no restart). Mandatory plausibility bounds and a staleness window gate every reading: stale → `Stale` with a null value (the cooling-graph stale path then commands maximum cooling), implausible/malformed → `Invalid`, missing/oversized/unreadable → `Unavailable`; the adapter **exposes zero capabilities** so nothing can ever be written through it. Registered in the service adapter list behind `TraceableHardwareAdapter`. 10 tests incl. no-capability invariant, stale/implausible/missing degradation, config hot-reload, invalid-definition warnings, and garbage-config fail-safe.
- **Battle.net game scanning.** `GameStoreKind.BattleNet` + `ScanBattleNet`: detects installed games by their NGDP marker files (`.build.info` / `.flavor.info`) with an access-safe, depth- and count-bounded directory walk; skips the launcher's own directory; default dashboard roots added. GOG and Xbox scanning already existed. 2 new tests (marker detection incl. launcher/plain-folder exclusion; no-executable warning).
- **Localization bulk slice 1:** the nine navigation titles extracted to `Nav_*` keys (`{loc:Loc}` in MainWindow.xaml) with German satellite translations; covered by a test asserting neutral+German resolution and no missing keys.
- Validation: Debug build 0 warnings/0 errors; full suite **542 tests (309 core + 233 integration)** passed; all nine pages rendered to `artifacts\ui-snapshots-open-source-line` and the Games & tools render was visually reviewed (the offline user-agent banner is expected in the isolated render host). The deployed service remains beta7; none of this pass's code is deployed.

## Verification snapshot: 2026-07-16 (Simple-mode UI polish)

- Live-render review of all nine pages found the Simple-mode Cooling and Performance pages mostly empty (two status sentences and dead space) while 450 live sensors existed. Both pages gained a curated **read-only live-readings card** using a new shared `SensorRowTemplate` resource (the Overview "Important sensors" row template, extracted rather than duplicated): Cooling shows fan/pump tachometers, commanded duties, and liquid temperatures (`Cooling.LiveReadings`); Performance shows CPU/GPU utilisation, headline clocks, and power draw (`Performance.LiveReadings`). Card copy states explicitly that nothing on the page writes hardware.
- Two data-correctness fixes came out of render review: the curated-pool dedupe now keys on unit as well as device+name (a fan tachometer in RPM and its duty control in % legitimately share a name — previously the RPM rows vanished), and the Performance card filters to CPU/GPU devices (a storage "Total Activity 0 %" sensor had leaked in).
- The Lighting static-colour field gained a live **colour preview swatch** (`Lighting.OpenRgbColourPreview`) via a new fail-safe `HexColourToBrushConverter` (unparsable text renders transparent, never throws).
- Validation: build 0 warnings/0 errors; full suite 542 tests passed; all nine pages re-rendered to `artifacts\ui-snapshots-polish2` and the Cooling, Performance, and Lighting renders were visually reviewed (live Nuvoton RPM + duty rows, CPU/GPU-only performance rows, and the #4EA1FF swatch confirmed). Presentation-only change: no IPC, capability, safety, or persistence path was touched.

## Verification snapshot: 2026-07-16 (Apple-inspired theme refresh)

- The design tokens in `App.xaml` moved to an Apple-dark-mode-inspired language, keeping the token structure and every automation ID/name intact: chroma-free graphite neutrals (canvas #0E0E10 → raised surface #35353D) instead of the blue-navy set; **alpha-white hairline borders** (#1F/#3DFFFFFF) so elevation reads from surface lightness rather than outlines; the iOS system accent #0A84FF with **white-on-blue** primary buttons (previously dark-on-light-blue; the hardcoded Refresh-button and checkbox-mark foregrounds updated to match); iOS dark status colours (safe #30D158, warning #FFB340, critical #FF6961, soft variants as 16% alpha washes); larger continuous-feel radii (cards 16, buttons/inputs/list items 10, popups 12); Bold 30 px page titles; quieter default buttons (hairline border, no accent hover outline) and subtler alpha scrollbar thumbs. The RigPilot mark's blue was aligned to the new accent.
- Contrast spot-checks: muted text #A8A8B0 on card surface ≈ 6.5:1; warning #FFB340 and critical #FF6961 were chosen over the raw iOS values for legibility as dark-background text. Colour still never carries status without a text label.
- Validation: build 0 warnings/0 errors; full suite 542 tests passed; all nine pages rendered to `artifacts\ui-snapshots-apple` and Overview, Profiles, and Cooling were visually reviewed (blue primary actions, graphite cards, hairline rows confirmed). Presentation-only: no IPC, capability, safety, or persistence path was touched.

## Verification snapshot: 2026-07-16 (Advanced Lab streamlining + UI review pass)

- Full-UI review: all nine pages rendered in Advanced Lab mode against the live beta7 service (user-deployed; diagnostics show the new `amd.adlx`/`intel.igcl` adapters healthy and the clock-offset transports reporting valid ranges).
- **Capability cards streamlined**: the shared `CapabilityTemplate` (Cooling list, Performance list, device decision matrix) now shows name + state badge + domain/range/evidence/risk chips, with the full evidence rationale and next-safe-step moved into a collapsed "Why, and the next safe step" expander. Wording unchanged, only collapsed — roughly twice as many capabilities fit per screen. Reason/NextSafeStep carry no automation IDs, so the accessibility surface is unaffected; the full UI automation smoke passes (all 9 pages, simple/advanced toggles, required automation IDs, accessible names).
- Experimental Control Center intro deduplicated ("Explanatory only — nothing on this panel sends a hardware command."); the session acknowledgement checkbox wording is safety text and was deliberately left verbatim.
- Navigation localization confirmed wired: nav labels resolve `{loc:Loc Nav_*}` from Strings.resx with the German satellite covering all nine (`Nav_Overview`…`Nav_Diagnostics`); localization tests cover neutral + de resolution.
- `docs/next-steps-prompt.md` rewritten as a tighter, self-contained continuation prompt (current beta7 state, five ordered phases, verbatim standing rules).
- Validation: suite **542 tests (309 core + 233 integration), 0 warnings**; UI smoke exit 0; payload `0.4.0-alpha-20260716-beta8` published and validated (6 components, protocol 2, Passed). A running dev-build dashboard instance was closed to unblock the build (relaunch is safe).

## Verification snapshot: 2026-07-16 (first witnessed live GPU write via the new slider UI)

- Beta12 deployed (owner UAC; runtime-preflight Ready, dashboard+service beta12/protocol 2). New in this line: the persisted "Hardware control" master switch (auto-arms GPU fan/power/clock families with the required confirmations on every connect), live GPU control sliders on the Performance page (one-action transactional profiles: bounds clamp, apply, read-back verify, rollback), the capability-card expander with per-capability persisted state, and the Install-LocalAlphaRuntime.ps1 fix for the transient SQLite `-shm` backup race.
- Fixed en route: Apply buttons were dead because the command's can-execute was never re-queried after the master switch or connection state changed (AsyncCommand needs explicit RaiseCanExecuteChanged).
- **Witnessed live pass (owner at the machine, screenshot evidence): GPU power limit applied and read-back verified at 259 W on the reference RTX 3090** via the slider — the first real arm-gated GPU write of the Phase 1 witnessed-pass list. Fan-duty, clock-offset writes, and the disarm-restores-defaults check remain to be witnessed.
- Suite 543 tests, 0 warnings; UI smoke green through every change.

## Verification snapshot: 2026-07-16 (design-language theme pass; beta13/14 deployed)

- beta13 (deployed): On/Off labels restricted to genuinely armable controls (`gpufan.`/`gpupower.`/`gpuclock.` prefixes); informational read-only cards (e.g. NVML per-fan telemetry) say "Read Only" again so they do not masquerade as switches. Live check confirmed `gpufan.duty:0` Experimental (armed) while the NVML per-fan cards stay ReadOnly — the fan slider is the write path.
- beta14 (deployed, live): theme rebuilt on three researched design languages — Material 3 tonal elevation (surfaces tint toward the blue primary per step: canvas #0C0D11 → surface #1A1C23 → raised #232732; M3 shape scale 18/12), Apple Liquid Glass card material (top-lit vertical gradient + specular top-edge border gradient + soft depth shadow via `CardBackgroundBrush`/`CardBorderBrush` in App.xaml), One UI pill buttons (18px radius) and looser padding. Colour still never carries status without a text label.
- Validation each step: 543 tests, 0 warnings, UI automation smoke green; `runtime-preflight` Ready on beta14 after user-approved UAC deploys.

## Verification snapshot: 2026-07-16 (all four GPU write families live-verified; operator-authorised self-test)

- **Operator-authorised live write test (explicit user instruction to test writes directly): all four armed GPU write families PASSED with hardware read-back on the reference RTX 3090** via the real service pipe and transaction engine (one-action ApplyProfileV2 each): fan duty 62 % ("verified within 5% of 62%"), power limit 300 W ("verified at 300 W"), core clock offset +15 MHz, memory clock offset +15 MHz. Harness: scratchpad console app over PCHelper.Ipc/Contracts; no bounds bypassed — every write went through prepare → clamp → apply → read-back verify.
- **Disarm-restores-defaults contract witnessed live**: `gpu-fan-disarm`/`gpu-power-disarm`/`gpu-clock-disarm` each confirmed restoration (auto fan curve, vendor default power limit, stock clocks). With the earlier 259 W slider apply, the Phase-1 "arm-gated GPU write" witnessed passes are now complete for fan/power/core/memory + restore.
- Dashboard fix (user report, beta15 pending deploy): NVML's informational read-only cards (per-fan telemetry, fan write-transport feasibility, power-limit bounds) are hidden from the Cooling/Performance lists whenever the corresponding writable channel exists — they duplicated working controls as "Read Only" rows. Full evidence remains in the Devices decision matrix. Cooling list went 13 → 10 entries, all actionable. The "Off while armed" moment in the user's screenshot was the one-second snapshot-refresh lag at arm time, confirmed against live CLI state (all four families Experimental).
- Validation: 543 tests, 0 warnings, UI smoke green; beta15 published/validated, awaiting the owner's UAC to deploy.

## Verification snapshot: 2026-07-16 (Simple-mode control parity; every control live-verified incl. case fan)

- **Fan controls card added to the Cooling page in BOTH Simple and Advanced modes**: every writable bounded motherboard fan output (`lhm.control:` Cooling, Experimental/Verified, numeric range) gets the same one-action transactional slider as the GPU families; LHM's duplicate GPU-fan path is excluded (the NVML `gpufan.duty` slider owns the GPU cooler). The GPU controls card on Performance is likewise visible in both modes. Engine-side floors, pump/CPU-fan protections, and read-back are unchanged.
- **Operator-authorised live test, second pass — ALL controls verified on real hardware**: GPU fan 62 % / power 300 W / core +15 MHz / memory +15 MHz (all PASS with read-back) plus **Case Fan #1 to 60 % and back to 100 % via the LibreHardwareMonitor path (software-control read-back matched)**. One client-side pipe timeout occurred on rapid back-to-back applies to the same LHM channel (second apply succeeded standalone) — noted as a possible client-timeout hardening item, not a service defect. GPU vendor defaults restored via disarm afterwards.
- Validation: 543 tests, 0 warnings, UI smoke green; Simple-mode Cooling render shows 9 actionable fan outputs. Payload `beta16` published/validated, awaiting owner UAC.

## Verification snapshot: 2026-07-16 (beta17: one-click GPU Auto OC + curve/calibration entry points)

- **Auto OC**: `StartGpuAutoOcCommand` on the always-visible GPU controls card drives the existing bounded tuning engine against `gpuclock.core:` — Performance objective, 83 °C ceiling, 10-minute screening, thermal/WHEA/display-reset aborts, rollback, boot sentinel; the Hardware-control switch supplies both acknowledgements. Result language stays "Passed 10-minute screening", never "stable". Abort + live operation status shown on the same card in both UI modes.
- **Fan curves & calibration**: the Fan controls card gained a "Set up fan curves & calibration" button (Simple mode) that opens the existing Advanced Lab curve studio/calibration wizard in place; no engine changes.
- Validation: 543 tests, 0 warnings, UI smoke green; beta16 then beta17 deployed via owner UAC (`runtime-preflight` Ready on beta17). The live Auto OC screening run is the user's to witness — it must not be fabricated on their behalf.

## Verification snapshot: 2026-07-16 (OpenRGB installed by user instruction; lighting writes live via the bridge)

- At the owner's explicit instruction, OpenRGB 0.9 (official openrgb.org release zip, 11.3 MB, GPL-2.0) was downloaded and extracted to `%LOCALAPPDATA%\Programs\OpenRGB` and its SDK server started (`--server --startminimized`, loopback 6742). **Not bundled** — GPL-2.0 vs GPL-3.0 incompatibility means it stays a separate user-installed app, exactly as the designed bridge contract requires. Note: first launch exited immediately (first-run behaviour); second launch is stable.
- **RigPilot's own `OpenRgbSdkClient` verified end-to-end live**: connected (protocol 4) and enumerated 4 controllers — Lian Li O11 Dynamic Razer Edition (64 LEDs), NZXT Kraken X3 (9), ASUS ROG STRIX X570-E GAMING Aura (8), EVision keyboard (126) — i.e. every device the RGB routing page listed as read-only/setup-needed. **Live write PASS: static #0A84FF at 100% applied to all 4 controllers** through the bridge path the Lighting page uses.
- OpenRGB is not auto-started at login (persistent-startup config needs owner consent); the Lighting page bridge toggle + Connect is the in-app route. "Better than OpenRGB" remains the native-adapter roadmap (Aura first) + signed pack ecosystem.

## Verification snapshot: 2026-07-16 (beta19: improvement sweep — Auto OC search clamp, OpenRGB auto-launch, fan slider live-init, temp curation)

- Discovered mid-sweep that the service had rolled back to the original Program Files 0.3 image (a declined UAC left beta18 undeployed) — the "service older than dashboard" lock in the user's screenshots was version mismatch, not a code regression. beta19 deploy restored a matched LocalAlpha runtime (runtime-preflight Ready).
- **Auto OC**: Performance-objective tunes on `gpuclock.*` clamp the search floor to 0 (stock) — the engine previously searched the full driver delta range (−1000..+1000 MHz), wasting screening time on underclocks.
- **RGB**: `EnsureOpenRgbServerRunningAsync` — the Lighting Connect action now auto-launches an installed OpenRGB (`%LOCALAPPDATA%\Programs\OpenRGB` or Program Files) in `--server --startminimized` mode when no server is running; on-demand only, nothing persistent, absent install falls through to the normal failure message.
- **Fans**: Cooling-page fan sliders initialise from the fan's live duty sensor (matched by control path suffix, clamped to bounds) instead of defaulting to 100 %.
- **Temps**: curated-sensor ranking now also promotes hotspot (both spellings) and liquid/coolant temperatures — the paste-mount delta and the AIO's true load signal.
- Validation: 543 tests, 0 warnings, UI smoke green; beta19 deployed via owner UAC and the dashboard relaunched on the matched image.

## Verification snapshot: 2026-07-16 (controls line: automatic cooling modes, native Kraken RGB, colourways, UX)

- **Owner policy amendment #2 (user-directed): Hardware control defaults ON** when no preference file exists; an explicit opt-out persists. Arming still records the Experimental + exact-device confirmations on every connect. Separately, the owner asked to force every control to Verified/Experimental and bypass this file's rules — that was **declined** (capability labels are evidence claims; rule 13 is not owner-waivable because forcing a label would enable writes with no read-back/reset evidence). The agreed substitute was making more controls genuinely usable, below.
- **Automatic cooling modes (one-click temp curves).** `AdaptiveCoolingProfileFactory.CreateAutomaticMode`: multi-output conservative curve — every uncalibrated output floors at the deterministic **50% safety floor**, always reaches the controller maximum at 85 °C, GPU mode binds to GPU temperature, case-fan mode to max(CPU, GPU). Service `ValidateCoolingGraphQualificationAsync` gained the matching conservative route: an output with no physically-observed commissioning session is accepted **only** at ≥50% floor + full maximum; anything lower still requires commissioning + calibration. Pump/CPU-fan role blocks, bounds/reset checks, and the stale→maximum-cooling runtime are unchanged. Dashboard buttons: `Cooling.EnableCaseFansAutoMode`, `Performance.EnableGpuFanAutoMode`. 5 factory tests.
- **Native Kraken X3 RGB control (RigPilot's own adapter).** `KrakenX3LightingWriter` derives the fixed/off lighting report (opcode 0x2A 0x04, sync channel ring+logo, speed table, footer static value) from liquidctl's kraken3 driver (re-reviewed 2026-07-16, attributed; no code copied). Lighting registers only — pump/cooling untouched; no firmware read-back exists, so results say "write issued; confirm visually", never verified. Runs in a crash-contained Adapter Host child (`--set-kraken-rgb`), `ContainedKrakenLighting` maps every failure to a non-issued outcome, service IPC `SetKrakenLighting` refuses without ConfirmExperimental + exact device id `nzxt:kraken-x3`, CLI `kraken-rgb`, dashboard buttons on Lighting. An exclusively-held device (CAM/OpenRGB) is a designed refusal. 7 tests (byte-exact report construction, confirmation gate, containment).
- **RGB colourways over the OpenRGB bridge**: deterministic static per-LED frames — Rainbow, Puddles, Sunset, Ocean, Lava, Static — computed by `LightingColourways` and written through the existing custom-mode path (`SetColourwayAsync`); preset names follow the community effect vocabulary (OpenRGB Effects/SignalRGB). Lighting page gained the colourway picker.
- **UX pass**: Cooling/Performance evidence banners now state the transactional write contract with a state-driven badge (amber "TURN ON HARDWARE CONTROL" → green "CONTROLS LIVE"); monitor brightness is a slider with a live % readout (explicit confirmation + read-back contract unchanged); `AdapterHostControllerDiscoveryProcess` accepts multi-argument child modes.
- Validation: build 0 warnings/0 errors; full suite **559 tests (314 core + 245 integration)**; all nine pages rendered to `artifacts\ui-snapshots-controls-line`, Lighting visually reviewed.
- **beta21 deployed (owner UAC; runtime-preflight Ready, client+service beta21) and the native Kraken write verified live:** first attempt returned the designed `AccessDenied` refusal while NZXT CAM held the HID stream; after the operator-authorised CAM closure (precedented conflict clearance), `kraken-rgb --colour 0A84FF` returned **WriteIssued** on the physical Kraken ("NZXT USB Device") — visual confirmation requested from the owner. The live confirmation gate was also exercised: the same command without `--confirm-experimental` was refused with `KRAKEN_LIGHTING_NOT_CONFIRMED` before any child process ran. Kraken telemetry has no CLI command (dashboard Devices page only); the CAM-closed telemetry pass remains one dashboard click. Live automatic-mode activation remains for the owner to witness in the dashboard.

## Verification snapshot: 2026-07-16 (close-blockers: terminate detected competing controllers)

- **"Close blockers" — owner-requested feature.** New `ConflictProcessTerminator` (Adapters) terminates the running processes of detected conflicting controllers so they release the device handles that block RigPilot's gated writes. Hard safety boundaries, pinned by 6 tests: it terminates **only** processes on `ConflictDetector`'s curated known-controller allowlist (never an arbitrary name), requires explicit `Confirm`, and never terminates a RigPilot process (self-protection). It is deliberately **distinct from the identity-verified hardware-takeover executor** — it takes over no hardware control, acquires no consent/lease, and needs no Authenticode gate; it is exactly equivalent to the operator closing the app in Task Manager (relaunch hands control back). Rule 7's takeover gate is unchanged; this is a separate user-initiated process-termination convenience.
- Allowlist extended with the suites the owner named: `rgb-fusion` (Gigabyte RGB Fusion / AORUS Engine / Gigabyte Control Center) and `mystic-light` (MSI Center / Dragon Center), alongside the existing Afterburner/CAM/Armoury Crate/AI Suite/Fan Control/SignalRGB/OpenRGB/iCUE/L-Connect entries.
- Plumbing: service IPC `StopConflictingProcesses` (LocalSystem terminates the elevated controllers) with an injectable `IProcessControl` seam; CLI `close-blockers --confirm [--id]`; Diagnostics "Warnings and conflicts" card gained an acknowledge checkbox + "Close N blocking apps" button (`Diagnostics.CloseBlockers`), and the old "never terminates another program" copy was corrected. `WindowsProcessControl` kills the whole process tree (helper-heavy suites like CAM) and skips inaccessible/exited processes.
- Validation: build 0 warnings/0 errors; full suite **565 tests (314 core + 251 integration)**; all nine pages rendered to `artifacts\ui-snapshots-close-blockers`, Diagnostics visually reviewed (acknowledge + "Close 2 blocking apps" shown against live FanControl/Afterburner conflicts).
- **beta22 deployed (owner UAC; runtime-preflight Ready) and close-blockers verified live:** `close-blockers` without `--confirm` was refused (`requires explicit confirmation`); with `--confirm` it terminated the two running blockers (MSIAfterburner pid 408, FanControl pid 21360), both `terminated=true`. Immediately afterward the fan controls flipped from Blocked to **9 Experimental + 1 ReadOnly** on the live service — i.e. clearing the blockers freed RigPilot's own gated write paths exactly as intended. No hardware was taken over by the action itself.

## Verification snapshot: 2026-07-16 (NZXT AIO pump control live: liquidctl-derived, floor 60%, READ-BACK VERIFIED on real hardware)

- **Kraken X3 pump-duty control shipped and live-verified** (deployed beta22). liquidctl-derived speed protocol (attributed; no code copied): 64-byte report `[0x72, 0x01, 0x00, 0x00]` + 40 flat duty bytes (critical temps 20–59 °C). Full stack mirrors the lighting pass: `KrakenX3PumpWriter` → Adapter Host `--set-kraken-pump` child → `ContainedKrakenPump` → IPC `SetKrakenPumpDuty` (Experimental + exact-device confirmed) → CLI `kraken-pump` → Cooling-page pump slider (both UI modes, 60–100 %).
- **Safety invariants at every layer**: duty hard-clamped to [60, 100] % in the request validator, the writer, AND the report builder — the pump can be slowed for noise, never below RigPilot's floor, never stopped. Read-back comes from the firmware's own status stream (byte 19 duty / 17–18 RPM) after the liquidctl-documented interval command `[0x70, 0x02, 0x01, 0xB8, 0x01]` (without it a fresh device never streams — discovered live). Exclusively-held devices are designed refusals.
- **Live evidence on the reference Kraken X3: `ReadBackVerified` — requested 80 %, firmware reported 80 % at 2343 RPM.** This is also the first live proof of the whole Kraken status-stream parser. Two live bugs fixed en route: output-buffer/report-ID sizing must follow the lighting writer's max-report-length contract (fixed IOException), and the status-interval init above. Restoration: NZXT CAM took the device back and reasserted the user's own curve (designed handoff); OpenRGB and CAM both holding the HID stream produce the AccessDenied refusal, never force.
- 12 new tests (byte-exact report pin incl. flat 40-point profile, floor clamps at builder and request layers, confirmation gate, contained failure mapping). Suite 581 tests (314 core + 267 integration), 0 warnings, UI smoke green; one flaky integration failure during the beta22 run did not reproduce on two re-runs (noted, not chased).

## Verification snapshot: 2026-07-16 (Apple sliders, native Aura lighting wired, Razer Chroma official-SDK client)

- **iOS-style Slider template** (App.xaml): thin rounded track, accent-filled left side, round white thumb with soft drop shadow, 45% disabled opacity. Applies app-wide (GPU/fan/pump sliders).
- **Native AURA lighting fully wired end-to-end**: `AuraUsbLightingWriter` (clean-room documented AURA addressable USB protocol, 65-byte 0xEC direct frames, VID 0x0B05/PID 0x18F3) → Adapter Host `--set-aura-rgb` → `ContainedAuraLighting` → IPC `SetAuraLighting` (Experimental + exact device `asus:aura-usb`) → Lighting-page "Apply to Aura board / off" buttons. **Live-verified earlier via the host child: WriteIssued on the real "AURA LED Controller".** No EEPROM/save; lighting registers only; contained.
- **Razer Chroma — official documented REST SDK** (`ChromaRestClient`, http://localhost:54235/razer/chromasdk): the LEGAL native path for the Lian Li O11 Dynamic Razer Edition (enumerates as a Chroma ChromaLink device) plus any Razer keyboard/mouse/headset. POST app-info → session, PUT CHROMA_STATIC (BGR colour, brightness-scaled) to all six device categories, DELETE to release. User-session (localhost REST, like the OpenRGB bridge). "Apply to Razer Chroma" button on the Lighting page. Runs only while Razer Synapse + Chroma Connect is installed (absent on this PC today → clean failure message). 5 tests pin BGR packing, brightness scale, malformed-colour rejection, snake_case app-info payload, and the no-server path.
- **Zotac Spectra GPU RGB: deliberately NOT natively written.** It is undocumented I2C built into the GPU (Zotac ships only FireStorm); blind GPU-bus writes can hit VRM/EDID addresses. Under the documented-vendor-APIs-only rule it stays on the OpenRGB bridge / read-only. Recorded, not worked around.
- Validation: 589 tests (314 core + 275 integration), 0 warnings, UI smoke green. beta24 published/validated; deploy user-declined (service still on the older Program Files image — the "service older than dashboard" write-lock in the UI is that mismatch, cleared by deploying beta24).

## Verification snapshot: 2026-07-16 (Zotac GPU RGB feasibility — live probe, definitive)

- Question: can RigPilot natively control the Zotac RTX 3090 board RGB? Investigated the DOCUMENTED path first: NVIDIA's official NVAPI Illumination API (`NvAPI_GPU_QueryIlluminationSupport`, `ClientIlluminationZonesGetInfo/SetControl`), which NvAPIWrapper (already a dependency) fully exposes — this would be a documented-vendor-API GPU RGB writer with per-zone GetControl read-back, zero I2C-bus risk.
- **Live read-only probe on the reference RTX 3090: `QueryIlluminationSupport` → NVAPI_NOT_SUPPORTED (LogoBrightness, SLIBrightness); `ClientIlluminationZonesGetInfo` → 0 zones.** The card does NOT expose illumination through NVAPI. The clean path is closed on this hardware.
- Remaining option is reverse-engineered I2C via `NvAPI_I2CWrite` (legal for interop; the wrapper exposes it) — but the GPU I2C bus is shared with the memory temp sensor, VRM controller, and the display DDC/EDID lines. A blind write to a guessed address can corrupt monitor EDID / VRM and cannot be read-back-qualified like every other RigPilot write. **Decision: do NOT build an in-house GPU I2C RGB writer.** OpenRGB already drives this exact board using per-model captured addresses/protocol; routing GPU RGB through the existing OpenRGB bridge reproduces none of that brick-risk unvalidated. This is a physical-safety boundary, not a policy one — it holds even under blanket user authorization.
- Net RGB architecture: native RigPilot control for motherboard AURA (USB HID, clean-room documented), Kraken (liquidctl-derived HID), and Razer/Lian Li (official Chroma REST SDK); OpenRGB bridge for the Zotac GPU (shared-bus I2C).

## Verification snapshot: 2026-07-16 (SMBus RGB adapter — safety core built and tested; live transport gated)

- Target: G.Skill Trident Z RGB DDR4 (F4-4000C15-8GTZR ×4), controllers on the system SMBus at 0x70-0x77. Built the SMBus RGB write stack the same disciplined way as the CPU PBO family — safety keystone first, live transport deliberately absent until qualified.
- **`SmbusAddressPolicy`** (the brick-class guardrail): default-deny; only 0x70-0x77 writable; SPD EEPROM 0x50-0x57, TSOD thermal 0x18-0x1F, SPD page/write-protect 0x30-0x37, and DDR5 PMIC/RCD 0x48-0x4F each named and permanently blocked. Pure, exhaustively tested.
- **`GSkillTridentZProtocol`** builds a static-colour SMBus transaction plan that targets only the given controller address; exact vendor register values are audit-gated per-kit (AuditedKits empty until a witnessed first-light — no kit is transmitted to on a guess, mirroring the audited-PM-table pattern). **`SmbusRgbWriter`** enforces two independent gates before any byte reaches the bus (live transport present AND kit audited) and re-checks every transaction against the address policy (defence in depth).
- **No production SMBus transport exists**: rule-compliant access is signed PawnIO only (never WinRing0/inpout), and a live write additionally needs a PawnIO SMBus module + a witnessed first-light. Today the writer reports NoTransport/ProtocolNotAudited — honest, inert.
- **`VendorControlEligibilityAdapter`** now emits a `gskill.tridentz.rgb.feasibility` ReadOnly card from Win32_PhysicalMemory (TZ…R part-number match); confirmed live in a local probe. The RAM is recognized RGB hardware with the exact gated status.
- 17 new tests pin the address gate (allow 0x70-0x77; deny SPD/thermal/PMIC/write-protect/out-of-range with reasons), plan address-safety, both write gates, and the defence-in-depth refusal. Suite 606 tests (314 core + 292 integration), 0 warnings.
- Remaining (gated, user/hardware): a signed PawnIO SMBus module, the exact Trident Z register-map capture, a witnessed first-light, then IPC/CLI/UI wiring — same shape as the Aura/Kraken native writers. Not blind-written to running RAM.

## Verification snapshot: 2026-07-17 (SMBus RGB — live PawnIO transport built; read-only bus probe ran; ENE default-address evidence; first-light staged)

- **Transport is real now**: `PawnIoSmbusTransport` drives the signed PawnIO `SmbusPIIX4` module (AMD FCH is PIIX4-compatible) via `ioctl_smbus_xfer` `[address, read/write, command, protocol(2=byte,3=word), data]`. The module blob is extracted at runtime from LibreHardwareMonitorLib 0.9.6's embedded resources (`LibreHardwareMonitor.Resources.PawnIo.SmbusPIIX4.bin`) — no new module distribution. The PIIX4 module has **no address filtering of its own**, so the transport is a policy chokepoint: every write calls `SmbusAddressPolicy.EnsureWritable`, and reads are refused outside 0x70-0x77.
- **Protocol corrected to the documented ENE map** (OpenRGB Aura-Controller-Registers — register facts only, no vendor code): 16-bit register space through SMBus cmd 0x00 (byte-swapped pointer word) + cmd 0x01 (data, auto-increment). Direct colours 0x8000 as **R,B,G** ×5 LEDs, direct-enable 0x8020=1, mode 0x8021, apply 0x80A0=1. `GSkillTridentZProtocol` renamed `EneSmbusRgbProtocol`; static-colour plan = 20 transactions. AuditedKits still empty.
- **Live read-only probe ran on the reference rig** (elevated adapter-host `--probe-smbus-rgb`; read-byte only, 0x70-0x77 only): 0x70-0x75 no ACK; **0x77 answers the full ENE detection window 0xA0-0xBF with a clean incrementing sequence** — 0x77 is the documented factory-default address for unconfigured ENE DRAM controllers, so the four Trident Z sticks are almost certainly all listening unmapped at the default (identical firmware answers in unison). 0x76 ACKs with unrelated data (unknown device — never write to it). Probe JSON archived in session scratchpad.
- **Operator-run entry points staged** (adapter-host args; never run automatically): `--identify-smbus-rgb` (one pointer write to name register 0x1000 + 16 byte reads; no colour/mode/apply touched) and `--first-light-smbus-rgb RRGGBB` (probes first, transmits only to pattern-matched addresses, `witnessedFirstLight: true` bypasses ONLY the audit gate — the address policy is enforced on every transaction regardless). Neither has been run; both await the operator watching the DIMMs.
- Tests updated/added: ENE plan sequence + byte-swap + RBG order, PIIX4 ioctl encoding, transport range refusals (reads and writes), read-only detection parsing, first-light gate limits (bypasses audit only, still cannot reach 0x50), identity-read bus footprint (exactly one pointer write). Suite 612 tests (314 core + 298 integration), 0 warnings.
- Remaining: witnessed identity read → witnessed first-light at 0x77 → add kit to AuditedKits with evidence → per-stick remap study (OpenRGB's remap touches SPD page-select territory — forbidden range; needs its own audit before ever considered) → IPC/CLI/UI wiring.

## Verification snapshot: 2026-07-17 (0.5 line: update check, first-run tour, 0.5.0 MSI)

- **Version bumped to `0.5.0-alpha`** across Directory.Build.props (VersionPrefix 0.5.0), every adapter-manifest version string, CLI/AdapterHost/UserAgent fallbacks, publish/build-gamebar/build-installer/LocalAlpha script defaults, the WiX project ProductVersions, and the release-line tests. `Test-RuntimePayload.ps1` passed for the published `0.5.0-alpha` payload (6 components, protocol 2).
- **In-app update check (Workstream H start; user process only, rule 11 intact).** `GitHubUpdateCheck` performs one bounded anonymous HTTPS read of the public GitHub releases API (`NiccTM/rigpilot/releases/latest`): pure `Evaluate`/`TryParseVersion` separated from HTTP for tests, release links restricted to github.com (anything else falls back to the releases page), nothing downloaded or installed, every failure degrades to an explanatory status line. Quiet once-per-launch check on real startup (never in the snapshot host or portable mode) plus a Diagnostics "Updates" card (`Diagnostics.CheckForUpdates` / `Diagnostics.OpenReleasePage` / `Diagnostics.UpdateStatus`). 13 new tests cover tag shapes, newer/equal/older comparison, the link-host restriction, and malformed-payload fail-safes.
- **First-run onboarding tour.** A three-step modal tour (transactional-write model → the Hardware control master switch → evidence labels and the Devices page) shown once per user: `OnboardingState` persists the completion flag beside the other dashboard preferences; unreadable state fails toward showing the tour again. Only real app startup calls `ShowOnboardingIfFirstRun` — the deterministic snapshot host never sees the overlay, and portable mode skips it. Automation IDs `Onboarding.*`; 3 new tests pin the flag semantics.
- **Localization**: all onboarding and update-check strings extracted to `Strings.resx` with German satellite translations (safety copy needs native review before a localized release, per docs/localization.md). Screen-reader/pointer QA remains an external manual pass — automation IDs and accessible names are in place on all new controls.
- **The 0.5.0 installer is built (not installed):** `artifacts\installer\RigPilot.Installer.msi` (23,658,496 bytes, SHA-256 `12B7B302F7DE75BDBF34534A8EF4DBA52DE617DA50DF56B82736A15DDA7EACFF`, unsigned) plus `RigPilot.Bundle.exe` with the bundled .NET 10 desktop runtime, both from the validated `0.5.0-alpha` payload.
- Validation: build 0 warnings/0 errors; full suite **643 tests (314 core + 329 integration)** passed (one release-line test updated for the bump); all nine pages rendered to `artifacts\ui-snapshots-0.5-line`, Diagnostics visually reviewed. Earlier the same day, `0.4.0-alpha-20260717-beta25` (the SMBus/Kraken-pump/Aura/Chroma line) was UAC-deployed and live-verified Ready. The 0.5.0-alpha LocalAlpha deploy was **declined at UAC** — the service remains beta25, so the relaunched 0.5 dashboard correctly shows the runtime-mismatch write lock until the owner deploys the matching runtime (`Update-LocalAlphaRuntime.ps1 -PayloadRoot artifacts\publish`).

## Verification snapshot: 2026-07-17 (competitive-gap line: undervolt, screen ambience, tray panel, drive health, profile sharing — beta26 deployed)

- **Guided GPU undervolt (documented APIs only).** `UndervoltPresets` computes Quiet (−25%) / Efficient (−15%) / Stock power-limit targets anchored on the driver's vendor default (range-max fallback, always clamped into the driver range); applied through the existing transactional slider path (`ApplyGpuControlAsync` → one-action ApplyProfileV2, read-back verified). A Performance-page "Guided undervolt" card and the tray menu expose the presets. The copy states explicitly this is a power-limit undervolt, NOT a VF-curve editor (per-point VF stays excluded by the documented-APIs rule). 8 tests.
- **Screen-ambient lighting (user-session, explicit start/stop).** `ScreenAmbientSampler` captures the primary display into a 32×18 in-memory thumbnail per 150 ms tick (never written to disk, logged, or transmitted), computes 24 clockwise edge-zone averages, and maps the ring onto any LED count; a persistent `OpenRgbAmbientSession` (custom mode once, then UpdateLeds frames on one loopback connection) drives every OpenRGB controller. Start refuses without a connected bridge or with a lighting conflict; stop and app dispose release the devices. Lighting-page section `Lighting.StartAmbient`/`StopAmbient`. 6 tests pin the zone math.
- **Tray quick-panel (G-Helper-style).** The tray menu gained a live telemetry line (top ImportantSensors), a Hardware-control toggle (same persisted property as the header switch), and the GPU-undervolt submenu — all one click from the tray, all through the same acknowledged transactional paths.
- **Drive health (read-only).** New IPC `GetStorageHealth` (read-only classified): service-side `StorageHealthProbe` queries the Windows Storage provider (MSFT_PhysicalDisk + MSFT_StorageReliabilityCounter, matched by ObjectId identity tail) for identity, media/bus type, OS health status, temperature, wear, and power-on hours; absent counters stay null, implausible temperatures (≤0, >150 °C) report as not-reported. Devices-page "Drive health" section. RigPilot has no storage write path. 5 mapper tests.
- **Profile sharing.** `ProfileShareFile` exports a typed V2 profile as `.rigpilot-profile.json` and imports shared files with forced safety posture: renamed, re-identified, machine-local references (cooling graph / scene / OSD / automation) stripped, `IsBuiltIn=false`, `IsExperimental=true` — so apply always demands the Experimental acknowledgement + exact-device confirmation, and every action is bounds-clamped locally. Profiles-page "Share profiles" card (`Profiles.ExportProfile`/`ImportProfile`). Scripts cannot travel because ProfileV2 cannot carry them (rule 6). 6 tests.
- Validation: build 0 warnings/0 errors; full suite **676 tests (314 core + 362 integration)** passed; all nine pages rendered to `artifacts\ui-snapshots-suite-gaps` and Performance was visually reviewed against the live service. Payload `0.5.0-alpha-20260717-beta26` published, validated (6 components, protocol 2), **UAC-deployed** (restore-then-install from the prior 0.5.0-alpha image), verified live: `runtime-preflight` Ready, client+service beta26, `canUseServiceWrites=true`; dashboard relaunched on the matching build. No hardware write, arm, or takeover was issued during the deploy; the undervolt presets and ambience remain user-triggered.

## Verification snapshot: 2026-07-17 (MainViewModel split phase 2, encoding-regression fix, benchmark history, music-reactive lighting)

- **MainViewModel split phase 2 (mechanical).** Nine more domain partials extracted verbatim from the monolith (Cooling, Lighting, GamesTools, Monitoring, Profiles, Automation, Ownership, Brightness, Benchmarks); `MainViewModel.cs` is now 5,760 lines (10,158 → 8,518 in phase 1 → 5,760). No member edited, only moved. Phase 1 shipped in PR #21 (merged).
- **Encoding regression fixed (self-inflicted, root-caused).** The phase-1/phase-2 split scripts used Windows PowerShell `Get-Content` (ANSI) + `WriteAllLines` (UTF-8), which mojibaked every non-ASCII char in the moved code — single-level in the phase-1 partials already merged to master, double-level in `MainViewModel.cs`. Fixed with a run-by-run iterative Latin1→UTF-8 re-decode that only rewrites a non-ASCII run when the decode is clean (leaves already-correct characters untouched, so the mixed file with post-split UTF-8 additions was safe), plus byte-exact fixes for the few arrow runs adjacent to correct chars. Also cleaned three pre-existing `°C`→`Â°C` corruptions (RuntimeTuneScreeningMonitor.cs, two test files) present on master from an earlier session. Tree-wide sweep confirms zero mojibake. **Lesson: never round-trip source through Windows PowerShell Get-Content/Set-Content for file edits — use the editor tools or explicit UTF-8 stream reads.**
- **Benchmark history (local, bounded).** `BenchmarkHistory` records every completed RTSS/PresentMon run to a per-user JSON file (newest-first, capped at 200, corrupt-file-safe), suppresses the repeated completion-poll duplicates, and computes a per-game/per-source delta ("vs previous vrchat.exe run: avg +5%, 1% low −5%") so tuning changes (undervolt presets, clock offsets) are measurable. Games & tools RTSS section shows the compare line + history list. 6 tests.
- **Music-reactive ambient lighting.** Added NAudio 2.2.1 (MIT; vuln scan clean). `AudioSpectrum` does a windowed FFT over the last 2048 mono samples into 24 log-spaced band energies with slow-decay peak normalisation; `AudioAmbientSource` is WASAPI **loopback** capture of the system's own output audio (never a microphone) into a rolling in-memory ring — nothing recorded to disk or transmitted, only LED colours leave the process. A "Music mode" checkbox on the Lighting page routes the existing persistent OpenRGB ambient session through the spectrum (bass on low LEDs, treble on high) instead of the screen sampler. 7 tests pin the band placement, normalisation, and colour scaling.
- Validation: build 0 warnings/0 errors; full suite **689 tests (314 core + 375 integration)** passed; all nine pages rendered to `artifacts\ui-snapshots-history-audio`, Lighting visually reviewed (em-dashes correct, Music-mode UI present). Deployed service remains 0.5.0-alpha-beta26; none of this pass's code is deployed yet.

## Verification snapshot: 2026-07-17 (GPU Auto OC made "very very good": refined edge-find + safety margin + memory pass)

- **Refined Auto-OC search (the way a careful overclocker works).** `HardwareTuneEngine` gained a coarse→fine→safety-margin flow, gated by two new optional `StartTuneRequest` fields (`RefinementCandidates`, `SafetyMargin`; both default 0 = the prior plain coarse scan, so every existing tune path and test is unchanged). After the coarse ascending scan finds the last stable candidate and the first failing one, it bisects that gap (`GpuAutoOcSearch.FineCandidates`) to locate the stability edge precisely — no longer capped by the coarse step size — then backs the shipped offset off by an absolute safety margin (`GpuAutoOcSearch.ApplyMargin`, direction-aware, snapped to the driver's fine step via `SnapToStep`) so the result carries headroom instead of sitting on the cliff. The margin is recorded honestly in the generated profile description ("A 15 MHz stability margin was applied below the observed limit of 90 MHz"); the label stays "Passed 10-minute screening", never "stable".
- **Memory Auto OC.** New one-click `StartGpuMemoryAutoOcAsync` runs the identical refined search on `gpuclock.memory:` (100 MHz margin vs 15 MHz for core — GDDR6X error scales in bigger increments, and memory is often the larger real-world gain on the 3090). Core uses `StartGpuClockAutoOcAsync` too; both go through the existing bounded engine (Performance objective, 83 °C ceiling, per-candidate + 10-minute final screening, WHEA/thermal/display-reset aborts, rollback, boot sentinel). One bounded capability per run is preserved (core and memory are separate operations); **no voltage is constructed or submitted anywhere** — only clock-offset values move.
- Service `StartTuneAsync` validates the new fields (`RefinementCandidates` 0–20, `SafetyMargin` non-negative finite) and passes them through to the engine. Performance page: "Auto OC (10-minute screen)" split into **"Auto OC core"** + **"Auto OC memory"** (`Performance.AutoOcCore` / `Performance.AutoOcMemory`) with tooltips describing the climb/edge-find/back-off strategy.
- Tests: 9 new — `GpuAutoOcSearch` fine-candidate/margin/snap math, an engine test proving the refined search finds a 90 MHz edge a coarse-20 scan would miss (stops at 80) and ships 85 MHz with the margin, and a control test proving refinement-off ships the plain coarse result unchanged. Full suite **698 tests (323 core + 375 integration), 0 warnings**; all nine pages rendered; live screenshot confirmed both Auto OC buttons. Not deployed — the tuning path needs a user-witnessed live run before any real clock is written; the deployed service remains beta26.

## Verification snapshot: 2026-07-17 (Auto OC even better + fan auto-mode presets)

- **Auto OC — thermal headroom + adaptive near-edge screening.** Two more refinements on top of the coarse→fine→margin search, both gated by defaulted fields so existing tune paths/tests are unchanged. (1) **Thermal-headroom stop** (`GpuAutoOcSearch.ReachedThermalHeadroom`, new `StartTuneRequest.ThermalHeadroomCelsius`): the climb halts once a *stable* candidate's peak temperature reaches within N °C of the ceiling (Auto OC uses 4 °C below 83 °C), so the shipped overclock keeps real cooling headroom, not just stability headroom — going higher would trade cooling away. (2) **Adaptive screening** (`GpuAutoOcSearch.RefinementScreeningTime`): refinement candidates sit right at the edge, so they screen for double the coarse per-candidate time (capped at the 5-minute limit) — more scrutiny where it matters, fast climb where it doesn't. Service validates headroom 0–40 °C. Still no voltage anywhere; one capability per run; WHEA/thermal/display-reset aborts, rollback, boot sentinel unchanged.
- **Fan auto modes — Silent / Balanced / Cooling presets.** New `CoolingCurveMode` enum + pure `CoolingCurveShape.For(mode, floor, max)` replace the single fixed automatic curve. All three share the non-negotiable safety anchors (start at the output's floor — the 50 % conservative floor for uncalibrated outputs — and reach the controller maximum by their critical temperature for emergency headroom); they differ in shape and responsiveness: **Silent** stays near the floor longer and only hits full speed near critical (88 °C), gentler slew + more hysteresis; **Cooling** ramps early and steeply, full speed at 78 °C, snappier slew + faster response; **Balanced** is the previous curve (85 °C). `AdaptiveCoolingProfileFactory.CreateAutomaticMode` takes the mode (default Balanced, so old calls/tests are unchanged); the graph id is stable per context so switching modes replaces rather than duplicates. Both the GPU fan (Performance) and case fans (Cooling) expose all three as buttons (`Performance.GpuFanSilent`/`EnableGpuFanAutoMode`/`GpuFanCooling`, `Cooling.CaseFansSilent`/`EnableCaseFansAutoMode`/`CaseFansCooling`). Pump/CPU-fan role protections and stale→max cooling unchanged.
- Tests: 18 new — curve-shape presets (floor/max anchors, Silent<Balanced<Cooling at a given temp, Cooling reaches full speed cooler + responds faster), factory mode selection, thermal-headroom stop (stops at 160 MHz with a rising-temp monitor vs climbing to 200 without), adaptive-screening-time and thermal-edge pure math. Full suite **716 tests (341 core + 375 integration), 0 warnings**; all nine pages rendered; live screenshots confirmed the Cooling case-fan modes and the Performance GPU-fan modes + Auto OC buttons. Not deployed (service still beta26); the Auto OC live run remains a user-witnessed pass.

## Verification snapshot: 2026-07-17 (one-click Close conflicting apps + conflict-aware messaging)

- **Root cause of "no armed GPU fan control".** A live probe showed every GPU/motherboard control `Blocked` by competing writers (gpufan/gpupower/gpuclock by MSI Afterburner + Fan Control; all `lhm.control:` fans by Fan Control) — the block-only-overlapping-controls policy working as designed, but the dashboard's "Turn on Hardware control and refresh" message was misleading because Hardware control was already on.
- **G-Helper-style quick kill button (user-requested).** RigPilot already had the close-blockers terminator (LocalSystem `StopConflictingProcesses`, from beta22) but it was gated behind a Diagnostics acknowledgement checkbox. New `CloseConflictingAppsCommand` surfaces it as a one-click button in a conflict bar on **both** the Performance GPU-controls card and the Cooling fan-controls card, shown only when a detected controller is running: it names the running blockers (`RunningConflictSummary`) and terminates them so RigPilot's gated controls unlock. No new safety surface — same IPC, same LocalSystem termination, same "equivalent to Task Manager; relaunch hands control back" boundary; the click is the explicit confirmation (the Diagnostics two-step checkbox path is unchanged). Buttons `Performance.CloseConflictingApps` / `Cooling.CloseConflictingApps`.
- **Conflict-aware messaging.** The fan auto-mode and Auto OC "unavailable" notices now inspect the capability's Blocked state and name the competing writer (`DescribeUnavailableFanOutputs` / `DescribeConflictOwners`), pointing at the new button, instead of the misleading Hardware-control hint.
- Validation: build 0 warnings/0 errors; full suite **716 tests (341 core + 375 integration)** passed; all nine pages rendered; **live screenshot confirmed** the Performance conflict bar naming "MSI Afterburner, NZXT CAM, Fan Control" with a live "Close 3 conflicting apps" button against the deployed beta26 service. The dashboard changes work against the existing beta26 service (the `StopConflictingProcesses` IPC already ships there); no redeploy needed to use the button.

## Verification snapshot: 2026-07-17 (IPC reliability: split connect/operation timeouts, clear timeout errors)

- **Fixed the recurring client-side apply timeout** (noted across several prior snapshots as a hardening item). `NamedPipeRequestClient` used a single short timeout (the App passed 3 s) for connect **and** write **and** read, so a transactional hardware apply — prepare, apply, read-back verify, plus the service serialising rapid back-to-back writes on one channel — could exceed it and fail client-side even though the write succeeded. Split into an independent `connectTimeout` (unchanged, fail-fast when the service is down) and `operationTimeout` (default 30 s) for the request/response once connected. Backward-compatible constructor (new optional param); the App's clients now get connect 3 s / operation 30 s automatically.
- **Actionable timeout errors.** A genuine timeout now throws a `TimeoutException` with a clear message ("The RigPilot service did not respond within 30 s. It may be busy with another hardware operation — try again in a moment.") instead of a bare "operation was canceled". Distinguished from real caller cancellation via the token.
- Tests: 2 new IPC tests — a 700 ms response inside the operation window but past a 250 ms connect timeout still succeeds (proving the split), and a never-responding server surfaces the actionable `TimeoutException`. Full suite **718 tests (341 core + 377 integration), 0 warnings**; build clean. No behaviour change on the happy path; every existing IPC test unchanged.

## Verification snapshot: 2026-07-17 (SMBus RGB — WITNESSED FIRST-LIGHT PASSED; kit audited; production path wired IPC/CLI-child/UI)

- **First-light succeeded, operator-witnessed**: static red transmitted to the ENE controller at 0x77 (20 transactions); the operator visually confirmed all four Trident Z sticks turned red. Identity was confirmed beforehand — the controller answers device name **'DIMM_LED-0103'** (register 0x1000, read via SMBus cmd 0x81 after a swapped pointer write to cmd 0x00; cmd 0x01 is write-only and reads 0xFF). All four sticks share the factory-default address 0x77 (never remapped by Aura), so they change colour in unison.
- Detection learnings pinned in code+tests: this firmware's read-only detection window (cmds 0xA0-0xBF) returns an incrementing sequence from base 0x00, not the documented 0x10 — `LooksLikeDetectionWindow` accepts any incrementing base with one glitch byte tolerated (live bus glitch observed), never a flat window. `IsKnownDeviceName` gates every transmit on prefixes DIMM_LED-/AUDA; a device that merely ACKs reads (the unknown 0x76 neighbour) is never written to. FCH port survey (read-only, incl. safe SPD DRAM-type presence reads): port 0 carries the four DDR4 SPDs at 0x50-0x53 — the DIMM bus is the default port.
- **F4-4000C15-8GTZR added to AuditedKits** with the evidence comment. Production path `SmbusRgbFirstLight.Apply` = identical detection+identity gating with the audit gate enforced; live-verified end-to-end via adapter-host child `--set-smbus-rgb FF0000` → WriteIssued/20.
- Wired the full chain mirroring Aura/Kraken: `--set-smbus-rgb RRGGBB|off` child (off = black) → `ContainedDimmRgb` (Core) → `IpcCommand.SetDimmRgb` (appended at enum end) + `DimmRgbRequestV1` (Experimental + exact-device 'gskill:tridentz-rgb-smbus') / `DimmRgbResultV1` → `SetDimmRgbAsync` in PCHelperRuntime → `ApplyDimmRgbCommand` in MainViewModel.LightingExtras + "Apply to RAM"/"RAM lighting off" buttons on the Lighting page. Feasibility card reason now reports the kit as controllable/audited.
- Suite 719 tests (341 core + 378 integration; convention contract tests auto-covered the new command/records), 0 warnings. NOT yet deployed — the installed service predates all of this; a deploy is needed before the app buttons work against the live service.
- Remaining (future, separately gated): per-stick control needs the ENE remap procedure, which touches SPD page-select territory (forbidden range) — requires its own audit before ever being attempted. One process-discipline note recorded: the first identity pass queried 0x76 (ACK-only gating) before the pattern/name gates were tightened — one pointer word reached the unknown device with no observed ill effect; the gates now make that impossible.

## Verification snapshot: 2026-07-17 (Native Razer USB writer — firmware-ACK-verified live; ELV8 = header-targeted Aura)

- **`RazerUsbRgbWriter`**: clean-room native writer for Razer Chroma USB devices from the community-documented protocol (openrazer docs; protocol facts only). 90-byte command report [status, txid 0x1F, remaining(2 BE), proto, dataSize, class, cmd, args(80), crc = XOR bytes 2..87, reserved] sent as HID feature report; only the extended-matrix STATIC effect (class 0x0F, cmd 0x02, dataSize 0x09, args varstore/led0/static/…/1/RGB) is ever issued — no firmware/profile/EEPROM command class exists in the type. Audited-PID allowlist gates writes; currently only 0x0F13 (Lian Li O11 Dynamic Razer Edition).
- **Interface lesson (live-diagnosed on the reference rig)**: the O11 exposes 7 HID interfaces; the vendor control interface (mi_02, feature length 91) REFUSES a normal read/write open even with nothing else running. Fix: zero-access-rights CreateFileW (desiredAccess 0, full sharing) + HidD_SetFeature/HidD_GetFeature — feature IOCTLs need no read/write access. `--probe-razer-usb` child prints per-interface diagnostics read-only.
- **Live-verified**: static red → firmware replied status 0x02 (success ACK). This path has genuine read-back, unlike Aura/Kraken lighting.
- Full chain wired mirroring the other native writers: `--set-razer-usb-rgb RRGGBB|off` child → `ContainedRazerRgb` → `IpcCommand.SetRazerRgb` + `RazerRgbRequestV1` (Experimental + exact-device 'razer:lianli-o11-dynamic') / `RazerRgbResultV1` → `SetRazerRgbAsync` → "Apply to Razer case" buttons on the Lighting page (distinct from the Chroma-SDK section, which still needs Synapse).
- **Cooler Master ELV8 ARGB GPU holder**: passive 3-pin ARGB strip, no addressable controller of its own (the bundled button box is hardware-cycled only). Software control = the per-header Aura targeting built earlier; the Lighting-page section is now named for the ELV8 with plug-into-a-motherboard-header guidance.
- Suite 745 tests (341 core + 404 integration), 0 warnings. All of this is uncommitted on master locally; the installed service still predates SetDimmRgb/SetRazerRgb — deploy pending (UAC declined twice earlier).

## Verification snapshot: 2026-07-17 (Full Auto OC, adaptive fan mode, sync-all RGB)

- **Full Auto OC** (`StartFullAutoOcAsync`, Performance page): runs the core auto-OC, polls the service operation to a terminal state (45-min bound, live `FullAutoOcStatus`), and only on `Completed` starts the memory pass — memory is never tuned on top of a failed core run. Reuses the bounded engine unchanged (83 °C ceiling, screening, rollback, boot sentinel).
- **Adaptive fan mode** (`CoolingModeSelector` in Core, pure + 6 tests): picks Silent (≤55 °C) / Cooling (≥75 °C hottest of CPU package & GPU core) / Balanced with a 4 °C hysteresis band so the choice doesn't flap; "Adaptive (pick for me)" button beside the case-fan presets; applies through the same verified graph engine.
- **Sync-all RGB** (`SyncAllRgbAsync`, Lighting page "SYNC EVERYTHING"): one click sends the chosen colour (or off) to Kraken + both Aura headers (carries the ELV8) + Trident Z RAM + Razer case through their own gated IPC paths, with a combined per-device ✓/✗ notice.
- Suite 754 tests (350 core + 404 integration), 0 warnings. Still uncommitted locally on master; deploy still pending (installed service predates SetDimmRgb/SetRazerRgb).

## Verification snapshot: 2026-07-17 (0.5.5-alpha deployed — user-approved UAC)

- Version prefix is 0.5.5; user asked to deploy this line as "0.55". Full suite re-run first: **754 tests (350 core + 404 integration), 0 warnings**.
- `publish.ps1 -Version 0.5.5-alpha` produced `artifacts\publish`; `Test-RuntimePayload.ps1` Passed (6 components, protocol 2).
- **User-approved UAC deploy via `Update-LocalAlphaRuntime.ps1` succeeded** (restore-then-install; `RestoredExistingLocalAlpha=True` from the `0.5.0-alpha-20260717-102246` beta26 image). Post-deploy read-only checks: service Running/Automatic, `status` healthy with writes enabled, `runtime-preflight` Ready — dashboard 0.5.5-alpha, service 0.5.5-alpha, protocol 2. The live service now carries `SetDimmRgb` and `SetRazerRgb`, so the DIMM/Razer/sync-all Lighting buttons and Full Auto OC work end-to-end.
- Dashboard relaunched from `artifacts\publish\app\PCHelper.App.exe` ("RigPilot — Overview", responding).
- The 0.5.5 work itself remains uncommitted on master (commit was not requested this pass).

## Verification snapshot: 2026-07-17 (Design tokens + Performance page pilot)

- **`Themes/RigPilot.Tokens.xaml`** (merged first in App.xaml via `ResourceDictionary.MergedDictionaries`): codifies the existing visual language as named tokens — capability-state brushes (`StateVerified`/`StateExperimental`/`StateBlocked`/`StateUnavailable` + Soft tints), 4px-grid spacing `Thickness` tokens, the radius ramp (card 11 / inset 10 / badge 12 / button 18), a type ramp, and a reusable `StatusBadgeStyle` (icon + word + colour, `AutomationProperties.Name` "Capability state: …"). New XAML should use tokens instead of literal margins/radii; pages migrate as touched.
- **Blocked is no longer red**: `CapabilityDisplay` `StateTone` now maps Blocked → new calm "Blocked" tone (blue-grey `#8FA8C8`), Critical is reserved for Faulted; `UiPresentationTests` updated deliberately. Rationale: a competing writer is informational and recoverable; alarm colours keep their meaning.
- **Performance page pilot** (One UI viewing/interaction split): conflict banner promoted out of the GPU card to the top of the page (both modes, `LiveSetting=Polite`, calm Blocked tone, same AutomationId); GPU controls card gains a 3px capability rail (green live / blue-grey blocked / neutral off) and a "Blocked" StatusBadge beside the Hardware-control badge while conflicts run; an always-visible "ADVANCED LAB" divider with a Simple-mode caption explains where the hidden tuning surface lives.
- Validation: build 0 warnings; **754 tests** pass (StateTone test updated); all nine pages rendered (`artifacts\ui-snapshots-tokens-pilot`, plus a 2160-tall Performance render) — banner, badge, and rail visually confirmed. Uncommitted; not deployed (visual-only dashboard changes; the 0.5.5 service is unaffected).
- **Cooling page, same pattern**: conflict banner promoted to page top (calm Blocked tone, same `Cooling.CloseConflictingApps` AutomationId); the read-only "Fans, pumps, and liquid" card and Simple-mode status cards **moved above** the fan controls card (viewing area first, matching Performance); fan controls card gains the 3px capability rail and a "Blocked" StatusBadge beside the outputs-count pill; "ADVANCED LAB" divider with Simple-mode caption before the curve studio/calibration workspace. Rendered tall (`artifacts\ui-snapshots-cooling-pilot`): banner, moved viewing area, badge, and rail confirmed; 754 tests still pass.
- **Lighting page, same pattern**: top banner keyed to `HasLightingConflict`/`LightingConflictReason` (lighting-family conflicts), with the Close button additionally gated on `HasRunningConflicts` because lighting-only writers are not on the closable allowlist (the reason text states RigPilot will not terminate them). The Static lighting card's rail/badge rest at **Experimental (amber)** — honest for visual-confirm native writes — flipping to Blocked blue-grey during conflicts. Old `SimpleLightingGuide` card replaced by the shared "ADVANCED LAB" divider + Simple-mode caption; the caption carries the `x:Name` so the UiSnapshot smoke contract still holds. Rendered tall (`artifacts\ui-snapshots-lighting-pilot`): amber rail + Experimental badge + divider confirmed (banner correctly absent — no lighting conflict in the snapshot); 754 tests pass.

## Verification snapshot: 2026-07-17 (Nav grouping + capability-state colour consistency)

- **Left-nav grouping** (Fluent 2 nav sections): new keyed `NavigationItemStyle` in App.xaml (based on the implicit `ListBoxItem` style, so it never leaks to other lists) renders a group-header eyebrow above any nav item carrying a `Tag`, *outside* the selection chrome. The nav stays a single 9-item `ListBox` — selection is entirely index-based (`Navigation.SelectedIndex` → `_pages[]`/`PageTitles[]`, with hardcoded `=2`/`=7` jumps and Ctrl+1-9), so no non-selectable rows were added. Groups: **CONTROL** (Overview/Profiles/Cooling/Performance/Lighting, standalone header kept), **AUTOMATION & TOOLS** (Tag on Automation), **SYSTEM** (Tag on Devices). Verified in renders; indices/shortcuts unchanged.
- **Capability-state colour consistency**: Overview "Control readiness" legend dot for "Blocked or unsupported" was `CriticalBrush` (red) → now `StateBlockedBrush` (calm blue-grey), matching the new language (red reserved for Faulted). The shared `CapabilityTemplate` (used by Performance capabilities, Cooling capabilities, **and** Devices capability decisions) already got the Blocked tone earlier, so all three lists render calm Blocked automatically.
- **6-page rollout decision (user: "tokens + layout, no false chrome")**: Overview, Profiles, Automation, Games & tools, Devices, Diagnostics were already `CardStyle`-based and correctly ordered (viewing/interaction). They are lists/tools, not the controls-above-readouts inversion that Cooling had, so no rail/banner/Blocked-badge chrome was added (that would imply blockable state where none exists). The genuine deltas were the nav grouping and the Blocked-colour alignment above; no cosmetic token churn was applied to XAML that already rendered correctly.
- Validation: build 0 warnings; 754 tests pass; all nine pages rendered (`artifacts\ui-snapshots-nav2`) — nav sections, Overview legend blue-grey, and Devices calm-Blocked decisions confirmed. Uncommitted on the branch (on top of the design-token commit `3c8b880`).

## Milestone: 2026-07-17 (0.5.5 merged to master — features + UI design system)

- **PR #23 merged to master** (merge commit `4653ccd`); branch `session/razer-usb-full-auto-oc-055`. Contents, in order: `2b7ca70` (0.5.5 hardware: native Razer USB RGB, Full Auto OC, adaptive fan mode, sync-all RGB; version prefix 0.5.5), `3c8b880` (design tokens + capability-state language across Performance/Cooling/Lighting), `08c2581` (nav grouping + Blocked-colour consistency). PR title/body rewritten to cover both feature and UI work with the honest-state rationale. `master` fast-forwarded and pulled locally.
- **Deploy status**: the installed background service is `0.5.5-alpha` (deployed earlier this session via UAC, `runtime-preflight` Ready). The three UI commits landed *after* that deploy but are dashboard-only XAML with no service component — nothing to redeploy. The **published `artifacts\publish` payload predates the UI commits**; republish only if the installer needs to carry the new UI.
- The app was launched from the freshly built Release binary (`src\PCHelper.App\bin\Release\net10.0-windows10.0.19041.0\PCHelper.App.exe`) so the running dashboard shows the new nav grouping, tokens, and calm-Blocked language — not the older published payload.
- Design-system conventions now in force for future UI work: use `Themes/RigPilot.Tokens.xaml` tokens (spacing/radius/type/state brushes) instead of literals; the four capability states are colour+icon+word (`StatusBadgeStyle`); **Blocked is calm blue-grey (informational), red is reserved for Faulted only**; the rail/banner/Blocked-badge chrome belongs only on pages with genuinely blockable hardware writes (Performance/Cooling/Lighting), never on read-only list/tool pages. Profiles is the one page with a possible future density pass (not a blocker; already token-based).

## Verification snapshot: 2026-07-18 (Phase 2 polling and binding efficiency, source only)

- **WPF collection churn:** `BatchedObservableCollection<T>` now reconciles immutable display rows by stable key, retains equal instances, applies insert/move/replace/remove operations under nested notification deferral, and emits one `Reset` at the end of the complete display update. The ScottPlot comparison subscriber therefore redraws once per changed refresh instead of once for `Clear` plus every `Add`; an identical snapshot emits no collection event.
- **Dashboard IPC cadence:** the one-second path retains fresh `SubscribeSensors` telemetry. The seven control-plane requests (handshake, status, operation, commissioning sessions/calibrations, cooling assignments, update status) run every 5 seconds, and trace plus the five reliability requests run every 10 seconds; active hardware operations retain one-second status polling. This changes steady no-operation traffic from 14 one-shot requests per second to one on most ticks and a code-derived average of 3 per second, without adding or changing an IPC command.
- **Service topology cache:** `AdapterCoordinator` now caches only adapters that explicitly implement `IAdapterTopologyCachePolicy`; zero-duration/dynamic adapters still probe every tick. Windows system/vendor WMI metadata uses 5 minutes; hot-pluggable peripheral inventory, the LHM Adapter Host, NVML topology/bounds, and ADLX/IGCL presence metadata use 30 seconds. Telemetry remains uncached every second, and `InvalidateTopology` is the device-change hook. `TraceableHardwareAdapter` forwards the policy so the service decorator does not defeat caching.
- **LHM update coalescing:** a successful topology probe marks its `UpdateHardware` result reusable by the immediately following sensor read for up to one polling interval. Normal cached ticks update once in `ReadSensorsAsync`; topology-refresh ticks update once in `ProbeAsync` and consume that same tree for telemetry. Prepare/verify hardware-control updates remain independent and unchanged.
- Verification: Debug and Release builds completed with 0 warnings/errors; 354 core + 417 integration tests passed (771 total). Five focused tests cover one-reset/no-op collection notifications, topology expiry, explicit invalidation, per-tick sensor reads, and zero-duration dynamic probes. The repository WPF smoke passed all nine pages, 220/220 named interactive controls, no duplicate automation IDs, and 7,439 visual elements; renders are under `artifacts/ui-snapshots-phase2-performance`. The first Release test attempt hit the existing 250 ms named-pipe startup timing test once; that test passed immediately in isolation and the complete Release suite then passed. No IPC command shape or hardware write path changed. The installed 0.5.5 service was not replaced, so service-side topology caching takes effect only after a separately requested deployment; the newly built source dashboard was relaunched and is responsive.

## Verification snapshot: 2026-07-18 (Phase 3 hardware-state integrity and crash recovery, source only)

- **Serialized, verified hardware-control arming:** the dashboard now drives one asynchronous `SetHardwareControlArmed` command behind a `SemaphoreSlim` and keeps the toggle read-only while it is running. The service serializes that request with profile mutations, opens the requested GPU-family gates as one operation, restores each family to its default baseline, and requires an independent adapter read-back before it commits the logical armed state. The dashboard persists its preference only when `AllRequestedFamiliesVerified` is true. Any failed or indeterminate mutation is fail-closed as `RecoveryRequired`; no UI `Verified` state is inferred from a successful write alone.
- **Durable control lease and verified recovery:** committed profile capabilities are saved in `HardwareControlLeaseV1` before the pending journal is cleared. Service startup evaluates the lease, pending transaction, and legacy committed profile before IPC starts; an unclean marker triggers default restoration plus read-back verification. A failed proof retains a `RecoveryRequired` lease and blocks every mutating IPC command while read-only diagnostics remain available. Clean shutdown is recorded only after leased controls are restored and verified; otherwise the next startup repeats recovery.
- **Adapter-host timeout containment:** named-pipe callers now use independent connection and operation budgets (2 seconds and 10 seconds for Adapter Host calls). A timed-out host generation is atomically detached and its complete process tree is terminated before a replacement can start. Read timeouts remain retryable diagnostics; mutation timeouts raise `HardwareStateUnknownException`, which propagates into the transaction recovery lock rather than reusing the host or assuming the write failed safely.
- Verification: the Release solution build completed with 0 warnings/errors; 363 core + 427 integration tests passed (790 total). Added coverage proves verified rollback, unknown-state lockout, lease-before-journal ordering, startup/clean-marker planning, default read-back failure, shared mutation serialization, atomic hung-host termination, separated pipe timeout behavior, and UI state persistence only after complete family verification. Tests use fake adapters and isolated disposable host processes; no installed service was replaced and no live hardware mutation was executed.

## Verification snapshot: 2026-07-18 (native RGB routing diagnosis and fail-closed UI reporting)

- The report that only Trident Z RAM changed was not a missing-device problem: read-only inventory found the exact Kraken X3, AURA LED Controller (`VID_0B05/PID_18F3`), and Lian Li O11/Razer controller (`VID_1532/PID_0F13`), with no running lighting conflict. The source dashboard is newer than the installed `0.5.5-alpha` service and correctly reports `UpgradeRequired` because the service lacks `verified-hardware-control`; service-owned writes remain locked until a matching runtime is explicitly deployed.
- Native RGB public methods now repeat the full `CanRunHardwareAction` safety check instead of relying only on WPF `CanExecute`. `SyncAllRgbAsync` retains each target's contained result message, so AccessDenied, device-not-found, RecoveryRequired, and firmware failures are shown per device instead of collapsing to an unexplained cross mark.
- Bounded exact-device checks used the already-selected `#4EA1FF` lighting colour only: the Razer case returned firmware ACK `0x02`; Kraken reported its ring/logo frame issued; AURA reported static frames issued to both headers; and the installed service's Kraken route also reported `WriteIssued`. Kraken/AURA have no lighting read-back and still require visual confirmation; no pump, fan, firmware, EEPROM, SPD, GPU-bus, or service-deployment action occurred.
- Verification: Release build completed with 0 warnings/errors; 363 core + 430 integration tests passed (793 total). Seven focused UI-state/RGB-result tests passed, including device-detail retention and transport-error precedence. The matching service was not published or deployed.

## Verification snapshot: 2026-07-18 (fan minimum lowered to 20%, source only)

- The uncalibrated automatic-curve minimum is now 20% for case and GPU fans. NVAPI, NVML control bounds, NVML read-only capability discovery, service validation, ViewModel status text, and Cooling/Performance UI copy all consume the same Core policy constant so the paths cannot silently drift back to different values.
- The effective minimum is `max(20%, controller-reported minimum)`. A read-only local source probe found 60 devices, 332 sensors, and 20 capabilities; both RTX 3090 fan targets reported their real 30–100% controller range, so RigPilot correctly exposes 30%, not an unsafe fabricated 20%, on this board. The independent Kraken pump minimum remains 60%; CPU-fan/pump role blocks, stale-sensor maximum cooling, full-speed critical points, reset/read-back gates, and zero-RPM prohibition are unchanged.
- Verification: Release solution build completed with 0 warnings/errors; 364 core + 430 integration tests passed (794 total). Focused automatic-curve coverage includes the 20% floor and the higher-controller-minimum override. The local probe and transport tests were read-only; no fan command, service deployment, or installed-runtime replacement occurred.

## Deployment snapshot: 2026-07-18 (Phase 1–3, RGB reporting, and 20% fan policy live)

- `publish.ps1 -Version 0.5.5-alpha` produced the six-component runtime under `artifacts\publish`; `Test-RuntimePayload.ps1 -ExpectedProductVersion 0.5.5` passed with protocol 2. The payload is an unsigned development build, so automatic process takeover remains hard-blocked.
- The user-approved UAC update through `Update-LocalAlphaRuntime.ps1` completed successfully. The PCHelper service is Running/Automatic from `C:\ProgramData\RigPilot\LocalAlpha\0.5.0-alpha-20260718-010606\payload\service\PCHelper.Service.exe`.
- Post-deploy `runtime-preflight --json` is Ready: dashboard/service `0.5.5-alpha`, negotiated protocol 2, no missing features, service writes available. `status --json` reports healthy, `recoveryRequired=false`, and `hardwareControlArmed=false`. The published dashboard was launched and is responsive on `RigPilot — Overview`. Deployment itself issued no hardware control or RGB command.

## Verification snapshot: 2026-07-18 (0.5.5 preview trust and release pipeline)

- One product version source in `Directory.Build.props` now feeds CI, publish, Game Bar, MSI, bundle, and local-alpha validation. PR #24's stale CI `0.4.0` packaging arguments were removed; prerelease SemVer is converted to a legal numeric MSI version.
- Unsigned public previews compile the service with `RigPilotPublicUnsignedPreview=true`. Every `IpcCommandPolicy` mutation returns `PUBLIC_PREVIEW_READ_ONLY`; read-only monitoring remains available. `runtime-contract.json` records the trust policy, and payload validation executes the published service's `--print-release-policy` probe rather than trusting the manifest alone.
- The release workflow supports staged SignPath signing of runtime components, MSI, and bundle, followed by Authenticode verification, runtime hash refresh, checksums, SPDX SBOM, and provenance attestations. SignPath organisation/project/configuration values remain external enrolment inputs. A PFX path remains as a fallback.
- Public startup is offline: the GitHub update request now runs only from the explicit dashboard action. `CODE_SIGNING.md` and `PRIVACY.md` define roles, approval, local data, and every network route.
- Verification: 376 core + 453 integration tests passed (829 total), workflow files parsed as YAML, `0.5.5-preview.1` was built with `publicUnsignedPreview=true;writesAllowed=false`, runtime payload validation passed, and WiX built `RigPilot.Installer.msi` with 0 warnings/errors. The tagged CI run later published this exact read-only payload as a GitHub prerelease. No service deployment, installation, or hardware write occurred.

## Verification snapshot: 2026-07-18 (trusted daily-driver onboarding and verified reset)

- The first-run tour is now a seven-step guided Simple-mode workflow: read-only hardware/ownership scan, capability-state explanation, Quiet/Efficiency/Performance selection, three-sample telemetry baseline, explicit eligibility/omission dry run, one service-owned profile transaction, and an independent Windows-default restoration proof. The initial stock-safe mode bundle changes only the discovered Windows power scheme; cooling, tuning, lighting, and OSD are explicitly omitted rather than presented as atomic when they are not.
- Mode choices use the shared neutral mode style and acquire a blue border only from confirmed `Tag=True` selection state. Hardware action buttons are neutral. Hardware-control, profile-apply, reset, and existing hardware-feature commands remain clickable when unavailable and open their prerequisite or recovery reason.
- `ResetHardware` now routes through `ProfileTransactionEngine.RestoreDefaultsAsync`; success requires `IHardwareStateVerifier.VerifyDefaultStateAsync`. Missing or mismatched read-back sets the service recovery lock and returns `RECOVERY_REQUIRED`, so the onboarding restoration result cannot be inferred from a reset call alone.
- Verification: Debug build completed with 0 warnings/errors; 376 core + 458 integration tests passed before the final clickability assertion was added, and the focused onboarding/UI/core suites passed. The read-only snapshot host rendered the guided mode-selection step at 1240x800 with only Efficiency outlined blue. No installed service was replaced and no hardware mutation was issued.

## Verification snapshot: 2026-07-18 (Auto OC V3 and profile dry-run evidence)

- `StartAutoOcV3` is an optional feature-negotiated protocol-v2 operation. The service requires exact bounded core/memory controls on one GPU, optionally accepts a bounded controller-reported power-limit control, captures three stock-state throughput baselines, rejects variation above the requested limit (3% from the dashboard), and selects Performance, Efficiency, or Quiet candidates only from measured workload results. The final combined synthetic screen defaults to 20 minutes. PresentMon remains optional supplementary evidence and cannot weaken the synthetic gate. No voltage action is constructed.
- Auto OC V3 captures a GPU identity/VBIOS/display-driver fingerprint, keeps generated profiles `Experimental` and `Provisional`, and always restores every captured prior control in reverse order with independent read-back. Any unproved restore becomes `RecoveryRequired`; only a provisional result with all requested families verified and a known restored state is persisted. Physical qualification and the 10 active-use hours/seven calendar days/three cold boots promotion gate remain incomplete.
- `PreviewProfileV2` is read-only and feature-negotiated. It reports required capabilities, optional omissions, ownership conflicts, linked cooling qualification, missing exact-device or hazard acknowledgements, service-atomic domains, user-session lighting/OSD companions, and the exact rollback boundary. The dashboard exposes a neutral Dry run action and consumes the same result before current-profile apply; it blocks missing read-back or linked resources rather than starting a partial bundle.
- Verification: Debug build completed with 0 warnings/errors; 386 core + 460 integration tests passed (846 total). New fake-adapter/policy coverage proves baseline rejection, objective scoring, final restoration, workload throughput/RPM capture, profile conflict/read-back blocking, unsigned-preview read-only classification, and feature negotiation. The profiles page rendered at 1240x900 in portable read-only mode with no clipping. No installed service was replaced and no hardware mutation was issued.

## Verification snapshot: 2026-07-18 (Auto OC tune-validity lifecycle; four evidence-soundness holes closed)

- **`AutoOcProfileValidationV1` + `AutoOcValidationPolicy`** complete the promotion gate that Auto OC V3 left open: durable per-profile records, service session accounting (`Activate`/`Deactivate`/unclean-session invalidation), fingerprint-drift invalidation, `RecoveryRequired` marking, and a read path (`GetAutoOcProfileValidations`) through to the profile card. Stability events are fed from the real `WindowsSystemHealthSignalProbe`. Cold boots are credited only at clean shutdown when defaults verify.
- **All four defects fixed here are one class: absence of evidence scored as favorable.** This is the recurring failure mode in this codebase, because the favorable outcome is almost always the `else` branch — it is invisible unless you deliberately ask "what happens when this data is missing?"
  1. **Idle time counted as proof of stability.** `Deactivate` credited `now - started`, pure wall clock, so a machine idling at the desktop for ten hours accrued the full 10-hour gate. A tune that had never done a minute of work could reach `Validated` — the state that unlocks *automatic* game/startup/automation activation. Active use now accrues only via `RecordActiveUseSample`, requiring the tuned GPU at ≥25% utilisation, clamped to a 30 s maximum per sample so a sleep/resume gap cannot inject unobserved hours. Absent utilisation telemetry is not qualifying load.
  2. **`Rejected` was missing from the promotion guard.** `EvaluatePromotion` guarded only `Invalidated`/`RecoveryRequired`, so accruing evidence rewrote a refused or discarded tune back to `Provisional` and onward to `Validated`. All three write points (promotion, sample accrual, session opening) now share one `IsTerminal` predicate rather than three hand-maintained state lists that can drift.
  3. **Missing sensors read as cool.** `AdaptiveCoolingController` fell through to `Choose(null, null, …)` → `Balanced`, so a machine reporting no temperatures would have had its cooling *stepped down*. Absent readings now escalate identically to stale ones; an already-cooling system holds without re-issuing writes.
  4. **Late crash signals were discarded.** WHEA/display-reset detection lags because the probe polls, so a crash at 12:59 could surface after a 13:00 shutdown closed the session — and that session was banked as a *successful cold boot*. Records now retain the last closed session window and attribute late events to it; events outside every window stay unattributed.
- **Verified sound, no change needed:** `HardwareFingerprintBuilder.TryCreate` refuses to build a fingerprint unless identity, VBIOS, and driver are all present, so an unreadable field can never become an empty-equals-empty match. This is the reference example — *reject at capture, so comparison never has to reason about missing data*. Also audited and found correctly guarded: every non-test `.All()` call site (vacuous-truth on empty sequences — the apply gate early-returns on `available.Length == 0`, the recovery gate requires `Count == available.Length`, `SupportsNonZeroCurve` requires `Count > 0`, the release gate always has baseline requirements), and long-lived graph state (a fresh `CoolingGraphRuntime` per activation keeps the unpruned dictionaries bounded).
- **`PrivilegedServiceBoundaryTests`** lock in two properties users of this software category consistently ask for and vendor suites consistently break: the SYSTEM service references **no networking assembly** (no telemetry, no accounts, no silent update channel — true by construction, now enforced at build time) and the long-lived runtime stays deterministically disposable, a structural guard against the handle-leak class that has starved kernel resources in a competing lighting service.

## Verification snapshot: 2026-07-18 (continuous adaptive cooling and fan start/stop — policy plus wiring)

- **Both policies shipped tested-but-dead first, then were wired.** `AdaptiveCoolingController` and `FanStartStopPolicy` initially had full test coverage and *zero* non-test references — the defects were documented and fixed in policy while remaining entirely unfixed in behaviour. Worth remembering: a fix that nothing calls is not a fix.
- **Continuous adaptive cooling.** Previously `CoolingModeSelector.Choose` was called from exactly one place — the button handler — so "Adaptive" picked a curve once at click time and the hysteresis only mattered across manual re-clicks. `TickAdaptiveCoolingAsync` now evaluates every telemetry tick in the service, so it keeps working with the dashboard closed. It recognises an automatic profile by the stable `auto.profile.` id the factory already emits, so **no new protocol surface** was added. Switching requires a 15 s sustained hold plus a 60 s dwell; both guards are bypassed **only to escalate** (emergency temperature ≥85 °C or missing/stale telemetry), never to reduce cooling. Evaluation runs *before* the cooling and mutation gates are taken, because the activation transaction acquires the cooling gate itself — evaluating inside it would deadlock on the first mode switch. A failed switch is caught and leaves the running curve untouched.
- **Fan start/stop** is applied as the final output stage, after slew: a stop must reach a true 0% *below* the calibration floor the earlier stages enforce, and slew-limiting the restart boost would defeat it. **Stopping is a privilege, not a setting** — it requires an opt-in `StopBelowPercent` *and* `FanCalibrationPolicy.SupportsVerifiedFanStop` evidence for that exact fan. A pump or protected CPU fan never earns that calibration, so no configuration can stop one, including from a corrupted persisted state (covered by test). Leaving rest commands at least the *measured* restart duty, overriding a too-low user setting, so a fan told to go 0% → 15% cannot sit stalled at 0 RPM while the curve believes it is spinning. Graph validation rejects configurations where the restart duty is at or below the stop threshold, which would oscillate.
- **Forum research grounding these priorities** (2026-07): the most-cited technical failure in this category is a vendor lighting service leaking >7M handles until kernel resources starve, alongside failure to re-recognise hardware after reinstall; the most-repeated feature request across comparable projects is fan-oscillation control (hysteresis + response time), with users describing the changing fan *pitch* as worse than the noise. The hold/dwell design is that pattern applied to mode switching. Curve-parity audit against Fan Control: hysteresis, response time, step up/down, mix, RPM calibration, `Sync`, `FeedbackAuto` (auto-curve), and per-output `Offset` **already existed** — only start and stop were genuinely missing. `CompetitorImporters` (ingesting Fan Control configs) is an under-sold adoption lever.

## Verification snapshot: 2026-07-18 (adapter-host probe timeout — root cause of stale sensors and missing motherboard fan control)

- **The defect.** The LibreHardwareMonitor topology probe takes **~16.5 s** with elevated access on the reference ROG STRIX X570-E (measured, twice, both cold). `AdapterHostProxy` applied a single 10 s `OperationTimeout` to every host call, so the probe was killed and the host recycled **on every capture**. The failure was self-perpetuating: the 30 s topology cache never populated, so the next capture reran the same doomed probe. This had been happening continuously; LHM had effectively never completed a probe in this deployment.
- **One timeout, three symptoms that looked unrelated.** (1) A fixed ~11.0–11.2 s per capture (10 s timeout + ~1 s recycle) — stable *because it was a timeout, not variable work*. Since each adapter stamps its samples when read and `AdapterCoordinator` stamps `CapturedAt` at the **end** of the sequential loop, every adapter's sensors appeared ~11 s stale, exceeding the cooling engine's 3 s `maximumSampleAge` and rejecting **all** automatic fan curves with `PROFILE_REJECTED … Sensor is stale`. (2) Zero `lhm.control` capabilities, hiding all seven Nuvoton NCT6798D motherboard fans; the Cooling page reported "0 outputs" (the motherboard *device* still appeared, because that comes from WMI via `SystemInventoryAdapter`, not LHM). (3) `ADAPTER_FAILED` on every snapshot.
- **The fix.** Topology discovery gets `TopologyProbeTimeout` = 45 s via an optional per-call budget on `SendReadAsync`. Sensor reads, health checks, and **mutations deliberately keep the 10 s `OperationTimeout`**: a mutation that outlives its timeout leaves hardware state unknown (`HardwareStateUnknownException`) and must be detected quickly rather than waited out. Widening that path would trade a safety property for a green checkmark.
- **Measured before → after on the reference rig:** sensor age vs capture 11.2 s → **0.61 s**; snapshot age 1.3–10.8 s → 0.5–1.2 s; `lhm.control` capabilities 2 (GPU only) → **9** (+7 NCT6798D fans, all Experimental); adapter warnings every capture → **none**.
- **Live adaptive-cooling verification** (first successful activation): dashboard reported *"Adaptive cooling picked Balanced: GPU 59 °C — the Balanced curve fits"*; service reported `ActiveProfileId=auto.profile.case-fans`, `GraphId=auto.cooling.case-fans`, `State=Normal`. Fan duties moved 58.4% → 65.9% (RPM 1277 → 1424). Across five samples over 25 s with temperature drifting 60 → 59 °C the mode **held** with no re-application — the 15 s hold and 60 s dwell behaving as designed. **Not yet exercised on hardware: a genuine mode transition** (crossing 75 °C under load to escalate to Cooling, then stepping back after dwell).
- **A UI gap found only by testing:** the "Adaptive (pick for me)" button existed solely in the case-fan section, which had zero outputs while this bug was live, whereas GPU fans — which worked — had no Adaptive button at all despite `StartAdaptiveCoolingAsync(gpuFans: true)` being fully implemented. The feature was unreachable on the reference machine. A GPU-side button was added.
- **A wrong hypothesis, recorded deliberately.** Before measuring, storage SMART was assumed to dominate capture cost; LibreHardwareMonitor was given tiered updates (storage/network on a 30 s cadence) and that change was published and installed. It moved the number **zero** — the timeout was on the *probe* path, not the sensor path. The tiering is retained only because it carries **honest per-subsystem update timestamps**: skipping a subsystem while stamping its samples with collection time would report stale data as fresh, which is the same defect class this codebase keeps closing. The 16.5 s measurement that actually identified the bug took one command and would have saved an entire publish/install cycle. **Measure before optimising; a suspiciously stable duration is a timeout, not work.**
- Verification: Debug build 0 warnings/errors; **885 tests** (422 core + 463 integration). Shipped as `0.5.5-alpha-20260718-180633`; installed service hash matches the published payload. Branch `codex/competitive-leadership` → [PR #25](https://github.com/NiccTM/rigpilot/pull/25) (6 commits, 48 files).

## Verification snapshot: 2026-07-18 (Auto OC end-to-end test — three defects fixed, one architectural blocker identified and NOT fixed)

**Auto OC does not work. The blocker is now precisely identified rather than mysterious.** Driving the shipped UI on the reference rig found four distinct defects; three are fixed, the fourth is architectural and is the next session's first task.

- **Defect A (fixed): the workload host could never launch from a deployed payload.** `WorkloadHostSession.ResolveExecutablePath` accepted a co-located `PCHelper.WorkloadHost.exe` on `File.Exists` alone. The published `app/` directory receives that apphost as a project-reference build side-effect **without** its managed `.dll`, so the check passed on a launcher that cannot start ("The application to execute does not exist: …\app\PCHelper.WorkloadHost.dll") and the correct sibling `workload-host/` component was never reached. Auto OC V3 had therefore never run on any deployed install — only from a dev build where both files sit together. `IsLaunchable` now requires the managed assembly, not just the apphost. Same pattern as the rest of the day: **testing for a proxy artifact instead of the thing that must actually be present.**
- **Defect B (fixed): the remedy destroyed the diagnosis.** Both Auto OC engines threw `could not prove N control state(s)` and discarded the individual exceptions into an `AggregateException` that never reached storage; only the count string persisted. The adapter trace that held the detail is a bounded 512-entry in-memory buffer, and the failure told the operator to restart the service — which flushes it. Failures now record `control name (capability id): ExceptionType: reason` per control **in the message itself**, because that string is what reaches the durable operation record. This fix is the only reason Defect D was found; it paid for itself on the first run after deployment.
- **Defect C (fixed): a genuine safety deadlock in the restore path.** `RollbackAsync` restores by calling `SetOffsetAsync`, which calls the transport's `EnsureWriteArmed()`. Auto OC disarms when it finishes, so: **disarm → restore is a write → write refused → cannot prove default state → RecoveryRequired → writes locked → still cannot restore.** The gate protecting against unauthorised writes blocked the write the safety system itself needs to return hardware to stock. The adapter layer already encoded the right intent (rollback is deliberately exempt from its own `EnsureWritesEnabled`); the transport did not honour it one layer down. Added `IGpuClockOffsetTransport.RestoreOffsetAsync`, which bypasses **only** the arm gate — transport-enabled, not-disposed, editable-P0, and driver-delta-range checks all still apply — and pointed rollback/reset at it. A restore can only move hardware toward the state it was already in, so refusing one strictly increases risk. Tests pin that an ordinary apply is **still** refused while disarmed.
- **Defect D (NOT fixed — architectural): NVAPI/NVML writes are refused in the LocalSystem service context.** With Defect C removed, the restore reached the driver and failed there. Exact durable evidence: `GPU power limit (gpupower.limit:0): InvalidOperationException: nvmlDeviceSetPowerManagementLimit failed with NVML result 4` (= `NVML_ERROR_NO_PERMISSION`), and both clock offsets: `NVIDIAApiException: NVAPI_INVALID_USER_PRIVILEGE`. The service runs as **LocalSystem** (session 0, no interactive desktop); NVAPI performance-state writes and NVML power-limit writes refuse that context. All three controls fail identically and reproducibly, which is why every Auto OC run ended `RecoveryRequired`. The likely fix is to perform these writes from the signed-in user session — the pattern `PCHelper.WorkloadHost` already uses — rather than the SYSTEM service.
- **Unresolved contradiction that must be settled before fixing D.** These same writes **succeeded earlier the same day**: GPU core +15 MHz and memory +15 MHz applied with read-back verification, and GPU fan/power writes verified live. Something differs between that path and the Auto OC V3 restore path. Do not design a user-session transport until this is explained — a working path may already exist, and building a second one would be wasted work on a wrong assumption.
- **Hardware was never left modified.** Verified independently with `nvidia-smi` after every failed run: `power.limit 350.00 W = power.default_limit 350.00 W`, memory at its normal idle P-state, core/memory offsets 0 MHz. RigPilot failed closed and locked its own writes because it could not *prove* restoration, not because the GPU was left tuned. Clearing the lock is **Diagnostics → Reset verified controls**.
- **Also found, not fixed: "Auto OC core" and "Auto OC memory" silently do nothing.** They route to the legacy `StartTune` path, which has **no workload generator**, so they screen an idle GPU and return in ~1 second with `State=Completed`, `ProgressPercent=100`, zero candidates, and the benign-sounding "No candidate passed screening". A user would reasonably conclude their card has no headroom. Only "Full Auto OC" reaches `StartAutoOcV3` with the isolated workload host. These two buttons should be repointed at V3 or removed — a product decision, since V3 tunes core and memory as one operation.
- **Deploy-script robustness:** running `Update-LocalAlphaRuntime.ps1` twice in succession **silently downgrades** the active runtime — its restore-then-install flow restores the pre-existing image, so the second run reverts the first. This cost a full diagnostic cycle (a fix appeared not to work because a stale build was active). Verify the installed binary hash against the published payload before concluding a fix failed. A repeat run should be a no-op or refuse, not a rollback.
- Verification: Debug build 0 warnings/errors; **890 tests** (424 core + 466 integration) at the diagnostics fix, 893 with the restore-path tests. Auto OC V3 confirmed to genuinely execute (operation `Kind=AutoOc`, workload host launched, GPU loaded to ~109 W and 9502 MHz memory) — it is the restoration stage alone that fails.

## Verification snapshot: 2026-07-18 (Live Full Auto OC run against the deployed 0.5.6 — restore-verification fix confirmed; new baseline-load finding)

- **User-witnessed live run** against the installed 0.5.6 service (preflight Ready, dashboard 0.5.6), driven through the published app via UI Automation (`Performance.FullAutoOc`). Operation `1a4986b4bf974976b549fa4eda5b6d4e`, `Kind=AutoOc`, ran 23:38:46→23:38:56 UTC.
- **The false-RecoveryRequired failure mode is gone — the fix works live.** All three restore writes were refused exactly as Defect D predicts (`gpupower.limit:0` → `nvmlDeviceSetPowerManagementLimit` NVML result 4; both clock offsets → `NVAPI_INVALID_USER_PRIVILEGE`), but `restorationProof` read every control back at its captured prior value (350 W, 0 MHz, 0 MHz) and recorded per-control "the hardware was never moved". Result: `priorStateRestored=true`, `hardwareStateKnown=true`, **`recoveryRequired=false`, `writesEnabled=true`, no write lock** after the run. Independently confirmed with nvidia-smi before and after: 210/405 MHz idle clocks and 350.00 W limit unchanged.
- **New durable evidence for the D investigation — the run now fails one stage *earlier* than the write refusal.** `validationState=Rejected` at **Baseline sample 1**: "The screening workload did not produce at least 70% measured target-device load; no stability claim was recorded." The workload genuinely ran from the service context (average power 87.6 W vs ~30 W idle, max temp 66.7 °C, 10 s duration) but under-loaded the 350 W card (~25%). So Defect D currently has two distinct LocalSystem symptoms to explain: (a) NVAPI/NVML write refusal, and (b) the screening workload no longer reaching the 70% load bar — where a prior dev-context run reached ~109 W. Same caution as the standing contradiction note: characterise why the load differs (session context? workload-host path? measurement?) before designing any fix.
- No source changes in this pass; evidence-gathering only. The other session's uncommitted restore-verification work on this branch is untouched.

## Verification snapshot: 2026-07-18 (Live feature test sweep on 0.5.6: Auto OC, GPU fan curve, case-fan adaptive, RGB)

- **User-requested live test of four subsystems** against the deployed 0.5.6 service, driven through the published app via UI Automation + CLI, with nvidia-smi and service status as independent witnesses. No conflicting controllers were running; all GPU/fan controls Experimental and unblocked.
- **GPU fan auto curve: PASS end-to-end.** Balanced mode → cooling graph `auto.cooling.gpu-fan` Normal, commanded `gpufan.duty:0` = 53.5%, and nvidia-smi read the physical fan at 52% (was 0%). So `nvmlDeviceSetFanSpeed`-class writes **work** from LocalSystem.
- **Case-fan adaptive: PASS.** "Adaptive (pick for me)" read live temps, picked Silent ("GPU 44 °C"), and activated `auto.profile.case-fans`: six NCT6798D PWM outputs at ~31.8%, protected header (control/4) correctly excluded. Left running.
- **RGB: PASS 4/4 routes.** CLI Kraken write plus app "Sync colour to all devices": Kraken ring+logo (no read-back), both Aura channels (no read-back), Trident Z (1 DIMM controller, 20 SMBus transactions, identity `0x77='DIMM_LED-0103'`), and **Razer case acknowledged by firmware (status 0x02)** — the one verified route. Combined result honestly reported "4 applied · 0 blocked · 0 failed".
- **Legacy "Auto OC core" button is worse than documented: it now LOCKS WRITES.** On 0.5.6 it reached `RecoveryRequired` at 5% (`Kind=Tuning`) — the legacy StartTune path has no restore-verification, so the defect the V3 fix killed still fires there. Compounding it: `_rollbackBlocked` is **never cleared at runtime** (no code path sets it false; Diagnostics "Reset verified controls" does not touch it), so the only recovery is an elevated service restart. Do not press Auto OC core/memory until these buttons are repointed at V3 or removed (existing product decision).
- **Second write-lock reproduction — fan-mode switching.** With the GPU-fan graph active, activating case fans replaces the profile; releasing the GPU fan calls `nvmlDeviceSetFanControlPolicy(auto)`, which is refused (NVML result 4) → `PROFILE_REJECTED` → cooling runtime `RecoveryRequired` → full write lock (second elevated restart needed). Yet `gpu-fan-disarm` via CLI succeeded and read back "driver automatic policy" moments later. **Precise defect-D data: per-API, not blanket — SetFanSpeed works, SetFanControlPolicy(auto)/SetPowerManagementLimit/NVAPI p-state writes are refused, and the two release paths behave differently.**
- **App-side gating friction found (UI bugs, not hardware):** (1) fan-mode clicks silently no-op with a misleading "Turn on Hardware control in the header first" notice when the app's snapshot is stale (gpufan captured ReadOnly while disarmed) — Refresh fixes it; (2) the header Hardware-control toggle would not commit on a fresh service (`SetHardwareControlArmed` all-families verification fails) until the three per-family CLI arms had run once, while the checkbox visual showed On with the badge showing Off — an honest-state mismatch to fix.
- End state: service 0.5.6 healthy, writes enabled, no recovery flag; case fans on the adaptive Silent curve; GPU fan back on driver auto policy; RGB at the synced colour; GPU clocks/power stock (verified 210/405 MHz, 350 W). Evidence-gathering only; no source changes.

## Verification snapshot: 2026-07-18 (Single-domain tune restore-verification fix — deployed and live-verified)

- **The legacy-tune write-lock is fixed.** Both `RestoreOriginalAsync` implementations in `HardwareOperations.cs` (the `HardwareTuneEngine` tune path and the calibration path) now restore through `HardwareRestoreVerification.RestoreAndVerifyAsync`: a refused restore write whose read-back proves the captured prior value is a proven restore, not an unknown state. Only genuinely unproven state still escalates to ResetToDefault → `HardwareOperationRecoveryException` (unchanged fail-closed semantics). This closes the gap the V3 fix left: the "Auto OC core"/"Auto OC memory" buttons and any single-domain tune could still lock all service writes on the LocalSystem refusal.
- **Test truthfulness:** `FakeNumericAdapter` now implements `IHardwareStateVerifier` (matching every production tunable adapter — LHM, all three NVIDIA adapters, WindowsPowerAdapter, AdapterHostProxy) plus `ApplyThrows`/`RollbackThrows` modelling the live all-writes-refused shape. New regression test `RefusedWritesWithProvenReadBackFailHonestlyWithoutRecovery` pins: refused writes + proven read-back → the driver refusal propagates as the failure, no `HardwareOperationRecoveryException`, no firmware reset.
- Suite **906 tests (430 core + 476 integration), 0 warnings** — includes this fix plus the previously uncommitted single-domain workload work (`ValidateTuneWorkload`, buttons launching the user-session workload host).
- **Published 0.5.6-alpha (7 components incl. workload-host, protocol 2, Passed) and user-approved UAC deploy.** Installed `PCHelper.Core.dll` hash `E4423256…` matches the published payload exactly (the install folder is again misnamed `0.5.5-alpha-…` — the known folder-naming defect; trust the hash and the service's reported 0.5.6-alpha, not the path).
- **Live re-test of the exact failing button ("Auto OC core"):** the run now launches the isolated workload host, fails honestly at the refused apply — `state=Failed`, `error=NVAPI_INVALID_USER_PRIVILEGE`, "the prior control state was restored" — with **`writesEnabled=true`, `recoveryRequired=false`** after the run, and nvidia-smi confirming stock (210/405 MHz, 350 W). Two hours earlier this same click ended `RecoveryRequired` at 5% and needed an elevated restart.
- Still open, unchanged: Defect D itself (per-API LocalSystem refusals; the per-API map from the test sweep is the design input), the profile-swap `SetFanControlPolicy(auto)` lock path (in `ProfileTransactionEngine`/deactivation, not touched by this fix), `_rollbackBlocked` having no runtime clear, and the deploy folder-naming defect.
- **Stale "Auto-tuning started…" banner fixed (user screenshot).** The single-domain paths awaited the operation but discarded the outcome (`_ = await WaitForOperationAsync(...)`), so the optimistic start notice from `StartTuneCoreAsync` persisted after the run had already failed. New `ShowTuneOutcomeNotice` (MainViewModel.HardwareControls.cs) replaces the banner with the terminal outcome — Success only when a profile was generated; Completed-without-result, Failed (with the operation's `Error`), Aborted, RecoveryRequired (Error tone), and superseded each get honest wording — wired into both `StartGpuClockAutoOcAsync` and `StartSelectedTuneAsync` (Full Auto OC already had its own outcome notice). Dashboard-only: republished 0.5.6-alpha app (no service redeploy). Live-verified: the same click now ends with the banner reading "GPU core clock auto-tuning failed: … NVAPI_INVALID_USER_PRIVILEGE"; writes enabled, no recovery. Suite 906 tests, 0 warnings.

## Verification snapshot: 2026-07-20 (Auto OC "Defect D" retired — real cause was a refused NVML fan-policy call poisoning NVAPI, not LocalSystem; workload saturation, per-sensor thermal ceilings, plateau warmup; NVAPI fan/power transports replace refused NVML setters; fan-assist removed, pre-V3 engine deprecated)

- **Three blockers found and fixed driving Full Auto OC end-to-end, on top of the 07-18 restore-verification fix.** The workload host now saturates the GPU (a deep in-flight dispatch queue AND large-enough individual dispatches — either alone left it at 37-38% load; both together reached Core 100%, Combined 100%, Memory 96.6%, at 322-327 W / 73 °C peak). GPU temperature ceilings are now judged per sensor (edge / hot spot / memory junction) instead of the peak of every bound sensor against one operator ceiling — GDDR6X memory junction runs near the core's 83 °C ceiling by design and was halting every run at the first candidate. Refused clock writes now name domain, requested value, and the driver's own delta range in the durable operation record, because the adapter-trace buffer that used to hold this detail is a bounded 512-entry ring that per-second sensor polling flushed within seconds.
- **The clock-write refusal that the 07-18 entries called "Defect D — NVAPI/NVML writes refused in the LocalSystem service context" was wrong, and is retired.** Manual core/memory clock offsets (+15, +30, +100 MHz) applied and read back correctly from the LocalSystem service throughout this session, including while the workload host ran at 100% — session 0 was never the blocker. The actual refusal had two real causes: (a) the engine screening its own no-op 0 kHz candidate against hardware already at 0 kHz — refused identically to a rollback-to-stock when already at stock — fixed by skipping a write once read-back proves the requested state is already current; and (b) a regression introduced this same session — holding the GPU fan high via `nvmlDeviceSetFanControlPolicy(manual)` is refused `NVML_ERROR_NO_PERMISSION` on this GeForce, and left the driver session refusing subsequent NVAPI pstate writes (`NVAPI_INVALID_USER_PRIVILEGE`) even though the identical write had just succeeded at arm time. Diagnosed via a write-origin discriminator (apply/restore, current delta, arm state) added to the failure message, across three deploys; fixed by gating the fan assist off, then deleting it outright once the NVAPI fan transport replaced its purpose. NVAPI access across the core adapter, memory adapter, periodic capability probe, and tuning engine is now serialized behind one shared handle-lock — previously only `SetArmed` was guarded, every read/write was not.
- **Baseline measurement fixed twice.** First, a 45 s discarded warmup window before sampling, because a cold GPU boosts higher than a warm one (sample 1 at 76 °C / 278 W scored 302 vs. warm samples at 80-83 °C / 332 W scoring 290 — 4.03% variation against a 3% limit, purely the transient). A live re-test then showed the *fixed duration* itself was wrong: without the fan assist the card settles toward a higher equilibrium than 45 s reaches (86.4→88.0 °C still climbing through the window, 4.89% variation). Warmup now repeats discarded load windows and stops when two consecutive windows agree in peak temperature within 1 °C, capped at twelve 15 s windows, with cap-exhaustion reported explicitly so a later variation rejection can be told apart from noise. Clock regression is now measured within a run against that run's own baseline rather than a monitor-construction-time snapshot carried over from a previous mode, which had been comparing e.g. memory 9752→5842 MHz across two unrelated operating states.
- **NVML fan and power *setters* are refused on this GeForce class; NVAPI is not.** Fan control moved to a fully-written but previously unselected NVAPI cooler transport (present since the 0.4.0 capture), NVML kept only as fallback when NVAPI exposes no controllable cooler. A follow-up fix polls the fan level toward target across a bounded settle window (≤8×250 ms) before verifying, because the NVAPI cooler reports live level, not setpoint, mid-ramp — a valid 50% apply had been rolling back after reading 41% mid-ramp. **Live-verified:** 50% GPU fan apply held and read back correctly; disarm restored the automatic curve (nvidia-smi 0%). Power limit got a new `NvApiGpuPowerLimitTransport` over NVAPI client power policies (P0 entry only), converting NVAPI's PCM units to the adapter's milliwatt contract using NVML's default-TDP read (NVML reads still work; only its writes are refused) as the anchor — NVAPI preferred, NVML fallback. **Live-verified:** manual 300 W apply (throttle-only, below the 350 W default) read back at 300 W via NVAPI and nvidia-smi; disarm restored 350 W.
- **Hygiene close-out.** The fan-assist path (`TryHoldGpuFanHighAsync`, its two consts, and the V3-runner capture/restore plumbing) is deleted outright, now fully superseded by the NVAPI fan transport. The pre-V3 `FullAutoOcEngine` (reachable only through the legacy `StartAutoOc` IPC command — the app always selects V3) is marked `[Obsolete]`; the one retained legacy call site and its test file suppress CS0618 with an explaining comment, so any *new* call site fails the build under `-warnaserror`.
- Verification per commit across the session: 923 → 936 → 940 → 942 → 946 → 950 → 961 tests, 0 warnings throughout.
- **Still open, not yet live-confirmed:** whether the plateau-based warmup is tight enough across the full baseline-variation gate to let a run actually ship a candidate overclock. The prior fixed-duration warmup was confirmed insufficient live; the plateau replacement had not been re-run live as of the session's last commit. This is the next session's first task — it is a measurement-tuning question, not the architectural clock-write blocker the 07-18 entries described.

## Verification snapshot: 2026-07-20 (GPU fan mode-switch write-lock — blast radius scoped to the fan; zero-RPM root cause identified)

Driving the GPU-fan auto-mode buttons (Performance page: Silent / Balanced / Cooling / Adaptive) live on the reference rig surfaced a chain of defects. All fixes below are Debug/Release-green (975 tests, 0 warnings) and the scoping fix is live-verified; the underlying zero-RPM behaviour is diagnosed but not yet fixed.

- **The headline defect: a GPU-fan cooling failure hard-locked *every* hardware capability service-wide.** Switching auto-modes (e.g. Cooling→Silent) could leave `_rollbackBlocked = true`, which blocks all mutating IPC — GPU clock, GPU power, case fans, everything — and has **no runtime clear** (only a service restart resets it; confirmed by reading every `_rollbackBlocked` write site). A single fan hiccup bricked the whole control surface until an elevated restart.
- **Scoped the lock's blast radius (the important fix, live-verified).** A GPU fan's duty is always clamped to a safe bound before any write reaches hardware (the adapter, the graph engine, and the runtime's own step limiter all enforce it) and it is never clock/power/voltage, so an unresolved *fan* reset is a bookkeeping gap, not a physical danger. Four distinct lock sites now scope a fan-only failure to the cooling subsystem instead of the global lock: the cooling-graph replace transaction (`ReplaceActiveCoolingGraphTransactionAsync`), the continuous-tick recovery (`RecoverCoolingGraphFailureAsync`), startup/shutdown recovery (`OnlyGpuFanRecoveryFailed`), and the arm/disarm transaction (`OnlyGpuFanFamilyFailed`). Any clock or power failure still hard-locks — those domains lack the fan's structural safety bound. **Live proof:** with the fan reset failing, `gpu-power-arm` and `gpu-clock-arm` both still succeeded (`armed: true`), capabilities read `gpupower.limit:0`/`gpuclock.core:0`/`gpuclock.memory:0` = Experimental while `gpufan.duty:0` = ReadOnly, and service `writesEnabled` stayed true throughout. Before the fix, the first fan failure would have failed both subsequent arms with `RECOVERY_REQUIRED`.
- **Also fixed on the way (all tested, deployed):** (1) a graph-replacement's first tick skipped its own slew limit because `LastApplied` started empty, demanding an instant duty jump the fan couldn't make — now seeds the retained-output rate-limit state from the previous graph (`SeedRetainedOutputRateLimits`); (2) the fan's restore-to-default is retried with a cooldown+backoff and the verify settle-poll was made *less dense* (fewer NVAPI calls over a longer wall-clock window), because the rapid settle-poll burst itself was destabilising the driver session and making the safety-critical restore hit `NVAPI_INVALID_USER_PRIVILEGE`.
- **Root cause of the fan failure itself — identified, NOT yet fixed: RTX 3090 zero-RPM idle.** At idle temps (~35 °C) the card's firmware keeps the fan physically stopped (passive/zero-RPM mode). Commanding a steady low duty (Silent's 30% floor) reads back **exactly 0%**, not a mid-ramp value — the steady setpoint can't overcome the stopped fan's static friction to leave zero-RPM, so the transactional read-back verification fails. Cooling mode's higher duty (53-56%) *did* spin the fan earlier the same session, which is the tell. The proper fix is a kickstart pulse (command a brief high duty to break static friction, then settle to target) rather than treating 0%-from-stopped as a hard failure — a bigger, separate change. Until then, Silent/low-duty GPU-fan modes will fail-to-apply at idle (now safely, cooling-scoped, without locking everything).
- **A misdiagnosis worth recording:** an apparent "Silent mode computes Cooling's curve math" bug was chased for a while and turned out to be the write-lock silently swallowing cooling ticks (the lock's early-return still advances the status `updatedAt` while `outputValues` freezes) — the duty looked stuck at Cooling's value because no new tick could apply. The stored graphs were always correct (verified via a new `cooling-graphs` CLI diagnostic). Measure the lock state before theorising about curve math.

### GPU fan restore: proven driver constraint, NOT an in-process bug

The RTX 3090 on this driver ties GPU fan ownership to the **owning process**, not the NVAPI
session. Manual control can be taken but cannot be given back from inside the service. Every
in-process release path was tried live and refused — do not re-derive this:

| Attempt | Result |
| --- | --- |
| `RestoreCoolerSettingsToDefault()` | `NVAPI_INVALID_USER_PRIVILEGE` |
| `RestoreCoolerSettingsToDefault(coolerId)` | `NVAPI_INVALID_USER_PRIVILEGE` |
| `SetCoolerSettings(policy=None)` | `NVAPI_INVALID_USER_PRIVILEGE` |
| `SetCoolerSettings(policy=None, level=30)` | `NVAPI_INVALID_USER_PRIVILEGE` |
| `SetClientFanCoolersControl(Auto)` | `NVAPI_INVALID_USER_PRIVILEGE` |
| `NVIDIA.Unload()` + `Initialize()` + rebind | ran; cooler still `Manual` |

`SetCoolerSettings(Manual, level)` succeeds from that same session throughout, so the status
is misleading: it is not a privilege deficiency and not the Ampere API-generation split
(the client fan-cooler interface *reads* fine and its *write* is refused just the same).
Restarting the service restores the automatic curve every time — process exit is the only
mechanism observed to work.

**Therefore the fix is architectural:** GPU-fan NVAPI ownership belongs in a child process,
where restoring means terminating that child and letting the driver reclaim. Do not spend
further effort on the in-session restore chain; that search is exhausted.

> **RESOLVED 2026-07-20 — DO NOT BUILD THE CHILD-PROCESS DESIGN.** The decisive test finally ran
> under valid conditions (round 5): clean `Automatic` baseline verified by the script's own guard,
> probe running as **LocalSystem in session 0** via `/RU SYSTEM`, bare exit. It took ownership at
> 70%, exited (PID 17312 confirmed gone), and the fan stayed **`Manual` at 70**. A child process
> exiting does **not** return GPU fan ownership to the driver, even when it is LocalSystem in
> session 0 exactly as the service is. The architecture proposed in this section does not work.
> Total cost of learning this: one reboot and about an hour. The rounds below are kept because the
> failure modes are instructive, not because their conclusions stand.

**The load-bearing premise was tested on 2026-07-20 and is FALSE (see the resolution above).** Every
row of the table above was observed on *service* exit, because the service is the only process that
has ever owned the fan (`PCHelperRuntime.cs` constructs `NvApiGpuFanCoolerTransport` in-process). The
design assumed that generalises to a child. It does not.

Measured with `PCHelper.AdapterHost.exe --probe-gpu-fan-ownership 70`, run from an elevated
PowerShell against a clean `Automatic` baseline, with the service running and holding its own
NVAPI session:

| Step | Observed |
| --- | --- |
| Baseline after service restart | `policy=Automatic` |
| Child (PID 28436) takes manual ownership, exits without restoring | `policy=Manual` |
| After child exit, child process gone | `policy=Manual, commandedDuty=70` — **our value, still applied** |
| Service reset attempt, 16:20:46, event 2009 | every restore path refused, same `INVALID_USER_PRIVILEGE` chain |

A further service restart afterwards did **not** restore it either: the fan stayed `Manual` at 70%
across a full stop/start of the service. So the 'restart the service and it comes back' behaviour
recorded further up does not hold when the ownership was taken by something other than the service.

**The confound — this test does not falsify the design.** The probe ran from an elevated PowerShell,
i.e. as an Administrator in the *interactive user's session* (session 2). The service runs as
LocalSystem in **session 0**, and a production fan child would be spawned by the service and inherit
that. Confirmed live: `PCHelper.Service` SessionId 0, `PCHelper.App` SessionId 2. The probe therefore
tested a session-2 admin child, not the session-0 LocalSystem child the design actually calls for.
What it establishes is narrower than it first appears: *ownership taken by a session-2 admin process
is not released when that process exits, nor by a subsequent service restart.*

**Round 2 (2026-07-20, same day): the probe re-run as LocalSystem in session 0 via a `/RU SYSTEM`
scheduled task.** It executed (PID 20688, exit 0, output captured to `artifacts/fan-probe.json`)
and commanded 85%. After it exited the fan read `Manual` at **85** — its own value, so execution is
proven and **a session-0 LocalSystem child's bare exit does not release ownership either.** This
weakens hypothesis 1 below considerably. Two caveats keep it short of conclusive: the baseline was
already contaminated (`policyBefore=Manual, 70`, owned by the dead session-2 probe), and the probe
ran *without* `NVIDIA.Unload()` — see the Unload-then-exit note below, which is the more likely
confound.

**Round 3: `--dispose-before-exit` (Unload-then-exit) as LocalSystem, session 0.** Ran (PID 9888,
exit 0), commanded 60%, called `NVIDIA.Unload()`, exited. Fan afterwards: `Manual` at **60** — its
own value. **Unload-then-exit does not release either**, so the hypothesis in the next paragraph is
also dead.

**Round 4 was also void** — run without the reboot (uptime confirmed 8h53m, `policyBefore=Manual,60`),
so it repeated the same confound a third time. `artifacts/run-fan-probe.cmd` now reads the baseline
itself and **refuses to run unless the fan is `Automatic`**, writing the refusal to
`artifacts/fan-probe.json`. A run from a stuck baseline is worse than no run: it consumes a round,
looks like data, and answers nothing.

**READ THIS BEFORE RUNNING ANOTHER PROBE — rounds 2, 3 and 4 are INCONCLUSIVE, not negative.** All ran
against a fan already stuck `Manual` from the dead session-2 probe. `SetCoolerSettings(Manual, level)`
demonstrably changes the *level*, but there is no evidence it transfers *ownership* — so if ownership
remained registered to the dead session-2 process, neither probe ever owned the fan and neither exit
could possibly have released it. Every "does not release" result after the first contamination is
explained equally well by "ownership never moved". The stated decision rule was that `Automatic` would
be decisive and `Manual` would not; `Manual` came back both times, so by that rule nothing was proven.
**A clean `Automatic` baseline is a precondition for any further ownership experiment, and it currently
requires a logoff or reboot.** Do not run more probe rounds from a stuck state — they cannot answer the
question no matter what they return.

**The probe's deliberate skip of `Dispose` is itself suspect (tested in round 3, refuted).** The probe skipped disposal to be
faithful to a killed child, so it exited with the NVAPI session live. But `Dispose` calls
`NVIDIA.Unload()`, and the service's *graceful* shutdown does call it. The elimination table records
Unload as insufficient **on its own** — that was Unload *without* process exit. Unload **followed
by** process exit was never tested, and it is exactly what a clean service shutdown does: the only
configuration under which release has ever been observed. `--dispose-before-exit` was added to the
probe to test precisely this. Do not conclude the design is dead until that has been run.

**Three live hypotheses; do not pick one without testing.**
1. *Session/token-bound.* Ownership is tied to the logon session or token. A LocalSystem session-0
   child would then behave like the service, and the design still works. Predicts logging off and
   back on releases the current stuck state.
2. *Last-NVAPI-client-exits.* The driver reclaims only when no NVAPI client remains. **Weakened by
   direct test:** `PCHelper.App` (session 2) was verified to hold `nvapi64.dll` + `nvml.dll`, was
   killed, and the fan stayed `Manual` at 70%. Note also that `NVDisplay.Container` always runs, so
   "no NVAPI client remains" is never satisfiable on a live system — making this hypothesis close to
   untestable as literally stated. Any surviving variant has to be scoped (per-session, or
   non-driver clients only).
3. *Owner-process-exit, service-specific for an unidentified reason.* Weakest, but not excluded.

> ## RESOLVED 2026-07-20: the restore table was measuring the wrong variable. Restore works.
>
> `--probe-gpu-fan-owner-restore 70`, run as **LocalSystem in session 0** from a clean `Automatic`
> baseline (PID 7252, exit 0):
>
> | Field | Value |
> | --- | --- |
> | `takeOwnershipOutcome` | **accepted** |
> | `policyWhileOwned` | `Manual`, 70 — the process genuinely held the fan |
> | `ownerRestoreOutcome` | **ACCEPTED** |
> | `policyAfterRestore` | **`Automatic`** |
>
> Independently confirmed afterwards: `gpu-fan-state` read `policy=Automatic`, `cooler 1=Auto,
> cooler 2=Auto`. The stuck fan was cleared without a reboot.
>
> **The elimination table's headline claim is refuted.** "Manual control can be taken but cannot be
> given back from inside the service" is false. `RestoreCoolerSettingsToDefault` — the first of the
> six refusals in that table — is routinely *accepted*. The distinguishing variable is never
> privilege and never the Ampere API-generation split.
>
> ## ⚠ THE "STALE HANDLE" ROOT CAUSE BELOW IS WRONG. Superseded — read the correction after the table.
>
> Established by controlled comparison, 2026-07-20, with identity held constant (LocalSystem, session 0
> on both sides):
>
> | Actor | Handle age | Owns the fan? | Restore |
> | --- | --- | --- | --- |
> | Service (`gpu-fan-disarm` after a Cooling-curve apply, Manual 66%) | cached at startup, ~1 h | **yes** | **REFUSED** — all six paths `NVAPI_INVALID_USER_PRIVILEGE` (event 2009) |
> | `--probe-gpu-fan-owner-restore` | seconds | yes | ACCEPTED |
> | `--probe-gpu-fan-restore-only` | seconds | **no** | **ACCEPTED** — fan `Manual 64` → `Automatic` |
>
> The third row is decisive: a fresh handle restored a fan it **never owned**, while the service's
> startup-cached handle could not restore one it **did** own. **Ownership is irrelevant. Handle
> freshness is the variable.** An intermediate conclusion of "ownership" was drawn earlier from the
> owner-restore probe and is WRONG — that probe held a fresh handle *and* ownership, so it separated
> neither. Do not repeat that inference.
>
> **`NVAPI_INVALID_USER_PRIVILEGE` on a stale handle is a misleading status.** It reports a privilege
> problem that does not exist. That single misleading code is what sent two sessions hunting an
> architectural fix. When NVAPI returns it, re-enumerate before believing it.
>
> **Consequences:** the child-process architecture is not merely unworkable, it is *unnecessary*. Do
> not build it. That conclusion still stands — it rests on the probe results, not on the discredited
> stale-handle theory.
>
> ## CORRECTION — stale handle is NOT the cause. Fix built, deployed, and FAILED.
>
> `RefreshGpuHandle()` (re-enumerate `PhysicalGPU` before restore) was implemented, published,
> deployed as `0.5.5-alpha-20260720-175131`, and re-tested against the identical fixture (arm →
> Cooling → `Manual 66%` → `gpu-fan-disarm`). **Byte-identical failure:** `RECOVERY_REQUIRED`, event
> 2009, all six paths `INVALID_USER_PRIVILEGE`. The service's handle was three minutes old at the
> time, so age was never the variable.
>
> **Also excluded by direct test — concurrency with the cooling tick.** After the failed disarm the
> duty sat stable at 65% across six samples over 12 s (no active writer), and a second `gpu-fan-disarm`
> from the same quiesced service was refused again with a fresh 2009. So the restore is not losing a
> race against the graph engine.
>
> **The evidence now says the variable is the PROCESS, not any state within it.** A freshly-started
> process restores the fan reliably — owning it or not. The service process cannot, regardless of
> handle age, ownership, or concurrent writes, and persists in that state.
>
> **Leading hypothesis (UNTESTED — do not treat as established, the last confident root cause here was
> wrong):** `RestoreAutomaticAsync` runs each strategy exactly once, and its last strategy
> `ReleaseNvApiSessionToReclaimFan` does `NVIDIA.Unload()` + `Initialize()` + rebind and then merely
> *checks* whether the cooler came back — it never re-attempts the restore calls against the new
> session. The `--probe-gpu-fan-unload-ownership` run did exactly that missing step (Dispose → fresh
> `TryCreate` → restore) and was ACCEPTED. So the service may be one retry short: re-initialise the
> session, then run the restore strategies again.
>
> **`RefreshGpuHandle()` was REVERTED**, not left in place. It was unproven code carrying a comment
> that asserted the disproven root cause; leaving it would have read as a fix. A note at its former
> site records why, so it is not re-added without new evidence. The deployed payload
> `0.5.5-alpha-20260720-175131` still contains it and is therefore *ahead* of source on that one
> change — harmless (best-effort, restore-path only), but redeploy before treating source and
> runtime as identical.
>
> ## Exclusion table — everything ruled out by direct measurement, 2026-07-20
>
> The constant across all of it: **a freshly-started process restores the fan reliably; the service
> process never can.** Reproduction is deterministic (arm → Cooling → `Manual 66%` → `gpu-fan-disarm`
> → `RECOVERY_REQUIRED` + event 2009, all six paths `INVALID_USER_PRIVILEGE`).
>
> | # | Hypothesis | How it was excluded |
> | --- | --- | --- |
> | 1 | Bare process exit releases the fan | Round-5 probe, clean baseline, LocalSystem session 0 — fan stayed `Manual` |
> | 2 | `NVIDIA.Unload()` strips ownership | Unload → rebind → restore ACCEPTED |
> | 3 | IPC impersonation runs restore as a non-elevated caller | `RunAsClient` is scoped to identity evaluation and reverts |
> | 4 | Reclaim needs the last NVAPI client to exit | Killed `PCHelper.App` (verified NVAPI client); no change |
> | 5 | Ownership is required to restore | `restore-only` probe restored a fan it never owned |
> | 6 | Stale cached `PhysicalGPU` handle | `RefreshGpuHandle()` built, **deployed**, re-tested — identical failure; handle was 3 min old |
> | 7 | Race with the cooling-graph tick | Duty stable across 6 samples/12 s; second disarm refused again |
> | 8 | The service's other NVAPI clients poison it | Bisect 1: +NVML, +clock-offset(writes), +power-limit, +LHM capture — all 5 stages ACCEPTED |
> | 9 | NVAPI cooler control is thread-affine | Bisect 2 stage B: write on a dedicated thread, restore on main — ACCEPTED |
> | 10 | Repeated writes poison the session | Bisect 2 stage C: 20 writes then restore — ACCEPTED |
> | 11 | Ownership decays with elapsed time | Bisect 2 stage D: write, idle 60 s, restore — ACCEPTED |
> | 12 | Service token has restricted privileges | Registry: no `RequiredPrivileges`, no `ServiceSidType`, full LocalSystem |
> | 13 | Sustained NVAPI read pressure destabilises the session | Bisect 3: continuous LHM telemetry loop **and** tight read loop — both ACCEPTED |
>
> **Conclusion: external modelling is exhausted.** Thirteen hypotheses, every one measured rather than
> argued, and none reproduces the refusal outside the service. Do not add a fourteenth probe — the
> remaining difference is something about the service process that cannot be inferred from outside it.
>
> **The next step is instrumentation, not another theory.** Add diagnostics *inside* the service at the
> restore call site and capture what the probe cannot see: the NVAPI status of each strategy alongside
> the live thread id, the process/thread token and its privilege set as actually held at that instant,
> the `PhysicalGPU` handle identity, and whether any other NVAPI call is in flight. That requires a
> deploy per iteration, so it should be built as a single rich diagnostic rather than a series of
> guesses — the deploy loop is what produced both of today's wrong root causes.
>
> **Two wrong root causes were published today** ("ownership", then "handle freshness"). Both came from
> reading a single probe that varied several things at once. Every fast, single-variable experiment was
> sound. Vary one thing per run.
>
> **Bisection round 1 result (2026-07-20): NEGATIVE — the NVAPI client set is not the poison.**
> `--probe-gpu-fan-bisect` as SYSTEM (PID 19976) added, cumulatively, NVML → NVAPI clock-offset with
> writes enabled → NVAPI power limit → LibreHardwareMonitor capture, taking ownership and restoring
> at each stage. **All five stages ACCEPTED**, fan `Automatic` after every one. So rebuilding the
> service's NVAPI environment does not reproduce the refusal.
>
> **Round 2 is built and ready: `--probe-gpu-fan-bisect2`.** The remaining structural difference is
> threading. Every probe that has ever succeeded did its write and its restore **on one thread**; the
> service writes duty from the cooling graph engine's timer thread and calls restore from the IPC
> handler thread. If NVAPI binds cooler control to the writing thread, that explains every
> observation — including why `--probe-gpu-fan-restore-only`, which never writes at all, always
> succeeds. Stages: (A) same-thread control, must pass; (B) write on a dedicated thread that exits,
> restore on the main thread; (C) 20 repeated writes then restore; (D) write, idle 60 s, restore.
> First refusal names the mechanism. Runs ~2 minutes; allow 150 s. Same warning as round 1 — a
> positive result leaves the fan `Manual` at 70%, safe and over-cooled, cleared by
> `--probe-gpu-fan-restore-only`.
>
> **Superseded, kept for the record: `--probe-gpu-fan-bisect`.** It rebuilds the service's NVAPI
> environment one component at a time in a single process, because each elevated run is expensive.
> Stages, cumulative, each taking ownership cleanly then attempting restore: (0) fan transport alone,
> the control, which must pass or the run is void; (1) + NVML; (2) + NVAPI clock-offset with writes
> enabled; (3) + NVAPI power limit; (4) + LibreHardwareMonitor capture. The first stage whose restore
> is REFUSED names the poison and the probe stops there. A positive result deliberately leaves the fan
> `Manual` at 70% (safe, over-cooled) because the poisoned process cannot restore it — that is the
> finding; clear it with `--probe-gpu-fan-restore-only` from a fresh process.
>
> **Why this shape.** Two wrong root causes today both came from the slow loop (publish → deploy →
> click → disarm, ~10 min), which is slow enough to encourage reasoning over measuring. Every fast
> experiment was sound. Reproduce outside the service, then iterate.
>
> **Cheapest way to test it is NOT another deploy.** Build a probe that mimics the service process
> more closely — the service also hosts LibreHardwareMonitor, which uses NVAPI itself, plus separate
> power and clock transports, so the process holds several NVAPI consumers. Reproduce the refusal
> outside the service first, then iterate without deploy cycles. Two deploys have now been spent on
> theories that a fast local reproduction would have falsified in minutes.

**REFUTED 2026-07-20 — `NVIDIA.Unload()` does NOT strip fan ownership.** `--probe-gpu-fan-unload-ownership 70`
as SYSTEM (PID 16864): took ownership, called `Dispose()` (which unloads), rebound a fresh transport,
and `RestoreAutomaticAsync` on the **new** session was **ACCEPTED**; fan ended `Automatic`. So the
suspicion below is wrong and `ReleaseNvApiSessionToReclaimFan` is not the saboteur. Kept for the record
because it was the most plausible mechanism and needed excluding.

**Also excluded: IPC impersonation.** `NamedPipeRequestServer.EvaluateClient` impersonates only inside a
scoped `server.RunAsClient(...)` to read the client identity, and reverts; command handlers run under the
service token. A restore is therefore not executing under a non-elevated caller's token.

**`gpu-fan-arm` / `gpu-fan-disarm` do NOT reproduce the failure — arming is not owning.** Tried
2026-07-20: arm → `armed:true` but `policy` stayed `Automatic`; disarm → success, message "GPU fan
read-back confirmed the driver automatic policy". The service never held the fan, so disarm hit
`RestoreAutomaticAsync`'s early-return guard (cooler not `Manual`, nothing to undo) and reported
success **without exercising a real restore at all**. Arming only enables writes; the service takes
ownership when a cooling graph actually commands a duty. Note the side observation: a disarm on an
already-automatic cooler reports success without testing anything, so a green disarm is not evidence
that restore works.

**To reproduce, the duty must come from the App** (Performance page cooling-mode buttons — prefer
*Cooling*, whose 53-56% actually spins the fan, over *Silent*, whose low duty collides with the RTX
3090 zero-RPM idle behaviour). No CLI path can command a GPU fan duty: `direct-prepare` is
write-blocked by `--confirm-no-write` and `operation` is read-only.

**What still distinguishes the service from a working probe (untested, next):** the probe creates its
transport, owns and restores the fan within a few seconds, in a fresh process. The service creates
`NvApiGpuFanCoolerTransport` **once at startup** (`PCHelperRuntime.cs:123`) and caches the `PhysicalGPU`
handle for the whole process lifetime. A stale cached handle is a strong candidate — NVAPI is known to
report misleading statuses for invalid handles, and `INVALID_USER_PRIVILEGE` from a *long-lived* handle
while a *fresh* handle succeeds fits every observation so far. Test by re-enumerating the GPU immediately
before a restore, or by ageing a transport and comparing.

**Superseded suspicion (kept for the record).** `RestoreAutomaticAsync`'s
last-resort strategy, `ReleaseNvApiSessionToReclaimFan`, calls `NVIDIA.Unload()` + `Initialize()`.
That unload is **process-global** — its own comment says "the handle every NVAPI transport caches goes
stale". After it runs the process holds a *new* NVAPI session, which is not the fan's owner. So the
last-resort restore strategy plausibly **destroys the very ownership that makes restore work**, and
once it has run, every subsequent restore in that process is refused — permanently, until restart.
That matches the observed "restore worked once, then never again" behaviour exactly. If confirmed,
the fix is to remove or hard-gate that strategy: it is not a fallback, it is the thing that breaks
the working path. Note also that `NvApiGpuFanCoolerTransport.Dispose()` calls `NVIDIA.Unload()` too,
so *any* transport disposal in-process may strip fan ownership from the others.

**Next experiment, and it may also un-stick a stuck fan without a reboot:** `--probe-gpu-fan-owner-restore
[duty]` (written 2026-07-20, wired into `artifacts/run-fan-probe.cmd`) takes manual ownership and then
calls `RestoreAutomaticAsync` *in the same process*, reporting whether the restore is accepted. It does
not need an `Automatic` baseline — it takes ownership itself — and its verdict is guarded on that write
actually succeeding, reporting `VOID` otherwise. **Must be run elevated / as SYSTEM.**

**Confirmed 2026-07-20: NVAPI GPU-fan writes require elevation.** Run from an unelevated process, even
`SetCoolerSettings(Manual, level)` — the write path the elimination table records as reliably available —
is refused with `NVAPI_INVALID_USER_PRIVILEGE`. So `INVALID_USER_PRIVILEGE` genuinely does mean
insufficient privilege in the unelevated case, and the table's observation that it is *misleading*
applies specifically to the elevated/service context, where the manual write succeeds while restore does
not. Do not attempt fan writes from an unelevated helper; and when reading a refusal, first establish
which of the two situations produced it.

Practical notes for whoever runs these: creating a `/RU SYSTEM` task from an unelevated context fails
with `ERROR: Access is denied`, so the elevated shell is required; and `artifacts/run-fan-probe.cmd`
self-guards on an `Automatic` baseline, so it is safe to invoke at any time.

**Recovery note, learned the hard way.** Once a session-2 process has taken manual ownership and
exited, the fan is stuck: neither a service restart nor killing every non-driver NVAPI client in that
session releases it. Do not assume the documented 'restart the service' recovery applies — it only
works when the *service itself* was the owner. Plan on a logoff or reboot to clear a stuck manual
state, and prefer running ownership experiments as SYSTEM for that reason. The probe clamps duty to a
50% floor precisely so that a stuck state is over-cooled rather than under-cooled.

**Separate finding from the same run — read-back after a fan write is not immediately valid.**
The child commanded 70% and `ReadStateAsync` *immediately* after the write returned 51% — the
pre-write auto-curve value — settling to 70% only later. Both `CommandedDutyPercent` and
`MeasuredDutyPercent` come from the same `cooler.CurrentLevel` field (`GpuFanControl.cs`), so a
a caller that reads straight after a write can read the *previous* value and wrongly conclude the
write failed. Distinct mechanism from the RTX 3090 zero-RPM idle behaviour recorded above.

**Correction — this is NOT a live defect in the adapter.** On checking, `NvidiaGpuFanAdapter`
already compensates: `VerifyAsync` and `VerifyRollbackStateAsync` both go through
`ReadSettledStateAsync`, which re-polls until the level is within tolerance instead of trusting the
first read. The staleness is a property of the raw `IGpuFanCoolerTransport` read, and it matters
for anything reading the transport *directly* — the ownership probes did, which is why their
`commandedDutyAfter` fields report the pre-write value and must not be read as the write failing.
Do not "fix" the adapter for this; it is already handled.

**"Model it on the existing adapter-host process" needs a caveat — read this before building.**
Every existing adapter-host child (`--set-kraken-pump`, `--set-aura-rgb`, `--set-smbus-rgb`,
…) is a **one-shot**: it writes a register that survives process exit, then exits. GPU fan
ownership is the exact inverse — it is released *by* exit. A one-shot fan child would take
manual control and immediately drop it. The fan child must live exactly as long as manual
control is wanted: **its lifetime is the ownership window, and `RestoreAutomaticAsync` is
implemented as killing it.** That is a shape no existing host has; reuse the adapter host's
launch/token/JSON conventions, not its one-shot lifetime.

Already solved, do not rebuild: `ChildProcessJob` (used by `AdapterHostControllerDiscoveryProcess`)
wraps children in a kill-on-close job object, so orphan and crash containment is done. The
insertion seam is `IGpuFanCoolerTransport` (6 members, `GpuFanControl.cs`) — a child-backed
implementation slots in behind it with no change to the adapter's safety logic.

Related fixes already in place: the restore chain tries each path and reports every NVAPI
status (the bare exception type is identical for all NVAPI faults and says nothing); the
`gpu-fan-state` CLI reads live policy/duty through the service's privileged session — prefer
it over loading NvAPIWrapper by reflection from outside, which answers from an unprivileged
session and also trips Defender's behavioural detection; and per-family reset failures are
logged (event 2009), which is what finally surfaced the table above.

## Change discipline

- Preserve unrelated user changes and assume the working tree can be dirty.
- Do not commit, push, publish, deploy, install drivers, or install the MSI unless the user asks.
- Use fake adapters and recorded traces for CI failure paths; never make CI depend on local hardware.
- Add tests for every change to transaction order, rollback, cooling safety, persistence migration, IPC authentication, privacy, or capability classification.
- Update `README.md`, `COMPATIBILITY.md`, relevant ADRs, and this file when architecture or verified capability claims change.
- Record exact device IDs, firmware, driver versions, adapter version, and evidence. Absence of a failure report is not qualification.
- Keep public language precise: a ten-minute tuning run is `Passed 10-minute screening`, not `stable`; provisional status requires 10 active-use hours, seven calendar days, and three successful cold boots without relevant WHEA or display-driver errors.
